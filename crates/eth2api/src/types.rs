//!
//! AUTO-GENERATED CODE - DO NOT EDIT!
//!
//! Eth Beacon Node API
//! Source: build/beacon-node-oapi.json
//! Version: v4.0.0 - Ethereum Proof-of-Stake Consensus Specification v1.5.0
//! Generated by `oas3-gen v0.24.0`
//!
//! API specification for the beacon node, which enables users to query and participate in Ethereum 2.0 phase 0 beacon chain.
//! 
//! All requests by default send and receive JSON, and as such should have either or both of the "Content-Type: application/json"
//! and "Accept: application/json" headers.  In addition, some requests can send and receive data in the SSZ format.  The header
//! "Content-Type: application/octet-stream" should be set in requests that contain SSZ data; a preference to receive SSZ data in
//! response can be indicated by setting the "Accept: application/octet-stream;q=1.0,application/json;q=0.9" header.  Note that
//! only a subset of requests can respond with data in SSZ format; these are noted in each individual request.
//! 
//! When handling requests, the server should return a 415 status code if the "Content-Type" header in the request specifies a format
//! that is not supported.  Similarly, it should return a 406 status code if it cannot produce a response in the format accepted by
//! the client as specified in the "Accept" header; if no "Accept" header is provided then it is assumed to be "application/json".
//! In any case, the server should indicate the format of the response by setting the corresponding "Content-Type" header.
//! 
//! API endpoints are individually versioned.  As such, there is no direct relationship between all v1 endpoints, all v2 endpoints,
//! _etc._ and no such relationship should be inferred.  All JSON responses return the requested data under a `data` key in the top
//! level of their response.  Additional metadata may or may not be present in other keys at the top level of the response, dependent
//! on the endpoint.  The rules that require an increase in version number are as follows:
//! 
//!   - no field that is listed in an endpoint shall be removed without an increase in the version number
//!   - no field that is listed in an endpoint shall be altered in terms of format (_e.g._ from a string to an array) without an
//!     increase in the version number
//! 
//! Note that it is possible for a field to be added to an endpoint's data or metadata without an increase in the version number.
//! 

use serde::{Deserialize, Serialize};
use validator::Validate;
static REGEX_AGGREGATE_AND_PROOF_REQUEST_BODY_ARRAY_SIGNATURE: std::sync::LazyLock<
    regex::Regex,
> = std::sync::LazyLock::new(|| {
    regex::Regex::new("^0x[a-fA-F0-9]{192}$").expect("invalid regex")
});
static REGEX_ALTAIR_BEACON_STATE_CURRENT_JUSTIFIED_CHECKPOINT_ROOT: std::sync::LazyLock<
    regex::Regex,
> = std::sync::LazyLock::new(|| {
    regex::Regex::new("^0x[a-fA-F0-9]{64}$").expect("invalid regex")
});
static REGEX_CAPELLA_SIGNED_BLS_TO_EXECUTION_CHANGE_MESSAGE_FROM_BLS_PUBKEY: std::sync::LazyLock<
    regex::Regex,
> = std::sync::LazyLock::new(|| {
    regex::Regex::new("^0x[a-fA-F0-9]{96}$").expect("invalid regex")
});
static REGEX_CAPELLA_SIGNED_BLS_TO_EXECUTION_CHANGE_MESSAGE_TO_EXECUTION_ADDRESS: std::sync::LazyLock<
    regex::Regex,
> = std::sync::LazyLock::new(|| {
    regex::Regex::new("^0x[a-fA-F0-9]{40}$").expect("invalid regex")
});
static REGEX_CONTRIBUTION_AGGREGATION_BITS: std::sync::LazyLock<regex::Regex> = std::sync::LazyLock::new(||
regex::Regex::new("^0x[a-fA-F0-9]{2,}$").expect("invalid regex"));
pub const ETH_CONSENSUS_VERSION: http::HeaderName = http::HeaderName::from_static(
    "eth-consensus-version",
);
#[derive(Debug, Clone, PartialEq, Serialize, oas3_gen_support::Default)]
#[serde(untagged)]
pub enum AggregateAndProofRequestBody {
    #[default]
    Array(Vec<AggregateAndProofRequestBodyArray>),
    Array2(Vec<AggregateAndProofRequestBodyArray2>),
}
///The [`SignedAggregateAndProof`](https://github.com/ethereum/consensus-specs/blob/v1.5.0/specs/electra/validator.md#signedaggregateandproof) object
#[derive(
    Debug,
    Clone,
    PartialEq,
    Serialize,
    validator::Validate,
    oas3_gen_support::Default
)]
pub struct AggregateAndProofRequestBodyArray {
    pub message: serde_json::Value,
    #[validate(
        length(min = 1u64),
        regex(path = "REGEX_AGGREGATE_AND_PROOF_REQUEST_BODY_ARRAY_SIGNATURE")
    )]
    pub signature: String,
}
///The [`SignedAggregateAndProof`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/phase0/validator.md#signedaggregateandproof) object
#[derive(
    Debug,
    Clone,
    PartialEq,
    Serialize,
    validator::Validate,
    oas3_gen_support::Default
)]
pub struct AggregateAndProofRequestBodyArray2 {
    pub message: serde_json::Value,
    #[validate(
        length(min = 1u64),
        regex(path = "REGEX_AGGREGATE_AND_PROOF_REQUEST_BODY_ARRAY_SIGNATURE")
    )]
    pub signature: String,
}
///The [`BeaconBlockBody`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/altair/beacon-chain.md#beaconblockbody) object from the CL Altair spec.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct AltairBeaconBlockBody {
    pub attestations: Vec<GetBlockAttestationsV2ResponseResponseDataArray2>,
    pub attester_slashings: Vec<GetPoolAttesterSlashingsV2ResponseResponseDataArray2>,
    pub deposits: Vec<AltairBeaconBlockBodyDeposit>,
    ///The [`Eth1Data`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/phase0/beacon-chain.md#eth1data) object from the CL spec.
    pub eth1_data: Eth1Data,
    pub graffiti: String,
    pub proposer_slashings: Vec<GetPoolProposerSlashingsResponseResponseDatum>,
    ///The RanDAO reveal value provided by the validator.
    pub randao_reveal: String,
    ///The [`SyncAggregate`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/altair/beacon-chain.md#syncaggregate) object from the CL Altair spec.
    pub sync_aggregate: SyncAggregate,
    pub voluntary_exits: Vec<GetPoolVoluntaryExitsResponseResponseDatum>,
}
///The [`Deposit`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/phase0/beacon-chain.md#deposit) object from the CL spec.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct AltairBeaconBlockBodyDeposit {
    ///The [`DepositData`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/phase0/beacon-chain.md#depositdata) object from the CL spec.
    pub data: AltairBeaconBlockBodyDepositData,
    ///Branch in the deposit tree.
    pub proof: Vec<String>,
}
///The [`DepositData`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/phase0/beacon-chain.md#depositdata) object from the CL spec.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct AltairBeaconBlockBodyDepositData {
    ///Amount in Gwei.
    pub amount: String,
    ///The validator's BLS public key, uniquely identifying them. _48-bytes, hex encoded with 0x prefix, case insensitive._
    pub pubkey: String,
    ///Container self-signature.
    pub signature: String,
    ///The withdrawal credentials.
    pub withdrawal_credentials: String,
}
///The [`Checkpoint`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/phase0/beacon-chain.md#checkpoint) object from the CL spec.
#[derive(
    Debug,
    Clone,
    PartialEq,
    Serialize,
    Deserialize,
    validator::Validate,
    oas3_gen_support::Default
)]
pub struct AltairBeaconStateCurrentJustifiedCheckpoint {
    #[validate(length(min = 1u64))]
    pub epoch: String,
    #[validate(
        length(min = 1u64),
        regex(path = "REGEX_ALTAIR_BEACON_STATE_CURRENT_JUSTIFIED_CHECKPOINT_ROOT")
    )]
    pub root: String,
}
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct AltairBeaconStateCurrentSyncCommittee {
    ///The validator's BLS public key, uniquely identifying them. _48-bytes, hex encoded with 0x prefix, case insensitive._
    pub aggregate_pubkey: String,
    pub pubkeys: Vec<String>,
}
#[derive(
    Debug,
    Clone,
    PartialEq,
    Serialize,
    validator::Validate,
    oas3_gen_support::Default
)]
pub struct AltairSignedContributionAndProofMessage {
    ///Index of validator in validator registry.
    #[validate(length(min = 1u64))]
    pub aggregator_index: String,
    #[validate(nested)]
    pub contribution: Contribution,
    #[validate(
        length(min = 1u64),
        regex(path = "REGEX_AGGREGATE_AND_PROOF_REQUEST_BODY_ARRAY_SIGNATURE")
    )]
    pub selection_proof: String,
}
pub type AttestationRequestBody = Vec<String>;
#[derive(Debug, Clone, PartialEq, Serialize, oas3_gen_support::Default)]
#[serde(untagged)]
pub enum AttestationRequestBody2 {
    #[default]
    Array(Vec<AttestationRequestBody2Array>),
    Array2(Vec<GetBlockAttestationsV2ResponseResponseDataArray2>),
}
///The [`SingleAttestation`](https://github.com/ethereum/consensus-specs/blob/v1.5.0/specs/electra/beacon-chain.md#singleattestation) object from the CL spec.
#[derive(
    Debug,
    Clone,
    PartialEq,
    Serialize,
    validator::Validate,
    oas3_gen_support::Default
)]
pub struct AttestationRequestBody2Array {
    ///The validator index that signed this attestation.
    #[validate(length(min = 1u64))]
    pub attester_index: String,
    ///The attestations committee index.
    #[validate(length(min = 1u64))]
    pub committee_index: String,
    ///The [`AttestationData`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/phase0/beacon-chain.md#attestationdata) object from the CL spec.
    #[validate(nested)]
    pub data: Data,
    ///BLS aggregate signature.
    #[validate(
        length(min = 1u64),
        regex(path = "REGEX_AGGREGATE_AND_PROOF_REQUEST_BODY_ARRAY_SIGNATURE")
    )]
    pub signature: String,
}
#[derive(Debug, Clone, PartialEq, Serialize, oas3_gen_support::Default)]
#[serde(untagged)]
pub enum AttesterSlashingRequestBody {
    ///The [`AttesterSlashing`](https://github.com/ethereum/consensus-specs/blob/v1.5.0/specs/electra/beacon-chain.md#attesterslashing) object from the CL spec.
    #[default]
    Object(GetPoolAttesterSlashingsV2ResponseResponseDataArray),
    ///The [`AttesterSlashing`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/phase0/beacon-chain.md#attesterslashing) object from the CL spec.
    Object2(GetPoolAttesterSlashingsV2ResponseResponseDataArray2),
}
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct BeaconCommitteeSelection501Response {
    ///Either specific error code in case of invalid request or http status code
    pub code: f64,
    ///Message describing error
    pub message: String,
    ///Optional stacktraces, sent when node is in debug mode
    pub stacktraces: Option<Vec<String>>,
}
pub type BeaconCommitteeSelectionRequestRequestBody = Vec<
    BeaconCommitteeSelectionRequestRequestBodyItem,
>;
#[derive(
    Debug,
    Clone,
    PartialEq,
    Serialize,
    Deserialize,
    validator::Validate,
    oas3_gen_support::Default
)]
pub struct BeaconCommitteeSelectionRequestRequestBodyItem {
    ///The `slot_signature` calculated by the validator for the upcoming attestation slot
    #[validate(
        length(min = 1u64),
        regex(path = "REGEX_AGGREGATE_AND_PROOF_REQUEST_BODY_ARRAY_SIGNATURE")
    )]
    pub selection_proof: String,
    ///The slot at which a validator is assigned to attest
    #[validate(length(min = 1u64))]
    pub slot: String,
    ///Index of the validator
    #[validate(length(min = 1u64))]
    pub validator_index: String,
}
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct BeaconCommitteeSelectionResponseResponse {
    pub data: Vec<BeaconCommitteeSelectionRequestRequestBodyItem>,
}
///The [`Fork`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/phase0/beacon-chain.md#fork) object from the CL spec.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct BeaconStateFork {
    ///a fork version number
    pub current_version: String,
    pub epoch: String,
    ///a fork version number
    pub previous_version: String,
}
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct BlindedBlock400Response {
    ///Either specific error code in case of invalid request or http status code
    pub code: f64,
    ///Message describing error
    pub message: String,
    ///Optional stacktraces, sent when node is in debug mode
    pub stacktraces: Option<Vec<String>>,
}
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct BlindedBlock406Response {
    ///The media type in "Accept" header is unsupported, and the request has been rejected. This occurs when the server cannot produce a response in the format accepted by the client.
    pub code: f64,
    ///Message describing error
    pub message: String,
    ///Optional stacktraces, sent when node is in debug mode
    pub stacktraces: Option<Vec<String>>,
}
#[derive(Debug, Clone, PartialEq, Serialize, oas3_gen_support::Default)]
#[serde(untagged)]
pub enum BlockRequestBody {
    ///The required signed components of block production according to the Fulu CL spec.
    #[default]
    Object(BlockRequestBodyObject),
    ///The required signed components of block production according to the Electra CL spec.
    Object2(BlockRequestBodyObject2),
    ///The required signed components of block production according to the Deneb CL spec.
    Object3(BlockRequestBodyObject3),
    ///The [`SignedBeaconBlock`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/phase0/beacon-chain.md#signedbeaconblock) object envelope from the CL Capella spec.
    Object4(BlockRequestBodyObject4),
    ///The [`SignedBeaconBlock`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/phase0/beacon-chain.md#signedbeaconblock) object envelope from the CL Bellatrix spec.
    Object5(BlockRequestBodyObject5),
    ///The [`SignedBeaconBlock`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/phase0/beacon-chain.md#signedbeaconblock) object envelope from the CL Altair spec.
    Object6(GetBlindedBlockResponseResponseDataObject5),
    ///The [`SignedBeaconBlock`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/phase0/beacon-chain.md#signedbeaconblock) object envelope from the CL spec.
    Object7(GetBlindedBlockResponseResponseDataObject6),
}
///The required signed components of block production according to the Fulu CL spec.
#[derive(
    Debug,
    Clone,
    PartialEq,
    Serialize,
    validator::Validate,
    oas3_gen_support::Default
)]
pub struct BlockRequestBodyObject {
    #[validate(length(min = 0u64, max = 4_096u64))]
    pub blobs: Vec<String>,
    ///Cell proofs of the blobs as defined in EIP-7594
    #[validate(length(min = 0u64, max = 33_554_432u64))]
    pub kzg_proofs: Vec<String>,
    ///The [`SignedBeaconBlock`](https://github.com/ethereum/consensus-specs/blob/v1.5.0/specs/phase0/beacon-chain.md#signedbeaconblock) object envelope from the CL Electra spec.
    #[validate(nested)]
    pub signed_block: SignedBlockContentsSignedBlock,
}
///The required signed components of block production according to the Electra CL spec.
#[derive(
    Debug,
    Clone,
    PartialEq,
    Serialize,
    validator::Validate,
    oas3_gen_support::Default
)]
pub struct BlockRequestBodyObject2 {
    #[validate(length(min = 0u64, max = 4_096u64))]
    pub blobs: Vec<String>,
    #[validate(length(min = 0u64, max = 4_096u64))]
    pub kzg_proofs: Vec<String>,
    ///The [`SignedBeaconBlock`](https://github.com/ethereum/consensus-specs/blob/v1.5.0/specs/phase0/beacon-chain.md#signedbeaconblock) object envelope from the CL Electra spec.
    #[validate(nested)]
    pub signed_block: SignedBlockContentsSignedBlock,
}
///The required signed components of block production according to the Deneb CL spec.
#[derive(
    Debug,
    Clone,
    PartialEq,
    Serialize,
    validator::Validate,
    oas3_gen_support::Default
)]
pub struct BlockRequestBodyObject3 {
    #[validate(length(min = 0u64, max = 4_096u64))]
    pub blobs: Vec<String>,
    #[validate(length(min = 0u64, max = 4_096u64))]
    pub kzg_proofs: Vec<String>,
    ///The [`SignedBeaconBlock`](https://github.com/ethereum/consensus-specs/blob/v1.4.0/specs/phase0/beacon-chain.md#signedbeaconblock) object envelope from the CL Deneb spec.
    #[validate(nested)]
    pub signed_block: DenebSignedBlockContentsSignedBlock,
}
///The [`SignedBeaconBlock`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/phase0/beacon-chain.md#signedbeaconblock) object envelope from the CL Capella spec.
#[derive(
    Debug,
    Clone,
    PartialEq,
    Serialize,
    Deserialize,
    validator::Validate,
    oas3_gen_support::Default
)]
pub struct BlockRequestBodyObject4 {
    ///The [`BeaconBlock`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/phase0/beacon-chain.md#beaconblock) object from the CL Capella spec.
    pub message: serde_json::Value,
    #[validate(
        length(min = 1u64),
        regex(path = "REGEX_AGGREGATE_AND_PROOF_REQUEST_BODY_ARRAY_SIGNATURE")
    )]
    pub signature: String,
}
///The [`SignedBeaconBlock`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/phase0/beacon-chain.md#signedbeaconblock) object envelope from the CL Bellatrix spec.
#[derive(
    Debug,
    Clone,
    PartialEq,
    Serialize,
    Deserialize,
    validator::Validate,
    oas3_gen_support::Default
)]
pub struct BlockRequestBodyObject5 {
    ///The [`BeaconBlock`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/phase0/beacon-chain.md#beaconblock) object from the CL Bellatrix spec.
    pub message: serde_json::Value,
    #[validate(
        length(min = 1u64),
        regex(path = "REGEX_AGGREGATE_AND_PROOF_REQUEST_BODY_ARRAY_SIGNATURE")
    )]
    pub signature: String,
}
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct BlsToExecutionChange400Response {
    ///Either specific error code in case of invalid request or http status code
    pub code: f64,
    ///List of individual items that have failed
    pub failures: Vec<BlsToExecutionChange400ResponseFailure>,
    ///Message describing error
    pub message: String,
}
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct BlsToExecutionChange400ResponseFailure {
    ///Index of item in the request list that caused the error
    pub index: f64,
    ///Message describing error
    pub message: String,
}
pub type BlsToExecutionChangeRequestBody = Vec<
    GetPoolBLSToExecutionChangesResponseResponseDatum,
>;
///Level of validation that must be applied to a block before it is broadcast.
#[derive(Debug, Clone, PartialEq, Eq, Hash, Serialize, oas3_gen_support::Default)]
pub enum BroadcastValidation {
    #[serde(rename = "gossip")]
    #[default]
    Gossip,
    #[serde(rename = "consensus")]
    Consensus,
    #[serde(rename = "consensus_and_equivocation")]
    ConsensusAndEquivocation,
}
impl core::fmt::Display for BroadcastValidation {
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        match self {
            Self::Gossip => write!(f, "gossip"),
            Self::Consensus => write!(f, "consensus"),
            Self::ConsensusAndEquivocation => write!(f, "consensus_and_equivocation"),
        }
    }
}
///The [`BLSToExecutionChange`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/capella/beacon-chain.md#blstoexecutionchange) object from the CL spec.
#[derive(
    Debug,
    Clone,
    PartialEq,
    Serialize,
    Deserialize,
    validator::Validate,
    oas3_gen_support::Default
)]
pub struct CapellaSignedBlsToExecutionChangeMessage {
    ///The validator's BLS public key, uniquely identifying them. _48-bytes, hex encoded with 0x prefix, case insensitive._
    #[validate(
        length(min = 1u64),
        regex(
            path = "REGEX_CAPELLA_SIGNED_BLS_TO_EXECUTION_CHANGE_MESSAGE_FROM_BLS_PUBKEY"
        )
    )]
    pub from_bls_pubkey: String,
    ///Execution address to which the credentials will be changed.
    #[validate(
        length(min = 1u64),
        regex(
            path = "REGEX_CAPELLA_SIGNED_BLS_TO_EXECUTION_CHANGE_MESSAGE_TO_EXECUTION_ADDRESS"
        )
    )]
    pub to_execution_address: String,
    ///Index of the validator for which credentials will be changed.
    #[validate(length(min = 1u64))]
    pub validator_index: String,
}
#[derive(
    Debug,
    Clone,
    PartialEq,
    Eq,
    Hash,
    Serialize,
    Deserialize,
    oas3_gen_support::Default
)]
pub enum ConsensusVersion {
    #[serde(rename = "phase0")]
    #[default]
    Phase0,
    #[serde(rename = "altair")]
    Altair,
    #[serde(rename = "bellatrix")]
    Bellatrix,
    #[serde(rename = "capella")]
    Capella,
    #[serde(rename = "deneb")]
    Deneb,
    #[serde(rename = "electra")]
    Electra,
    #[serde(rename = "fulu")]
    Fulu,
}
impl core::fmt::Display for ConsensusVersion {
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        match self {
            Self::Phase0 => write!(f, "phase0"),
            Self::Altair => write!(f, "altair"),
            Self::Bellatrix => write!(f, "bellatrix"),
            Self::Capella => write!(f, "capella"),
            Self::Deneb => write!(f, "deneb"),
            Self::Electra => write!(f, "electra"),
            Self::Fulu => write!(f, "fulu"),
        }
    }
}
#[derive(
    Debug,
    Clone,
    PartialEq,
    Serialize,
    Deserialize,
    validator::Validate,
    oas3_gen_support::Default
)]
pub struct Contribution {
    ///A bit is set if a signature from the validator at the corresponding index in the subcommittee is present in the aggregate `signature`.
    #[validate(length(min = 1u64), regex(path = "REGEX_CONTRIBUTION_AGGREGATION_BITS"))]
    pub aggregation_bits: String,
    ///Block root for this contribution.
    #[validate(
        length(min = 1u64),
        regex(path = "REGEX_ALTAIR_BEACON_STATE_CURRENT_JUSTIFIED_CHECKPOINT_ROOT")
    )]
    pub beacon_block_root: String,
    ///Signature by the validator(s) over the block root of `slot`
    #[validate(
        length(min = 1u64),
        regex(path = "REGEX_AGGREGATE_AND_PROOF_REQUEST_BODY_ARRAY_SIGNATURE")
    )]
    pub signature: String,
    ///The slot at which the validator is providing a sync committee contribution.
    #[validate(length(min = 1u64))]
    pub slot: String,
    ///The index of the subcommittee that the contribution pertains to.
    #[validate(length(min = 1u64))]
    pub subcommittee_index: String,
}
pub type ContributionAndProofRequestBody = Vec<ContributionAndProofRequestBodyItem>;
#[derive(
    Debug,
    Clone,
    PartialEq,
    Serialize,
    validator::Validate,
    oas3_gen_support::Default
)]
pub struct ContributionAndProofRequestBodyItem {
    #[validate(nested)]
    pub message: AltairSignedContributionAndProofMessage,
    #[validate(
        length(min = 1u64),
        regex(path = "REGEX_AGGREGATE_AND_PROOF_REQUEST_BODY_ARRAY_SIGNATURE")
    )]
    pub signature: String,
}
///The [`AttestationData`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/phase0/beacon-chain.md#attestationdata) object from the CL spec.
#[derive(
    Debug,
    Clone,
    PartialEq,
    Serialize,
    Deserialize,
    validator::Validate,
    oas3_gen_support::Default
)]
pub struct Data {
    ///LMD GHOST vote.
    #[validate(
        length(min = 1u64),
        regex(path = "REGEX_ALTAIR_BEACON_STATE_CURRENT_JUSTIFIED_CHECKPOINT_ROOT")
    )]
    pub beacon_block_root: String,
    #[validate(length(min = 1u64))]
    pub index: String,
    #[validate(length(min = 1u64))]
    pub slot: String,
    ///The [`Checkpoint`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/phase0/beacon-chain.md#checkpoint) object from the CL spec.
    #[validate(nested)]
    pub source: AltairBeaconStateCurrentJustifiedCheckpoint,
    ///The [`Checkpoint`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/phase0/beacon-chain.md#checkpoint) object from the CL spec.
    #[validate(nested)]
    pub target: AltairBeaconStateCurrentJustifiedCheckpoint,
}
///The [`SignedBeaconBlock`](https://github.com/ethereum/consensus-specs/blob/v1.4.0/specs/phase0/beacon-chain.md#signedbeaconblock) object envelope from the CL Deneb spec.
#[derive(
    Debug,
    Clone,
    PartialEq,
    Serialize,
    Deserialize,
    validator::Validate,
    oas3_gen_support::Default
)]
pub struct DenebSignedBlockContentsSignedBlock {
    ///The [`BeaconBlock`](https://github.com/ethereum/consensus-specs/blob/v1.4.0/specs/phase0/beacon-chain.md#beaconblock) object from the CL Deneb spec.
    pub message: serde_json::Value,
    #[validate(
        length(min = 1u64),
        regex(path = "REGEX_AGGREGATE_AND_PROOF_REQUEST_BODY_ARRAY_SIGNATURE")
    )]
    pub signature: String,
}
///The [`IndexedAttestation`](https://github.com/ethereum/consensus-specs/blob/v1.5.0/specs/electra/beacon-chain.md#indexedattestation) object from the CL spec.
#[derive(
    Debug,
    Clone,
    PartialEq,
    Serialize,
    Deserialize,
    validator::Validate,
    oas3_gen_support::Default
)]
pub struct ElectraAttesterSlashingAttestation1 {
    ///Attesting validator indices
    #[validate(length(max = 131_072u64))]
    pub attesting_indices: Vec<String>,
    ///The [`AttestationData`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/phase0/beacon-chain.md#attestationdata) object from the CL spec.
    #[validate(nested)]
    pub data: Data,
    ///The BLS signature of the `IndexedAttestation`, created by the validator of the attestation.
    #[validate(
        length(min = 1u64),
        regex(path = "REGEX_AGGREGATE_AND_PROOF_REQUEST_BODY_ARRAY_SIGNATURE")
    )]
    pub signature: String,
}
///The [`Eth1Data`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/phase0/beacon-chain.md#eth1data) object from the CL spec.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct Eth1Data {
    ///Ethereum 1.x block hash.
    pub block_hash: String,
    ///Total number of deposits.
    pub deposit_count: String,
    ///Root of the deposit tree.
    pub deposit_root: String,
}
///Provides endpoint to subscribe to beacon node Server-Sent-Events stream.
///Consumers should use [eventsource](https://html.spec.whatwg.org/multipage/server-sent-events.html#the-eventsource-interface)
///implementation to listen on those events.
///
///Servers _may_ send SSE comments beginning with `:` for any purpose, including to keep the
///event stream connection alive in the presence of proxy servers.
#[derive(Debug, Clone, validator::Validate, oas3_gen_support::Default)]
pub struct EventstreamRequest {
    pub query: EventstreamRequestQuery,
}
#[bon::bon]
impl EventstreamRequest {
    ///Create a new request with the given parameters.
    #[builder]
    pub fn new(topics: Vec<EventstreamRequestQueryTopic>) -> anyhow::Result<Self> {
        let request = Self {
            query: EventstreamRequestQuery { topics },
        };
        request.validate()?;
        Ok(request)
    }
}
impl EventstreamRequest {
    ///Parse the HTTP response into the response enum.
    pub async fn parse_response(
        req: reqwest::Response,
    ) -> anyhow::Result<EventstreamResponse> {
        let status = req.status();
        if status.is_success() {
            let data = <oas3_gen_support::EventStream<String>>::from_response(req);
            return Ok(EventstreamResponse::Ok(data));
        }
        if status.as_u16() == 400u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(EventstreamResponse::BadRequest(data));
        }
        if status.as_u16() == 500u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(EventstreamResponse::InternalServerError(data));
        }
        let _ = req.bytes().await?;
        return Ok(EventstreamResponse::Unknown);
    }
}
#[derive(Debug, Clone, PartialEq, Serialize, oas3_gen_support::Default)]
pub struct EventstreamRequestQuery {
    ///Event types to subscribe to
    pub topics: Vec<EventstreamRequestQueryTopic>,
}
#[derive(Debug, Clone, PartialEq, Eq, Hash, Serialize, oas3_gen_support::Default)]
pub enum EventstreamRequestQueryTopic {
    #[serde(rename = "head")]
    #[default]
    Head,
    #[serde(rename = "block")]
    Block,
    #[serde(rename = "block_gossip")]
    BlockGossip,
    #[serde(rename = "attestation")]
    Attestation,
    #[serde(rename = "single_attestation")]
    SingleAttestation,
    #[serde(rename = "voluntary_exit")]
    VoluntaryExit,
    #[serde(rename = "bls_to_execution_change")]
    BlsToExecutionChange,
    #[serde(rename = "proposer_slashing")]
    ProposerSlashing,
    #[serde(rename = "attester_slashing")]
    AttesterSlashing,
    #[serde(rename = "finalized_checkpoint")]
    FinalizedCheckpoint,
    #[serde(rename = "chain_reorg")]
    ChainReorg,
    #[serde(rename = "contribution_and_proof")]
    ContributionAndProof,
    #[serde(rename = "light_client_finality_update")]
    LightClientFinalityUpdate,
    #[serde(rename = "light_client_optimistic_update")]
    LightClientOptimisticUpdate,
    #[serde(rename = "payload_attributes")]
    PayloadAttributes,
    #[serde(rename = "blob_sidecar")]
    BlobSidecar,
    #[serde(rename = "data_column_sidecar")]
    DataColumnSidecar,
}
impl core::fmt::Display for EventstreamRequestQueryTopic {
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        match self {
            Self::Head => write!(f, "head"),
            Self::Block => write!(f, "block"),
            Self::BlockGossip => write!(f, "block_gossip"),
            Self::Attestation => write!(f, "attestation"),
            Self::SingleAttestation => write!(f, "single_attestation"),
            Self::VoluntaryExit => write!(f, "voluntary_exit"),
            Self::BlsToExecutionChange => write!(f, "bls_to_execution_change"),
            Self::ProposerSlashing => write!(f, "proposer_slashing"),
            Self::AttesterSlashing => write!(f, "attester_slashing"),
            Self::FinalizedCheckpoint => write!(f, "finalized_checkpoint"),
            Self::ChainReorg => write!(f, "chain_reorg"),
            Self::ContributionAndProof => write!(f, "contribution_and_proof"),
            Self::LightClientFinalityUpdate => write!(f, "light_client_finality_update"),
            Self::LightClientOptimisticUpdate => {
                write!(f, "light_client_optimistic_update")
            }
            Self::PayloadAttributes => write!(f, "payload_attributes"),
            Self::BlobSidecar => write!(f, "blob_sidecar"),
            Self::DataColumnSidecar => write!(f, "data_column_sidecar"),
        }
    }
}
///Response types for eventstream
#[derive(Debug)]
pub enum EventstreamResponse {
    ///200: Opened SSE stream.
    Ok(oas3_gen_support::EventStream<String>),
    ///400: The topics supplied could not be parsed
    BadRequest(BlindedBlock400Response),
    ///500: Beacon node internal error.
    InternalServerError(BlindedBlock400Response),
    ///default: Unknown response
    Unknown,
}
///Aggregates all attestations matching given attestation data root, slot and committee index.
///
///A 503 error must be returned if the block identified by the response
///`beacon_block_root` is optimistic (i.e. the aggregated attestation attests
///to a block that has not been fully verified by an execution engine).
///
///A 404 error must be returned if no attestation is available for the requested
///`attestation_data_root`.
#[derive(Debug, Clone, validator::Validate, oas3_gen_support::Default)]
pub struct GetAggregatedAttestationV2Request {
    #[validate(nested)]
    pub query: GetAggregatedAttestationV2RequestQuery,
}
#[bon::bon]
impl GetAggregatedAttestationV2Request {
    ///Create a new request with the given parameters.
    #[builder]
    pub fn new(
        attestation_data_root: String,
        slot: String,
        committee_index: String,
    ) -> anyhow::Result<Self> {
        let request = Self {
            query: GetAggregatedAttestationV2RequestQuery {
                attestation_data_root,
                slot,
                committee_index,
            },
        };
        request.validate()?;
        Ok(request)
    }
}
impl GetAggregatedAttestationV2Request {
    ///Parse the HTTP response into the response enum.
    pub async fn parse_response(
        req: reqwest::Response,
    ) -> anyhow::Result<GetAggregatedAttestationV2Response> {
        let status = req.status();
        if status.is_success() {
            let content_type_str = req
                .headers()
                .get(reqwest::header::CONTENT_TYPE)
                .and_then(|v| v.to_str().ok())
                .unwrap_or("application/json");
            if content_type_str.contains("json") {
                let data = oas3_gen_support::Diagnostics::<
                    GetAggregatedAttestationV2ResponseResponse,
                >::json_with_diagnostics(req)
                    .await?;
                return Ok(GetAggregatedAttestationV2Response::Ok(data));
            }
            if content_type_str.starts_with("application/octet-stream")
                || content_type_str.starts_with("image/")
                || content_type_str.starts_with("audio/")
                || content_type_str.starts_with("video/")
            {
                let data = req.bytes().await?.to_vec();
                return Ok(GetAggregatedAttestationV2Response::OkBinary(data));
            }
        }
        if status.as_u16() == 400u16 {
            let data = oas3_gen_support::Diagnostics::<
                PendingConsolidation400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetAggregatedAttestationV2Response::BadRequest(data));
        }
        if status.as_u16() == 404u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetAggregatedAttestationV2Response::NotFound(data));
        }
        if status.as_u16() == 406u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock406Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetAggregatedAttestationV2Response::NotAcceptable(data));
        }
        if status.as_u16() == 500u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetAggregatedAttestationV2Response::InternalServerError(data));
        }
        let _ = req.bytes().await?;
        return Ok(GetAggregatedAttestationV2Response::Unknown);
    }
}
#[derive(
    Debug,
    Clone,
    PartialEq,
    Serialize,
    validator::Validate,
    oas3_gen_support::Default
)]
pub struct GetAggregatedAttestationV2RequestQuery {
    ///HashTreeRoot of AttestationData that validator wants aggregated
    #[validate(
        length(min = 1u64),
        regex(path = "REGEX_ALTAIR_BEACON_STATE_CURRENT_JUSTIFIED_CHECKPOINT_ROOT")
    )]
    pub attestation_data_root: String,
    #[validate(length(min = 1u64))]
    pub slot: String,
    #[validate(length(min = 1u64))]
    pub committee_index: String,
}
///Response types for getAggregatedAttestationV2
#[derive(Debug, Clone)]
pub enum GetAggregatedAttestationV2Response {
    ///200: Returns aggregated `Attestation` object with same `AttestationData` root, slot and committee index.
    Ok(GetAggregatedAttestationV2ResponseResponse),
    ///200: Returns aggregated `Attestation` object with same `AttestationData` root, slot and committee index.
    OkBinary(Vec<u8>),
    ///400: Invalid request syntax.
    BadRequest(PendingConsolidation400Response),
    ///404: Not found
    NotFound(BlindedBlock400Response),
    ///406: Accepted media type is not supported.
    NotAcceptable(BlindedBlock406Response),
    ///500: Beacon node internal error.
    InternalServerError(BlindedBlock400Response),
    ///default: Unknown response
    Unknown,
}
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct GetAggregatedAttestationV2ResponseResponse {
    pub data: GetAggregatedAttestationV2ResponseResponseData,
    pub version: ConsensusVersion,
}
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
#[serde(untagged)]
pub enum GetAggregatedAttestationV2ResponseResponseData {
    ///The [`Attestation`](https://github.com/ethereum/consensus-specs/blob/v1.5.0/specs/electra/beacon-chain.md#attestation) object from the CL spec.
    #[default]
    Object(GetBlockAttestationsV2ResponseResponseDataArray),
    ///The [`Attestation`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/phase0/beacon-chain.md#attestation) object from the CL spec.
    Object2(GetBlockAttestationsV2ResponseResponseDataArray2),
}
///Retrieve attestation reward info for validators specified by array of public keys or validator index. If no array is provided, return reward info for every validator.
#[derive(Debug, Clone, validator::Validate, oas3_gen_support::Default)]
pub struct GetAttestationsRewardsRequest {
    #[validate(nested)]
    pub path: GetAttestationsRewardsRequestPath,
    ///An array of either hex encoded public key (any bytes48 with 0x prefix) or validator index
    pub body: Option<AttestationRequestBody>,
}
#[bon::bon]
impl GetAttestationsRewardsRequest {
    ///Create a new request with the given parameters.
    #[builder]
    pub fn new(
        epoch: String,
        body: Option<AttestationRequestBody>,
    ) -> anyhow::Result<Self> {
        let request = Self {
            path: GetAttestationsRewardsRequestPath {
                epoch,
            },
            body,
        };
        request.validate()?;
        Ok(request)
    }
}
impl GetAttestationsRewardsRequest {
    ///Parse the HTTP response into the response enum.
    pub async fn parse_response(
        req: reqwest::Response,
    ) -> anyhow::Result<GetAttestationsRewardsResponse> {
        let status = req.status();
        if status.is_success() {
            let data = oas3_gen_support::Diagnostics::<
                GetAttestationsRewardsResponseResponse,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetAttestationsRewardsResponse::Ok(data));
        }
        if status.as_u16() == 400u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetAttestationsRewardsResponse::BadRequest(data));
        }
        if status.as_u16() == 404u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetAttestationsRewardsResponse::NotFound(data));
        }
        if status.as_u16() == 500u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetAttestationsRewardsResponse::InternalServerError(data));
        }
        let _ = req.bytes().await?;
        return Ok(GetAttestationsRewardsResponse::Unknown);
    }
}
#[derive(Debug, Clone, PartialEq, validator::Validate, oas3_gen_support::Default)]
pub struct GetAttestationsRewardsRequestPath {
    ///The epoch to get rewards info from
    #[validate(length(min = 1u64))]
    pub epoch: String,
}
///Response types for getAttestationsRewards
#[derive(Debug, Clone)]
pub enum GetAttestationsRewardsResponse {
    ///200: Success
    Ok(GetAttestationsRewardsResponseResponse),
    ///400: Invalid get attestations rewards request
    BadRequest(BlindedBlock400Response),
    ///404: Epoch not known or required data not available
    NotFound(BlindedBlock400Response),
    ///500: Beacon node internal error.
    InternalServerError(BlindedBlock400Response),
    ///default: Unknown response
    Unknown,
}
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct GetAttestationsRewardsResponseResponse {
    ///Rewards info for attestations
    pub data: GetAttestationsRewardsResponseResponseData,
    ///True if the response references an unverified execution payload. Optimistic information may be invalidated at a later time. If the field is not present, assume the False value.
    pub execution_optimistic: bool,
    ///True if the response references the finalized history of the chain, as determined by fork choice. If the field is not present, additional calls are necessary to compare the epoch of the requested information with the finalized checkpoint.
    pub finalized: bool,
}
///Rewards info for attestations
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct GetAttestationsRewardsResponseResponseData {
    pub ideal_rewards: Vec<GetAttestationsRewardsResponseResponseDataIdealReward>,
    pub total_rewards: Vec<GetAttestationsRewardsResponseResponseDataTotalReward>,
}
///Ideal rewards info for a single attestation
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct GetAttestationsRewardsResponseResponseDataIdealReward {
    ///validator's effective balance in gwei
    pub effective_balance: String,
    ///Ideal attester's reward for head vote in gwei
    pub head: String,
    ///Ideal attester's inactivity penalty in gwei
    pub inactivity: String,
    ///Ideal attester's inclusion_delay reward in gwei (phase0 only)
    pub inclusion_delay: Option<String>,
    ///Ideal attester's reward for source vote in gwei
    pub source: String,
    ///Ideal attester's reward for target vote in gwei
    pub target: String,
}
///Rewards info for a single attestation
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct GetAttestationsRewardsResponseResponseDataTotalReward {
    ///attester's reward for head vote in gwei
    pub head: String,
    ///attester's inactivity penalty in gwei
    pub inactivity: String,
    ///attester's inclusion_delay reward in gwei (phase0 only)
    pub inclusion_delay: Option<String>,
    ///attester's reward for source vote in gwei
    pub source: String,
    ///attester's reward for target vote in gwei
    pub target: String,
    ///one entry for every validator based on their attestations in the epoch
    pub validator_index: String,
}
pub type GetAttesterDutiesBodyRequestBody = Vec<String>;
///Requests the beacon node to provide a set of attestation duties, which should be performed by validators, for a particular epoch.
///Duties should only need to be checked once per epoch, however a chain reorganization (of > MIN_SEED_LOOKAHEAD epochs) could occur, resulting in a change of duties. For full safety, you should monitor head events and confirm the dependent root in this response matches:
///- event.previous_duty_dependent_root when `compute_epoch_at_slot(event.slot) == epoch`
///- event.current_duty_dependent_root when `compute_epoch_at_slot(event.slot) + 1 == epoch`
///- event.block otherwise
///
///The dependent_root value is `get_block_root_at_slot(state, compute_start_slot_at_epoch(epoch - 1) - 1)` or the genesis block root in the case of underflow.
#[derive(Debug, Clone, validator::Validate, oas3_gen_support::Default)]
pub struct GetAttesterDutiesRequest {
    #[validate(nested)]
    pub path: GetAttesterDutiesRequestPath,
    ///An array of the validator indices for which to obtain the duties.
    pub body: GetAttesterDutiesBodyRequestBody,
}
#[bon::bon]
impl GetAttesterDutiesRequest {
    ///Create a new request with the given parameters.
    #[builder]
    pub fn new(
        epoch: String,
        body: GetAttesterDutiesBodyRequestBody,
    ) -> anyhow::Result<Self> {
        let request = Self {
            path: GetAttesterDutiesRequestPath {
                epoch,
            },
            body,
        };
        request.validate()?;
        Ok(request)
    }
}
impl GetAttesterDutiesRequest {
    ///Parse the HTTP response into the response enum.
    pub async fn parse_response(
        req: reqwest::Response,
    ) -> anyhow::Result<GetAttesterDutiesResponse> {
        let status = req.status();
        if status.is_success() {
            let data = oas3_gen_support::Diagnostics::<
                GetAttesterDutiesResponseResponse,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetAttesterDutiesResponse::Ok(data));
        }
        if status.as_u16() == 400u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetAttesterDutiesResponse::BadRequest(data));
        }
        if status.as_u16() == 500u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetAttesterDutiesResponse::InternalServerError(data));
        }
        if status.as_u16() == 503u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetAttesterDutiesResponse::ServiceUnavailable(data));
        }
        let _ = req.bytes().await?;
        return Ok(GetAttesterDutiesResponse::Unknown);
    }
}
#[derive(Debug, Clone, PartialEq, validator::Validate, oas3_gen_support::Default)]
pub struct GetAttesterDutiesRequestPath {
    ///Should only be allowed 1 epoch ahead
    #[validate(length(min = 1u64))]
    pub epoch: String,
}
///Response types for getAttesterDuties
#[derive(Debug, Clone)]
pub enum GetAttesterDutiesResponse {
    ///200: Success response
    Ok(GetAttesterDutiesResponseResponse),
    ///400: Invalid epoch or index
    BadRequest(BlindedBlock400Response),
    ///500: Beacon node internal error.
    InternalServerError(BlindedBlock400Response),
    ///503: Beacon node is currently syncing, try again later.
    ServiceUnavailable(BlindedBlock400Response),
    ///default: Unknown response
    Unknown,
}
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct GetAttesterDutiesResponseResponse {
    pub data: Vec<GetAttesterDutiesResponseResponseDatum>,
    ///The block root that this response is dependent on.
    pub dependent_root: String,
    ///True if the response references an unverified execution payload. Optimistic information may be invalidated at a later time. If the field is not present, assume the False value.
    pub execution_optimistic: bool,
}
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct GetAttesterDutiesResponseResponseDatum {
    ///The committee index
    pub committee_index: String,
    ///Number of validators in committee
    pub committee_length: String,
    ///Number of committees at the provided slot
    pub committees_at_slot: String,
    ///The validator's BLS public key, uniquely identifying them. _48-bytes, hex encoded with 0x prefix, case insensitive._
    pub pubkey: String,
    ///The slot at which the validator must attest.
    pub slot: String,
    ///Index of validator in committee
    pub validator_committee_index: String,
    ///Index of validator in validator registry
    pub validator_index: String,
}
///Retrieves blinded block for given block ID.
///Depending on `Accept` header it can be returned either as JSON or as bytes serialized by SSZ
#[derive(Debug, Clone, validator::Validate, oas3_gen_support::Default)]
pub struct GetBlindedBlockRequest {
    #[validate(nested)]
    pub path: GetBlindedBlockRequestPath,
}
#[bon::bon]
impl GetBlindedBlockRequest {
    ///Create a new request with the given parameters.
    #[builder]
    pub fn new(block_id: String) -> anyhow::Result<Self> {
        let request = Self {
            path: GetBlindedBlockRequestPath {
                block_id,
            },
        };
        request.validate()?;
        Ok(request)
    }
}
impl GetBlindedBlockRequest {
    ///Parse the HTTP response into the response enum.
    pub async fn parse_response(
        req: reqwest::Response,
    ) -> anyhow::Result<GetBlindedBlockResponse> {
        let status = req.status();
        if status.is_success() {
            let content_type_str = req
                .headers()
                .get(reqwest::header::CONTENT_TYPE)
                .and_then(|v| v.to_str().ok())
                .unwrap_or("application/json");
            if content_type_str.contains("json") {
                let data = oas3_gen_support::Diagnostics::<
                    GetBlindedBlockResponseResponse,
                >::json_with_diagnostics(req)
                    .await?;
                return Ok(GetBlindedBlockResponse::Ok(data));
            }
            if content_type_str.starts_with("application/octet-stream")
                || content_type_str.starts_with("image/")
                || content_type_str.starts_with("audio/")
                || content_type_str.starts_with("video/")
            {
                let data = req.bytes().await?.to_vec();
                return Ok(GetBlindedBlockResponse::OkBinary(data));
            }
        }
        if status.as_u16() == 400u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetBlindedBlockResponse::BadRequest(data));
        }
        if status.as_u16() == 404u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetBlindedBlockResponse::NotFound(data));
        }
        if status.as_u16() == 406u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock406Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetBlindedBlockResponse::NotAcceptable(data));
        }
        if status.as_u16() == 500u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetBlindedBlockResponse::InternalServerError(data));
        }
        let _ = req.bytes().await?;
        return Ok(GetBlindedBlockResponse::Unknown);
    }
}
#[derive(Debug, Clone, PartialEq, validator::Validate, oas3_gen_support::Default)]
pub struct GetBlindedBlockRequestPath {
    ///Block identifier.
    ///Can be one of: "head" (canonical head in node's view), "genesis", "finalized", \<slot\>, \<hex encoded blockRoot with 0x prefix\>.
    ///- Example: `"head".to_string()`
    #[validate(length(min = 1u64))]
    pub block_id: String,
}
///Response types for getBlindedBlock
#[derive(Debug, Clone)]
pub enum GetBlindedBlockResponse {
    ///200: Successful response
    Ok(GetBlindedBlockResponseResponse),
    ///200: Successful response
    OkBinary(Vec<u8>),
    ///400: The block ID supplied could not be parsed
    BadRequest(BlindedBlock400Response),
    ///404: Block not found
    NotFound(BlindedBlock400Response),
    ///406: Accepted media type is not supported.
    NotAcceptable(BlindedBlock406Response),
    ///500: Beacon node internal error.
    InternalServerError(BlindedBlock400Response),
    ///default: Unknown response
    Unknown,
}
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct GetBlindedBlockResponseResponse {
    pub data: GetBlindedBlockResponseResponseData,
    ///True if the response references an unverified execution payload. Optimistic information may be invalidated at a later time. If the field is not present, assume the False value.
    pub execution_optimistic: bool,
    ///True if the response references the finalized history of the chain, as determined by fork choice. If the field is not present, additional calls are necessary to compare the epoch of the requested information with the finalized checkpoint.
    pub finalized: bool,
    pub version: ConsensusVersion,
}
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
#[serde(untagged)]
pub enum GetBlindedBlockResponseResponseData {
    ///A variant of the [`SignedBeaconBlock`](https://github.com/ethereum/consensus-specs/blob/v1.5.0/specs/phase0/beacon-chain.md#signedbeaconblock) object envelope from the CL Electra spec, which contains a `BlindedBeaconBlock` rather than a `BeaconBlock`.
    #[default]
    Object(GetBlindedBlockResponseResponseDataObject),
    ///A variant of the [`SignedBeaconBlock`](https://github.com/ethereum/consensus-specs/blob/v1.4.0/specs/phase0/beacon-chain.md#signedbeaconblock) object envelope from the CL Deneb spec, which contains a `BlindedBeaconBlock` rather than a `BeaconBlock`.
    Object2(GetBlindedBlockResponseResponseDataObject2),
    ///A variant of the [`SignedBeaconBlock`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/phase0/beacon-chain.md#signedbeaconblock) object envelope from the CL Capella spec, which contains a `BlindedBeaconBlock` rather than a `BeaconBlock`.
    Object3(GetBlindedBlockResponseResponseDataObject3),
    ///A variant of the [`SignedBeaconBlock`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/phase0/beacon-chain.md#signedbeaconblock) object envelope from the CL Bellatrix spec, which contains a `BlindedBeaconBlock` rather than a `BeaconBlock`.
    Object4(GetBlindedBlockResponseResponseDataObject4),
    ///The [`SignedBeaconBlock`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/phase0/beacon-chain.md#signedbeaconblock) object envelope from the CL Altair spec.
    Object5(GetBlindedBlockResponseResponseDataObject5),
    ///The [`SignedBeaconBlock`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/phase0/beacon-chain.md#signedbeaconblock) object envelope from the CL spec.
    Object6(GetBlindedBlockResponseResponseDataObject6),
}
///A variant of the [`SignedBeaconBlock`](https://github.com/ethereum/consensus-specs/blob/v1.5.0/specs/phase0/beacon-chain.md#signedbeaconblock) object envelope from the CL Electra spec, which contains a `BlindedBeaconBlock` rather than a `BeaconBlock`.
#[derive(
    Debug,
    Clone,
    PartialEq,
    Serialize,
    Deserialize,
    validator::Validate,
    oas3_gen_support::Default
)]
pub struct GetBlindedBlockResponseResponseDataObject {
    ///A variant of the [`BeaconBlock`](https://github.com/ethereum/consensus-specs/blob/v1.5.0/specs/phase0/beacon-chain.md#beaconblock) object from the CL Electra spec, which contains a `BlindedBeaconBlockBody` rather than a `BeaconBlockBody`.
    pub message: serde_json::Value,
    #[validate(
        length(min = 1u64),
        regex(path = "REGEX_AGGREGATE_AND_PROOF_REQUEST_BODY_ARRAY_SIGNATURE")
    )]
    pub signature: String,
}
///A variant of the [`SignedBeaconBlock`](https://github.com/ethereum/consensus-specs/blob/v1.4.0/specs/phase0/beacon-chain.md#signedbeaconblock) object envelope from the CL Deneb spec, which contains a `BlindedBeaconBlock` rather than a `BeaconBlock`.
#[derive(
    Debug,
    Clone,
    PartialEq,
    Serialize,
    Deserialize,
    validator::Validate,
    oas3_gen_support::Default
)]
pub struct GetBlindedBlockResponseResponseDataObject2 {
    ///A variant of the [`BeaconBlock`](https://github.com/ethereum/consensus-specs/blob/v1.4.0/specs/phase0/beacon-chain.md#beaconblock) object from the CL Deneb spec, which contains a `BlindedBeaconBlockBody` rather than a `BeaconBlockBody`.
    pub message: serde_json::Value,
    #[validate(
        length(min = 1u64),
        regex(path = "REGEX_AGGREGATE_AND_PROOF_REQUEST_BODY_ARRAY_SIGNATURE")
    )]
    pub signature: String,
}
///A variant of the [`SignedBeaconBlock`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/phase0/beacon-chain.md#signedbeaconblock) object envelope from the CL Capella spec, which contains a `BlindedBeaconBlock` rather than a `BeaconBlock`.
#[derive(
    Debug,
    Clone,
    PartialEq,
    Serialize,
    Deserialize,
    validator::Validate,
    oas3_gen_support::Default
)]
pub struct GetBlindedBlockResponseResponseDataObject3 {
    ///A variant of the [`BeaconBlock`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/phase0/beacon-chain.md#beaconblock) object from the CL Capella spec, which contains a `BlindedBeaconBlockBody` rather than a `BeaconBlockBody`.
    pub message: serde_json::Value,
    #[validate(
        length(min = 1u64),
        regex(path = "REGEX_AGGREGATE_AND_PROOF_REQUEST_BODY_ARRAY_SIGNATURE")
    )]
    pub signature: String,
}
///A variant of the [`SignedBeaconBlock`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/phase0/beacon-chain.md#signedbeaconblock) object envelope from the CL Bellatrix spec, which contains a `BlindedBeaconBlock` rather than a `BeaconBlock`.
#[derive(
    Debug,
    Clone,
    PartialEq,
    Serialize,
    Deserialize,
    validator::Validate,
    oas3_gen_support::Default
)]
pub struct GetBlindedBlockResponseResponseDataObject4 {
    ///A variant of the [`BeaconBlock`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/phase0/beacon-chain.md#beaconblock) object from the CL Bellatrix spec, which contains a `BlindedBeaconBlockBody` rather than a `BeaconBlockBody`.
    pub message: serde_json::Value,
    #[validate(
        length(min = 1u64),
        regex(path = "REGEX_AGGREGATE_AND_PROOF_REQUEST_BODY_ARRAY_SIGNATURE")
    )]
    pub signature: String,
}
///The [`SignedBeaconBlock`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/phase0/beacon-chain.md#signedbeaconblock) object envelope from the CL Altair spec.
#[derive(
    Debug,
    Clone,
    PartialEq,
    Serialize,
    Deserialize,
    validator::Validate,
    oas3_gen_support::Default
)]
pub struct GetBlindedBlockResponseResponseDataObject5 {
    ///The [`BeaconBlock`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/phase0/beacon-chain.md#beaconblock) object from the CL Altair spec.
    pub message: serde_json::Value,
    #[validate(
        length(min = 1u64),
        regex(path = "REGEX_AGGREGATE_AND_PROOF_REQUEST_BODY_ARRAY_SIGNATURE")
    )]
    pub signature: String,
}
///The [`SignedBeaconBlock`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/phase0/beacon-chain.md#signedbeaconblock) object envelope from the CL spec.
#[derive(
    Debug,
    Clone,
    PartialEq,
    Serialize,
    Deserialize,
    validator::Validate,
    oas3_gen_support::Default
)]
pub struct GetBlindedBlockResponseResponseDataObject6 {
    ///The [`BeaconBlock`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/phase0/beacon-chain.md#beaconblock) object from the CL spec.
    pub message: serde_json::Value,
    #[validate(
        length(min = 1u64),
        regex(path = "REGEX_AGGREGATE_AND_PROOF_REQUEST_BODY_ARRAY_SIGNATURE")
    )]
    pub signature: String,
}
///Retrieves blob sidecars for a given block id.
///Depending on `Accept` header it can be returned either as json or as bytes serialized by SSZ.
///
///If the `indices` parameter is specified, only the blob sidecars with the specified indices will be returned. There are no guarantees
///for the returned blob sidecars in terms of ordering.
#[derive(Debug, Clone, validator::Validate, oas3_gen_support::Default)]
pub struct GetBlobSidecarsRequest {
    #[validate(nested)]
    pub path: GetBlobSidecarsRequestPath,
    pub query: GetBlobSidecarsRequestQuery,
}
#[bon::bon]
impl GetBlobSidecarsRequest {
    ///Create a new request with the given parameters.
    #[builder]
    pub fn new(block_id: String, indices: Option<Vec<String>>) -> anyhow::Result<Self> {
        let request = Self {
            path: GetBlobSidecarsRequestPath {
                block_id,
            },
            query: GetBlobSidecarsRequestQuery {
                indices,
            },
        };
        request.validate()?;
        Ok(request)
    }
}
impl GetBlobSidecarsRequest {
    ///Parse the HTTP response into the response enum.
    pub async fn parse_response(
        req: reqwest::Response,
    ) -> anyhow::Result<GetBlobSidecarsResponse> {
        let status = req.status();
        if status.is_success() {
            let content_type_str = req
                .headers()
                .get(reqwest::header::CONTENT_TYPE)
                .and_then(|v| v.to_str().ok())
                .unwrap_or("application/json");
            if content_type_str.contains("json") {
                let data = oas3_gen_support::Diagnostics::<
                    GetBlobSidecarsResponseResponse,
                >::json_with_diagnostics(req)
                    .await?;
                return Ok(GetBlobSidecarsResponse::Ok(data));
            }
            if content_type_str.starts_with("application/octet-stream")
                || content_type_str.starts_with("image/")
                || content_type_str.starts_with("audio/")
                || content_type_str.starts_with("video/")
            {
                let data = req.bytes().await?.to_vec();
                return Ok(GetBlobSidecarsResponse::OkBinary(data));
            }
        }
        if status.as_u16() == 400u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetBlobSidecarsResponse::BadRequest(data));
        }
        if status.as_u16() == 404u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetBlobSidecarsResponse::NotFound(data));
        }
        if status.as_u16() == 406u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock406Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetBlobSidecarsResponse::NotAcceptable(data));
        }
        if status.as_u16() == 500u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetBlobSidecarsResponse::InternalServerError(data));
        }
        let _ = req.bytes().await?;
        return Ok(GetBlobSidecarsResponse::Unknown);
    }
}
#[derive(Debug, Clone, PartialEq, validator::Validate, oas3_gen_support::Default)]
pub struct GetBlobSidecarsRequestPath {
    ///Block identifier.
    ///Can be one of: "head" (canonical head in node's view), "genesis", "finalized", \<slot\>, \<hex encoded blockRoot with 0x prefix\>.
    ///- Example: `"head".to_string()`
    #[validate(length(min = 1u64))]
    pub block_id: String,
}
#[serde_with::skip_serializing_none]
#[derive(Debug, Clone, PartialEq, Serialize, oas3_gen_support::Default)]
pub struct GetBlobSidecarsRequestQuery {
    ///Array of indices for blob sidecars to request for in the specified block. Returns all blob sidecars in the block if not specified.
    pub indices: Option<Vec<String>>,
}
///Response types for getBlobSidecars
#[derive(Debug, Clone)]
pub enum GetBlobSidecarsResponse {
    ///200: Successful response
    Ok(GetBlobSidecarsResponseResponse),
    ///200: Successful response
    OkBinary(Vec<u8>),
    ///400: The block ID supplied could not be parsed
    BadRequest(BlindedBlock400Response),
    ///404: Block not found
    NotFound(BlindedBlock400Response),
    ///406: Accepted media type is not supported.
    NotAcceptable(BlindedBlock406Response),
    ///500: Beacon node internal error.
    InternalServerError(BlindedBlock400Response),
    ///default: Unknown response
    Unknown,
}
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct GetBlobSidecarsResponseResponse {
    pub data: GetBlobSidecarsResponseResponseData,
    ///True if the response references an unverified execution payload. Optimistic information may be invalidated at a later time. If the field is not present, assume the False value.
    pub execution_optimistic: bool,
    ///True if the response references the finalized history of the chain, as determined by fork choice. If the field is not present, additional calls are necessary to compare the epoch of the requested information with the finalized checkpoint.
    pub finalized: bool,
    pub version: GetBlobSidecarsResponseResponseVersion,
}
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
#[serde(untagged)]
pub enum GetBlobSidecarsResponseResponseData {
    #[default]
    Array(Vec<GetBlobSidecarsResponseResponseDataArray>),
    Array2(Vec<GetBlobSidecarsResponseResponseDataArray>),
}
///A blob sidecar as defined in the Deneb consensus spec.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct GetBlobSidecarsResponseResponseDataArray {
    ///A blob is `FIELD_ELEMENTS_PER_BLOB * size_of(BLSFieldElement) = 4096 * 32 = 131072` bytes (`DATA`) representing a Blob as defined in Deneb
    pub blob: String,
    pub index: String,
    ///A G1 curve point. Same as BLS standard "is valid pubkey" check but also allows `0x00..00` for point-at-infinity
    pub kzg_commitment: String,
    pub kzg_commitment_inclusion_proof: Vec<String>,
    ///A G1 curve point. Used for verifying that the `KZGCommitment` for a given `Blob` is correct.
    pub kzg_proof: String,
    ///The [`SignedBeaconBlockHeader`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/phase0/beacon-chain.md#signedbeaconblockheader) object envelope from the CL spec.
    pub signed_block_header: Phase0ProposerSlashingSignedHeader1,
}
#[derive(Debug, Clone, PartialEq, Eq, Hash, Serialize, Deserialize, oas3_gen_support::Default)]
pub enum GetBlobSidecarsResponseResponseVersion {
    #[serde(rename = "deneb")]
    #[default]
    Deneb,
    #[serde(rename = "electra")]
    Electra,
}
impl core::fmt::Display for GetBlobSidecarsResponseResponseVersion {
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        match self {
            Self::Deneb => write!(f, "deneb"),
            Self::Electra => write!(f, "electra"),
        }
    }
}
///Retrieves blobs for a given block id.
///Depending on `Accept` header it can be returned either as json or as bytes serialized by SSZ.
///
///If the `versioned_hashes` parameter is specified, only the blobs for the specified versioned hashes will be returned. Blobs are
///returned as an ordered list matching the order of their corresponding KZG commitments in the block.
///
///After the Fulu fork, only supernodes (which custody all data columns) are required to return blobs. Clients may implement
///blob reconstruction logic for non-super nodes.
#[derive(Debug, Clone, validator::Validate, oas3_gen_support::Default)]
pub struct GetBlobsRequest {
    #[validate(nested)]
    pub path: GetBlobsRequestPath,
    pub query: GetBlobsRequestQuery,
}
#[bon::bon]
impl GetBlobsRequest {
    ///Create a new request with the given parameters.
    #[builder]
    pub fn new(
        block_id: String,
        versioned_hashes: Option<Vec<String>>,
    ) -> anyhow::Result<Self> {
        let request = Self {
            path: GetBlobsRequestPath { block_id },
            query: GetBlobsRequestQuery {
                versioned_hashes,
            },
        };
        request.validate()?;
        Ok(request)
    }
}
impl GetBlobsRequest {
    ///Parse the HTTP response into the response enum.
    pub async fn parse_response(
        req: reqwest::Response,
    ) -> anyhow::Result<GetBlobsResponse> {
        let status = req.status();
        if status.is_success() {
            let content_type_str = req
                .headers()
                .get(reqwest::header::CONTENT_TYPE)
                .and_then(|v| v.to_str().ok())
                .unwrap_or("application/json");
            if content_type_str.contains("json") {
                let data = oas3_gen_support::Diagnostics::<
                    GetBlobsResponseResponse,
                >::json_with_diagnostics(req)
                    .await?;
                return Ok(GetBlobsResponse::Ok(data));
            }
            if content_type_str.starts_with("application/octet-stream")
                || content_type_str.starts_with("image/")
                || content_type_str.starts_with("audio/")
                || content_type_str.starts_with("video/")
            {
                let data = req.bytes().await?.to_vec();
                return Ok(GetBlobsResponse::OkBinary(data));
            }
        }
        if status.as_u16() == 400u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetBlobsResponse::BadRequest(data));
        }
        if status.as_u16() == 404u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetBlobsResponse::NotFound(data));
        }
        if status.as_u16() == 406u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock406Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetBlobsResponse::NotAcceptable(data));
        }
        if status.as_u16() == 500u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetBlobsResponse::InternalServerError(data));
        }
        let _ = req.bytes().await?;
        return Ok(GetBlobsResponse::Unknown);
    }
}
#[derive(Debug, Clone, PartialEq, validator::Validate, oas3_gen_support::Default)]
pub struct GetBlobsRequestPath {
    ///Block identifier.
    ///Can be one of: "head" (canonical head in node's view), "genesis", "finalized", \<slot\>, \<hex encoded blockRoot with 0x prefix\>.
    ///- Example: `"head".to_string()`
    #[validate(length(min = 1u64))]
    pub block_id: String,
}
#[serde_with::skip_serializing_none]
#[derive(Debug, Clone, PartialEq, Serialize, oas3_gen_support::Default)]
pub struct GetBlobsRequestQuery {
    ///Array of versioned hashes for blobs to request for in the specified block. Returns all blobs in the block if not specified.
    pub versioned_hashes: Option<Vec<String>>,
}
///Response types for getBlobs
#[derive(Debug, Clone)]
pub enum GetBlobsResponse {
    ///200: Successful response
    Ok(GetBlobsResponseResponse),
    ///200: Successful response
    OkBinary(Vec<u8>),
    ///400: The block ID supplied could not be parsed
    BadRequest(BlindedBlock400Response),
    ///404: Block not found
    NotFound(BlindedBlock400Response),
    ///406: Accepted media type is not supported.
    NotAcceptable(BlindedBlock406Response),
    ///500: Beacon node internal error.
    InternalServerError(BlindedBlock400Response),
    ///default: Unknown response
    Unknown,
}
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct GetBlobsResponseResponse {
    pub data: Vec<String>,
    ///True if the response references an unverified execution payload. Optimistic information may be invalidated at a later time. If the field is not present, assume the False value.
    pub execution_optimistic: bool,
    ///True if the response references the finalized history of the chain, as determined by fork choice. If the field is not present, additional calls are necessary to compare the epoch of the requested information with the finalized checkpoint.
    pub finalized: bool,
}
///Retrieves attestation included in requested block.
#[derive(Debug, Clone, validator::Validate, oas3_gen_support::Default)]
pub struct GetBlockAttestationsV2Request {
    #[validate(nested)]
    pub path: GetBlockAttestationsV2RequestPath,
}
#[bon::bon]
impl GetBlockAttestationsV2Request {
    ///Create a new request with the given parameters.
    #[builder]
    pub fn new(block_id: String) -> anyhow::Result<Self> {
        let request = Self {
            path: GetBlockAttestationsV2RequestPath {
                block_id,
            },
        };
        request.validate()?;
        Ok(request)
    }
}
impl GetBlockAttestationsV2Request {
    ///Parse the HTTP response into the response enum.
    pub async fn parse_response(
        req: reqwest::Response,
    ) -> anyhow::Result<GetBlockAttestationsV2Response> {
        let status = req.status();
        if status.is_success() {
            let data = oas3_gen_support::Diagnostics::<
                GetBlockAttestationsV2ResponseResponse,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetBlockAttestationsV2Response::Ok(data));
        }
        if status.as_u16() == 400u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetBlockAttestationsV2Response::BadRequest(data));
        }
        if status.as_u16() == 404u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetBlockAttestationsV2Response::NotFound(data));
        }
        if status.as_u16() == 500u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetBlockAttestationsV2Response::InternalServerError(data));
        }
        let _ = req.bytes().await?;
        return Ok(GetBlockAttestationsV2Response::Unknown);
    }
}
#[derive(Debug, Clone, PartialEq, validator::Validate, oas3_gen_support::Default)]
pub struct GetBlockAttestationsV2RequestPath {
    ///Block identifier.
    ///Can be one of: "head" (canonical head in node's view), "genesis", "finalized", \<slot\>, \<hex encoded blockRoot with 0x prefix\>.
    ///- Example: `"head".to_string()`
    #[validate(length(min = 1u64))]
    pub block_id: String,
}
///Response types for getBlockAttestationsV2
#[derive(Debug, Clone)]
pub enum GetBlockAttestationsV2Response {
    ///200: Success
    Ok(GetBlockAttestationsV2ResponseResponse),
    ///400: The block ID supplied could not be parsed
    BadRequest(BlindedBlock400Response),
    ///404: Block not found
    NotFound(BlindedBlock400Response),
    ///500: Beacon node internal error.
    InternalServerError(BlindedBlock400Response),
    ///default: Unknown response
    Unknown,
}
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct GetBlockAttestationsV2ResponseResponse {
    pub data: GetBlockAttestationsV2ResponseResponseData,
    ///True if the response references an unverified execution payload. Optimistic information may be invalidated at a later time. If the field is not present, assume the False value.
    pub execution_optimistic: bool,
    ///True if the response references the finalized history of the chain, as determined by fork choice. If the field is not present, additional calls are necessary to compare the epoch of the requested information with the finalized checkpoint.
    pub finalized: bool,
    pub version: ConsensusVersion,
}
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
#[serde(untagged)]
pub enum GetBlockAttestationsV2ResponseResponseData {
    #[default]
    Array(Vec<GetBlockAttestationsV2ResponseResponseDataArray>),
    Array2(Vec<GetBlockAttestationsV2ResponseResponseDataArray2>),
}
///The [`Attestation`](https://github.com/ethereum/consensus-specs/blob/v1.5.0/specs/electra/beacon-chain.md#attestation) object from the CL spec.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct GetBlockAttestationsV2ResponseResponseDataArray {
    ///Attester aggregation bits.
    pub aggregation_bits: String,
    ///Committee bits.
    pub committee_bits: String,
    ///The [`AttestationData`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/phase0/beacon-chain.md#attestationdata) object from the CL spec.
    pub data: Data,
    ///BLS aggregate signature.
    pub signature: String,
}
///The [`Attestation`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/phase0/beacon-chain.md#attestation) object from the CL spec.
#[derive(
    Debug,
    Clone,
    PartialEq,
    Serialize,
    Deserialize,
    validator::Validate,
    oas3_gen_support::Default
)]
pub struct GetBlockAttestationsV2ResponseResponseDataArray2 {
    ///Attester aggregation bits.
    #[validate(length(min = 1u64), regex(path = "REGEX_CONTRIBUTION_AGGREGATION_BITS"))]
    pub aggregation_bits: String,
    ///The [`AttestationData`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/phase0/beacon-chain.md#attestationdata) object from the CL spec.
    #[validate(nested)]
    pub data: Data,
    ///BLS aggregate signature.
    #[validate(
        length(min = 1u64),
        regex(path = "REGEX_AGGREGATE_AND_PROOF_REQUEST_BODY_ARRAY_SIGNATURE")
    )]
    pub signature: String,
}
///Retrieves block header for given block id.
#[derive(Debug, Clone, validator::Validate, oas3_gen_support::Default)]
pub struct GetBlockHeaderRequest {
    #[validate(nested)]
    pub path: GetBlockHeaderRequestPath,
}
#[bon::bon]
impl GetBlockHeaderRequest {
    ///Create a new request with the given parameters.
    #[builder]
    pub fn new(block_id: String) -> anyhow::Result<Self> {
        let request = Self {
            path: GetBlockHeaderRequestPath {
                block_id,
            },
        };
        request.validate()?;
        Ok(request)
    }
}
impl GetBlockHeaderRequest {
    ///Parse the HTTP response into the response enum.
    pub async fn parse_response(
        req: reqwest::Response,
    ) -> anyhow::Result<GetBlockHeaderResponse> {
        let status = req.status();
        if status.is_success() {
            let data = oas3_gen_support::Diagnostics::<
                GetBlockHeaderResponseResponse,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetBlockHeaderResponse::Ok(data));
        }
        if status.as_u16() == 400u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetBlockHeaderResponse::BadRequest(data));
        }
        if status.as_u16() == 404u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetBlockHeaderResponse::NotFound(data));
        }
        if status.as_u16() == 500u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetBlockHeaderResponse::InternalServerError(data));
        }
        let _ = req.bytes().await?;
        return Ok(GetBlockHeaderResponse::Unknown);
    }
}
#[derive(Debug, Clone, PartialEq, validator::Validate, oas3_gen_support::Default)]
pub struct GetBlockHeaderRequestPath {
    ///Block identifier.
    ///Can be one of: "head" (canonical head in node's view), "genesis", "finalized", \<slot\>, \<hex encoded blockRoot with 0x prefix\>.
    ///- Example: `"head".to_string()`
    #[validate(length(min = 1u64))]
    pub block_id: String,
}
///Response types for getBlockHeader
#[derive(Debug, Clone)]
pub enum GetBlockHeaderResponse {
    ///200: Success
    Ok(GetBlockHeaderResponseResponse),
    ///400: The block ID supplied could not be parsed
    BadRequest(BlindedBlock400Response),
    ///404: Block not found
    NotFound(BlindedBlock400Response),
    ///500: Beacon node internal error.
    InternalServerError(BlindedBlock400Response),
    ///default: Unknown response
    Unknown,
}
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct GetBlockHeaderResponseResponse {
    pub data: GetBlockHeadersResponseResponseDatum,
    ///True if the response references an unverified execution payload. Optimistic information may be invalidated at a later time. If the field is not present, assume the False value.
    pub execution_optimistic: bool,
    ///True if the response references the finalized history of the chain, as determined by fork choice. If the field is not present, additional calls are necessary to compare the epoch of the requested information with the finalized checkpoint.
    pub finalized: bool,
}
///Retrieves block headers matching given query. By default it will fetch current head slot blocks.
#[derive(Debug, Clone, validator::Validate, oas3_gen_support::Default)]
pub struct GetBlockHeadersRequest {
    #[validate(nested)]
    pub query: GetBlockHeadersRequestQuery,
}
#[bon::bon]
impl GetBlockHeadersRequest {
    ///Create a new request with the given parameters.
    #[builder]
    pub fn new(
        slot: Option<String>,
        parent_root: Option<String>,
    ) -> anyhow::Result<Self> {
        let request = Self {
            query: GetBlockHeadersRequestQuery {
                slot,
                parent_root,
            },
        };
        request.validate()?;
        Ok(request)
    }
}
impl GetBlockHeadersRequest {
    ///Parse the HTTP response into the response enum.
    pub async fn parse_response(
        req: reqwest::Response,
    ) -> anyhow::Result<GetBlockHeadersResponse> {
        let status = req.status();
        if status.is_success() {
            let data = oas3_gen_support::Diagnostics::<
                GetBlockHeadersResponseResponse,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetBlockHeadersResponse::Ok(data));
        }
        if status.as_u16() == 400u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetBlockHeadersResponse::BadRequest(data));
        }
        if status.as_u16() == 500u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetBlockHeadersResponse::InternalServerError(data));
        }
        let _ = req.bytes().await?;
        return Ok(GetBlockHeadersResponse::Unknown);
    }
}
#[serde_with::skip_serializing_none]
#[derive(
    Debug,
    Clone,
    PartialEq,
    Serialize,
    validator::Validate,
    oas3_gen_support::Default
)]
pub struct GetBlockHeadersRequestQuery {
    pub slot: Option<String>,
    #[validate(
        regex(path = "REGEX_ALTAIR_BEACON_STATE_CURRENT_JUSTIFIED_CHECKPOINT_ROOT")
    )]
    pub parent_root: Option<String>,
}
///Response types for getBlockHeaders
#[derive(Debug, Clone)]
pub enum GetBlockHeadersResponse {
    ///200: Success
    Ok(GetBlockHeadersResponseResponse),
    ///400: The block ID supplied could not be parsed
    BadRequest(BlindedBlock400Response),
    ///500: Beacon node internal error.
    InternalServerError(BlindedBlock400Response),
    ///default: Unknown response
    Unknown,
}
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct GetBlockHeadersResponseResponse {
    pub data: Vec<GetBlockHeadersResponseResponseDatum>,
    ///True if the response references an unverified execution payload. Optimistic information may be invalidated at a later time. If the field is not present, assume the False value.
    pub execution_optimistic: bool,
    ///True if the response references the finalized history of the chain, as determined by fork choice. If the field is not present, additional calls are necessary to compare the epoch of the requested information with the finalized checkpoint.
    pub finalized: bool,
}
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct GetBlockHeadersResponseResponseDatum {
    pub canonical: bool,
    ///The [`SignedBeaconBlockHeader`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/phase0/beacon-chain.md#signedbeaconblockheader) object envelope from the CL spec.
    pub header: Phase0ProposerSlashingSignedHeader1,
    pub root: String,
}
///Retrieve block reward info for a single block. The rewards value is the sum
///of values of the proposer rewards from attestations, sync committees and
///slashings included in the proposal.
#[derive(Debug, Clone, validator::Validate, oas3_gen_support::Default)]
pub struct GetBlockRewardsRequest {
    #[validate(nested)]
    pub path: GetBlockRewardsRequestPath,
}
#[bon::bon]
impl GetBlockRewardsRequest {
    ///Create a new request with the given parameters.
    #[builder]
    pub fn new(block_id: String) -> anyhow::Result<Self> {
        let request = Self {
            path: GetBlockRewardsRequestPath {
                block_id,
            },
        };
        request.validate()?;
        Ok(request)
    }
}
impl GetBlockRewardsRequest {
    ///Parse the HTTP response into the response enum.
    pub async fn parse_response(
        req: reqwest::Response,
    ) -> anyhow::Result<GetBlockRewardsResponse> {
        let status = req.status();
        if status.is_success() {
            let data = oas3_gen_support::Diagnostics::<
                GetBlockRewardsResponseResponse,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetBlockRewardsResponse::Ok(data));
        }
        if status.as_u16() == 400u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetBlockRewardsResponse::BadRequest(data));
        }
        if status.as_u16() == 404u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetBlockRewardsResponse::NotFound(data));
        }
        if status.as_u16() == 500u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetBlockRewardsResponse::InternalServerError(data));
        }
        let _ = req.bytes().await?;
        return Ok(GetBlockRewardsResponse::Unknown);
    }
}
#[derive(Debug, Clone, PartialEq, validator::Validate, oas3_gen_support::Default)]
pub struct GetBlockRewardsRequestPath {
    ///Block identifier.
    ///Can be one of: "head" (canonical head in node's view), "genesis", "finalized", \<slot\>, \<hex encoded blockRoot with 0x prefix\>.
    ///- Example: `"head".to_string()`
    #[validate(length(min = 1u64))]
    pub block_id: String,
}
///Response types for getBlockRewards
#[derive(Debug, Clone)]
pub enum GetBlockRewardsResponse {
    ///200: Success
    Ok(GetBlockRewardsResponseResponse),
    ///400: Invalid get block rewards request
    BadRequest(BlindedBlock400Response),
    ///404: Block or required state not found
    NotFound(BlindedBlock400Response),
    ///500: Beacon node internal error.
    InternalServerError(BlindedBlock400Response),
    ///default: Unknown response
    Unknown,
}
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct GetBlockRewardsResponseResponse {
    ///Rewards info for a single block
    pub data: GetBlockRewardsResponseResponseData,
    ///True if the response references an unverified execution payload. Optimistic information may be invalidated at a later time. If the field is not present, assume the False value.
    pub execution_optimistic: bool,
    ///True if the response references the finalized history of the chain, as determined by fork choice. If the field is not present, additional calls are necessary to compare the epoch of the requested information with the finalized checkpoint.
    pub finalized: bool,
}
///Rewards info for a single block
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct GetBlockRewardsResponseResponseData {
    ///block reward component due to included attestations in gwei
    pub attestations: String,
    ///block reward component due to included attester_slashings in gwei
    pub attester_slashings: String,
    ///proposer of the block, the proposer index who receives these rewards
    pub proposer_index: String,
    ///block reward component due to included proposer_slashings in gwei
    pub proposer_slashings: String,
    ///block reward component due to included sync_aggregate in gwei
    pub sync_aggregate: String,
    ///total block reward in gwei, equal to attestations + sync_aggregate + proposer_slashings + attester_slashings
    pub total: String,
}
///Retrieves hashTreeRoot of BeaconBlock/BeaconBlockHeader
#[derive(Debug, Clone, validator::Validate, oas3_gen_support::Default)]
pub struct GetBlockRootRequest {
    #[validate(nested)]
    pub path: GetBlockRootRequestPath,
}
#[bon::bon]
impl GetBlockRootRequest {
    ///Create a new request with the given parameters.
    #[builder]
    pub fn new(block_id: String) -> anyhow::Result<Self> {
        let request = Self {
            path: GetBlockRootRequestPath {
                block_id,
            },
        };
        request.validate()?;
        Ok(request)
    }
}
impl GetBlockRootRequest {
    ///Parse the HTTP response into the response enum.
    pub async fn parse_response(
        req: reqwest::Response,
    ) -> anyhow::Result<GetBlockRootResponse> {
        let status = req.status();
        if status.is_success() {
            let data = oas3_gen_support::Diagnostics::<
                GetBlockRootResponseResponse,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetBlockRootResponse::Ok(data));
        }
        if status.as_u16() == 400u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetBlockRootResponse::BadRequest(data));
        }
        if status.as_u16() == 404u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetBlockRootResponse::NotFound(data));
        }
        if status.as_u16() == 500u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetBlockRootResponse::InternalServerError(data));
        }
        let _ = req.bytes().await?;
        return Ok(GetBlockRootResponse::Unknown);
    }
}
#[derive(Debug, Clone, PartialEq, validator::Validate, oas3_gen_support::Default)]
pub struct GetBlockRootRequestPath {
    ///Block identifier.
    ///Can be one of: "head" (canonical head in node's view), "genesis", "finalized", \<slot\>, \<hex encoded blockRoot with 0x prefix\>.
    ///- Example: `"head".to_string()`
    #[validate(length(min = 1u64))]
    pub block_id: String,
}
///Response types for getBlockRoot
#[derive(Debug, Clone)]
pub enum GetBlockRootResponse {
    ///200: Success
    Ok(GetBlockRootResponseResponse),
    ///400: The block ID supplied could not be parsed
    BadRequest(BlindedBlock400Response),
    ///404: Block not found
    NotFound(BlindedBlock400Response),
    ///500: Beacon node internal error.
    InternalServerError(BlindedBlock400Response),
    ///default: Unknown response
    Unknown,
}
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct GetBlockRootResponseResponse {
    pub data: GetBlockRootResponseResponseData,
    ///True if the response references an unverified execution payload. Optimistic information may be invalidated at a later time. If the field is not present, assume the False value.
    pub execution_optimistic: bool,
    ///True if the response references the finalized history of the chain, as determined by fork choice. If the field is not present, additional calls are necessary to compare the epoch of the requested information with the finalized checkpoint.
    pub finalized: bool,
}
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct GetBlockRootResponseResponseData {
    ///HashTreeRoot of BeaconBlock/BeaconBlockHeader object
    pub root: String,
}
///Retrieves block details for given block id.
///Depending on `Accept` header it can be returned either as json or as bytes serialized by SSZ
#[derive(Debug, Clone, validator::Validate, oas3_gen_support::Default)]
pub struct GetBlockV2Request {
    #[validate(nested)]
    pub path: GetBlockV2RequestPath,
}
#[bon::bon]
impl GetBlockV2Request {
    ///Create a new request with the given parameters.
    #[builder]
    pub fn new(block_id: String) -> anyhow::Result<Self> {
        let request = Self {
            path: GetBlockV2RequestPath { block_id },
        };
        request.validate()?;
        Ok(request)
    }
}
impl GetBlockV2Request {
    ///Parse the HTTP response into the response enum.
    pub async fn parse_response(
        req: reqwest::Response,
    ) -> anyhow::Result<GetBlockV2Response> {
        let status = req.status();
        if status.is_success() {
            let content_type_str = req
                .headers()
                .get(reqwest::header::CONTENT_TYPE)
                .and_then(|v| v.to_str().ok())
                .unwrap_or("application/json");
            if content_type_str.contains("json") {
                let data = oas3_gen_support::Diagnostics::<
                    GetBlockV2ResponseResponse,
                >::json_with_diagnostics(req)
                    .await?;
                return Ok(GetBlockV2Response::Ok(data));
            }
            if content_type_str.starts_with("application/octet-stream")
                || content_type_str.starts_with("image/")
                || content_type_str.starts_with("audio/")
                || content_type_str.starts_with("video/")
            {
                let data = req.bytes().await?.to_vec();
                return Ok(GetBlockV2Response::OkBinary(data));
            }
        }
        if status.as_u16() == 400u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetBlockV2Response::BadRequest(data));
        }
        if status.as_u16() == 404u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetBlockV2Response::NotFound(data));
        }
        if status.as_u16() == 406u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock406Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetBlockV2Response::NotAcceptable(data));
        }
        if status.as_u16() == 500u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetBlockV2Response::InternalServerError(data));
        }
        let _ = req.bytes().await?;
        return Ok(GetBlockV2Response::Unknown);
    }
}
#[derive(Debug, Clone, PartialEq, validator::Validate, oas3_gen_support::Default)]
pub struct GetBlockV2RequestPath {
    ///Block identifier.
    ///Can be one of: "head" (canonical head in node's view), "genesis", "finalized", \<slot\>, \<hex encoded blockRoot with 0x prefix\>.
    ///- Example: `"head".to_string()`
    #[validate(length(min = 1u64))]
    pub block_id: String,
}
///Response types for getBlockV2
#[derive(Debug, Clone)]
pub enum GetBlockV2Response {
    ///200: Successful response
    Ok(GetBlockV2ResponseResponse),
    ///200: Successful response
    OkBinary(Vec<u8>),
    ///400: The block ID supplied could not be parsed
    BadRequest(BlindedBlock400Response),
    ///404: Block not found
    NotFound(BlindedBlock400Response),
    ///406: Accepted media type is not supported.
    NotAcceptable(BlindedBlock406Response),
    ///500: Beacon node internal error.
    InternalServerError(BlindedBlock400Response),
    ///default: Unknown response
    Unknown,
}
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct GetBlockV2ResponseResponse {
    pub data: GetBlockV2ResponseResponseData,
    ///True if the response references an unverified execution payload. Optimistic information may be invalidated at a later time. If the field is not present, assume the False value.
    pub execution_optimistic: bool,
    ///True if the response references the finalized history of the chain, as determined by fork choice. If the field is not present, additional calls are necessary to compare the epoch of the requested information with the finalized checkpoint.
    pub finalized: bool,
    pub version: ConsensusVersion,
}
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
#[serde(untagged)]
pub enum GetBlockV2ResponseResponseData {
    ///The [`SignedBeaconBlock`](https://github.com/ethereum/consensus-specs/blob/v1.5.0/specs/phase0/beacon-chain.md#signedbeaconblock) object envelope from the CL Electra spec.
    #[default]
    Object(SignedBlockContentsSignedBlock),
    ///The [`SignedBeaconBlock`](https://github.com/ethereum/consensus-specs/blob/v1.4.0/specs/phase0/beacon-chain.md#signedbeaconblock) object envelope from the CL Deneb spec.
    Object2(DenebSignedBlockContentsSignedBlock),
    ///The [`SignedBeaconBlock`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/phase0/beacon-chain.md#signedbeaconblock) object envelope from the CL Capella spec.
    Object3(BlockRequestBodyObject4),
    ///The [`SignedBeaconBlock`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/phase0/beacon-chain.md#signedbeaconblock) object envelope from the CL Bellatrix spec.
    Object4(BlockRequestBodyObject5),
    ///The [`SignedBeaconBlock`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/phase0/beacon-chain.md#signedbeaconblock) object envelope from the CL Altair spec.
    Object5(GetBlindedBlockResponseResponseDataObject5),
    ///The [`SignedBeaconBlock`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/phase0/beacon-chain.md#signedbeaconblock) object envelope from the CL spec.
    Object6(GetBlindedBlockResponseResponseDataObject6),
}
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct GetDebugChainHeadsResponseResponse {
    pub data: Vec<GetDebugChainHeadsResponseResponseDatum>,
}
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct GetDebugChainHeadsResponseResponseDatum {
    ///True if the response references an unverified execution payload. Optimistic information may be invalidated at a later time. If the field is not present, assume the False value.
    pub execution_optimistic: bool,
    pub root: String,
    pub slot: String,
}
///Retrieves all possible chain heads (leaves of fork choice tree).
#[derive(Debug, Clone, validator::Validate, oas3_gen_support::Default)]
pub struct GetDebugChainHeadsV2Request {}
impl GetDebugChainHeadsV2Request {
    ///Parse the HTTP response into the response enum.
    pub async fn parse_response(
        req: reqwest::Response,
    ) -> anyhow::Result<GetDebugChainHeadsV2Response> {
        let status = req.status();
        if status.is_success() {
            let data = oas3_gen_support::Diagnostics::<
                GetDebugChainHeadsResponseResponse,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetDebugChainHeadsV2Response::Ok(data));
        }
        if status.as_u16() == 500u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetDebugChainHeadsV2Response::InternalServerError(data));
        }
        let _ = req.bytes().await?;
        return Ok(GetDebugChainHeadsV2Response::Unknown);
    }
}
///Response types for getDebugChainHeadsV2
#[derive(Debug, Clone)]
pub enum GetDebugChainHeadsV2Response {
    ///200: Success
    Ok(GetDebugChainHeadsResponseResponse),
    ///500: Beacon node internal error.
    InternalServerError(BlindedBlock400Response),
    ///default: Unknown response
    Unknown,
}
///Retrieves data column sidecars for a given block id.
///Depending on `Accept` header it can be returned either as json or as bytes serialized by SSZ.
///
///If the `indices` parameter is specified, only the data column sidecars with the specified indices will be returned. There are no guarantees
///for the returned data column sidecars in terms of ordering.
#[derive(Debug, Clone, validator::Validate, oas3_gen_support::Default)]
pub struct GetDebugDataColumnSidecarsRequest {
    #[validate(nested)]
    pub path: GetDebugDataColumnSidecarsRequestPath,
    pub query: GetDebugDataColumnSidecarsRequestQuery,
}
#[bon::bon]
impl GetDebugDataColumnSidecarsRequest {
    ///Create a new request with the given parameters.
    #[builder]
    pub fn new(block_id: String, indices: Option<Vec<String>>) -> anyhow::Result<Self> {
        let request = Self {
            path: GetDebugDataColumnSidecarsRequestPath {
                block_id,
            },
            query: GetDebugDataColumnSidecarsRequestQuery {
                indices,
            },
        };
        request.validate()?;
        Ok(request)
    }
}
impl GetDebugDataColumnSidecarsRequest {
    ///Parse the HTTP response into the response enum.
    pub async fn parse_response(
        req: reqwest::Response,
    ) -> anyhow::Result<GetDebugDataColumnSidecarsResponse> {
        let status = req.status();
        if status.is_success() {
            let content_type_str = req
                .headers()
                .get(reqwest::header::CONTENT_TYPE)
                .and_then(|v| v.to_str().ok())
                .unwrap_or("application/json");
            if content_type_str.contains("json") {
                let data = oas3_gen_support::Diagnostics::<
                    GetDebugDataColumnSidecarsResponseResponse,
                >::json_with_diagnostics(req)
                    .await?;
                return Ok(GetDebugDataColumnSidecarsResponse::Ok(data));
            }
            if content_type_str.starts_with("application/octet-stream")
                || content_type_str.starts_with("image/")
                || content_type_str.starts_with("audio/")
                || content_type_str.starts_with("video/")
            {
                let data = req.bytes().await?.to_vec();
                return Ok(GetDebugDataColumnSidecarsResponse::OkBinary(data));
            }
        }
        if status.as_u16() == 400u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetDebugDataColumnSidecarsResponse::BadRequest(data));
        }
        if status.as_u16() == 404u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetDebugDataColumnSidecarsResponse::NotFound(data));
        }
        if status.as_u16() == 406u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock406Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetDebugDataColumnSidecarsResponse::NotAcceptable(data));
        }
        if status.as_u16() == 500u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetDebugDataColumnSidecarsResponse::InternalServerError(data));
        }
        let _ = req.bytes().await?;
        return Ok(GetDebugDataColumnSidecarsResponse::Unknown);
    }
}
#[derive(Debug, Clone, PartialEq, validator::Validate, oas3_gen_support::Default)]
pub struct GetDebugDataColumnSidecarsRequestPath {
    ///Block identifier.
    ///Can be one of: "head" (canonical head in node's view), "genesis", "finalized", \<slot\>, \<hex encoded blockRoot with 0x prefix\>.
    ///- Example: `"head".to_string()`
    #[validate(length(min = 1u64))]
    pub block_id: String,
}
#[serde_with::skip_serializing_none]
#[derive(Debug, Clone, PartialEq, Serialize, oas3_gen_support::Default)]
pub struct GetDebugDataColumnSidecarsRequestQuery {
    ///Array of indices for data column sidecars to request for in the specified block. This endpoint will only return columns that the node is actually custodying. If not specified, returns all data column sidecars that this node is custodying in the block.
    pub indices: Option<Vec<String>>,
}
///Response types for getDebugDataColumnSidecars
#[derive(Debug, Clone)]
pub enum GetDebugDataColumnSidecarsResponse {
    ///200: Successful response
    Ok(GetDebugDataColumnSidecarsResponseResponse),
    ///200: Successful response
    OkBinary(Vec<u8>),
    ///400: The block ID supplied could not be parsed
    BadRequest(BlindedBlock400Response),
    ///404: Block not found
    NotFound(BlindedBlock400Response),
    ///406: Accepted media type is not supported.
    NotAcceptable(BlindedBlock406Response),
    ///500: Beacon node internal error.
    InternalServerError(BlindedBlock400Response),
    ///default: Unknown response
    Unknown,
}
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
#[serde(default)]
pub struct GetDebugDataColumnSidecarsResponseResponse {
    pub data: Vec<GetDebugDataColumnSidecarsResponseResponseDatum>,
    ///True if the response references an unverified execution payload. Optimistic information may be invalidated at a later time. If the field is not present, assume the False value.
    pub execution_optimistic: bool,
    ///True if the response references the finalized history of the chain, as determined by fork choice. If the field is not present, additional calls are necessary to compare the epoch of the requested information with the finalized checkpoint.
    pub finalized: bool,
    #[default("fulu".to_string())]
    pub version: String,
}
///A [`DataColumnSidecar`](https://github.com/ethereum/consensus-specs/blob/dev/specs/fulu/das-core.md#datacolumnsidecar) as defined in the Fulu consensus spec.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct GetDebugDataColumnSidecarsResponseResponseDatum {
    pub column: Vec<String>,
    pub index: String,
    pub kzg_commitments: Vec<String>,
    pub kzg_commitments_inclusion_proof: Vec<String>,
    pub kzg_proofs: Vec<String>,
    ///The [`SignedBeaconBlockHeader`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/phase0/beacon-chain.md#signedbeaconblockheader) object envelope from the CL spec.
    pub signed_block_header: Phase0ProposerSlashingSignedHeader1,
}
///Retrieves all current fork choice context.
#[derive(Debug, Clone, validator::Validate, oas3_gen_support::Default)]
pub struct GetDebugForkChoiceRequest {}
impl GetDebugForkChoiceRequest {
    ///Parse the HTTP response into the response enum.
    pub async fn parse_response(
        req: reqwest::Response,
    ) -> anyhow::Result<GetDebugForkChoiceResponse> {
        let status = req.status();
        if status.is_success() {
            let data = oas3_gen_support::Diagnostics::<
                GetForkChoiceResponseResponse,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetDebugForkChoiceResponse::Ok(data));
        }
        if status.as_u16() == 500u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetDebugForkChoiceResponse::InternalServerError(data));
        }
        let _ = req.bytes().await?;
        return Ok(GetDebugForkChoiceResponse::Unknown);
    }
}
///Response types for getDebugForkChoice
#[derive(Debug, Clone)]
pub enum GetDebugForkChoiceResponse {
    ///200: Success
    Ok(GetForkChoiceResponseResponse),
    ///500: Beacon node internal error.
    InternalServerError(BlindedBlock400Response),
    ///default: Unknown response
    Unknown,
}
///Retrieve Eth1 deposit contract address and chain ID.
#[derive(Debug, Clone, validator::Validate, oas3_gen_support::Default)]
pub struct GetDepositContractRequest {}
impl GetDepositContractRequest {
    ///Parse the HTTP response into the response enum.
    pub async fn parse_response(
        req: reqwest::Response,
    ) -> anyhow::Result<GetDepositContractResponse> {
        let status = req.status();
        if status.is_success() {
            let data = oas3_gen_support::Diagnostics::<
                GetDepositContractResponseResponse,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetDepositContractResponse::Ok(data));
        }
        if status.as_u16() == 500u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetDepositContractResponse::InternalServerError(data));
        }
        let _ = req.bytes().await?;
        return Ok(GetDepositContractResponse::Unknown);
    }
}
///Response types for getDepositContract
#[derive(Debug, Clone)]
pub enum GetDepositContractResponse {
    ///200: Success
    Ok(GetDepositContractResponseResponse),
    ///500: Beacon node internal error.
    InternalServerError(BlindedBlock400Response),
    ///default: Unknown response
    Unknown,
}
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct GetDepositContractResponseResponse {
    pub data: GetDepositContractResponseResponseData,
}
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct GetDepositContractResponseResponseData {
    ///Hex encoded deposit contract address with 0x prefix
    pub address: String,
    ///Id of Eth1 chain on which contract is deployed.
    pub chain_id: String,
}
///Retrieves the committees for the given state.
#[derive(Debug, Clone, validator::Validate, oas3_gen_support::Default)]
pub struct GetEpochCommitteesRequest {
    #[validate(nested)]
    pub path: GetEpochCommitteesRequestPath,
    pub query: GetEpochCommitteesRequestQuery,
}
#[bon::bon]
impl GetEpochCommitteesRequest {
    ///Create a new request with the given parameters.
    #[builder]
    pub fn new(
        state_id: String,
        epoch: Option<String>,
        index: Option<String>,
        slot: Option<String>,
    ) -> anyhow::Result<Self> {
        let request = Self {
            path: GetEpochCommitteesRequestPath {
                state_id,
            },
            query: GetEpochCommitteesRequestQuery {
                epoch,
                index,
                slot,
            },
        };
        request.validate()?;
        Ok(request)
    }
}
impl GetEpochCommitteesRequest {
    ///Parse the HTTP response into the response enum.
    pub async fn parse_response(
        req: reqwest::Response,
    ) -> anyhow::Result<GetEpochCommitteesResponse> {
        let status = req.status();
        if status.is_success() {
            let data = oas3_gen_support::Diagnostics::<
                GetEpochCommitteesResponseResponse,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetEpochCommitteesResponse::Ok(data));
        }
        if status.as_u16() == 400u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetEpochCommitteesResponse::BadRequest(data));
        }
        if status.as_u16() == 404u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetEpochCommitteesResponse::NotFound(data));
        }
        if status.as_u16() == 500u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetEpochCommitteesResponse::InternalServerError(data));
        }
        let _ = req.bytes().await?;
        return Ok(GetEpochCommitteesResponse::Unknown);
    }
}
#[derive(Debug, Clone, PartialEq, validator::Validate, oas3_gen_support::Default)]
pub struct GetEpochCommitteesRequestPath {
    ///State identifier.
    ///Can be one of: "head" (canonical head in node's view), "genesis", "finalized", "justified", \<slot\>, \<hex encoded stateRoot with 0x prefix\>.
    ///- Example: `"head".to_string()`
    #[validate(length(min = 1u64))]
    pub state_id: String,
}
#[serde_with::skip_serializing_none]
#[derive(Debug, Clone, PartialEq, Serialize, oas3_gen_support::Default)]
pub struct GetEpochCommitteesRequestQuery {
    ///Fetch committees for the given epoch.  If not present then the committees for the epoch of the state will be obtained.
    pub epoch: Option<String>,
    ///Restrict returned values to those matching the supplied committee index.
    pub index: Option<String>,
    ///Restrict returned values to those matching the supplied slot.
    pub slot: Option<String>,
}
///Response types for getEpochCommittees
#[derive(Debug, Clone)]
pub enum GetEpochCommitteesResponse {
    ///200: Success
    Ok(GetEpochCommitteesResponseResponse),
    ///400: Invalid state ID, index, epoch, slot, or combination thereof
    BadRequest(BlindedBlock400Response),
    ///404: State not found
    NotFound(BlindedBlock400Response),
    ///500: Beacon node internal error.
    InternalServerError(BlindedBlock400Response),
    ///default: Unknown response
    Unknown,
}
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct GetEpochCommitteesResponseResponse {
    pub data: Vec<GetEpochCommitteesResponseResponseDatum>,
    ///True if the response references an unverified execution payload. Optimistic information may be invalidated at a later time. If the field is not present, assume the False value.
    pub execution_optimistic: bool,
    ///True if the response references the finalized history of the chain, as determined by fork choice. If the field is not present, additional calls are necessary to compare the epoch of the requested information with the finalized checkpoint.
    pub finalized: bool,
}
///Group of validators assigned to attest at specific slot and that have the same committee index (shard in phase 1)
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct GetEpochCommitteesResponseResponseDatum {
    ///Committee index at a slot
    pub index: String,
    pub slot: String,
    ///List of validator indices assigned to this committee
    pub validators: Vec<String>,
}
///Retrieves the current sync committee for the given state. Also returns the subcommittee assignments.
#[derive(Debug, Clone, validator::Validate, oas3_gen_support::Default)]
pub struct GetEpochSyncCommitteesRequest {
    #[validate(nested)]
    pub path: GetEpochSyncCommitteesRequestPath,
    pub query: GetEpochSyncCommitteesRequestQuery,
}
#[bon::bon]
impl GetEpochSyncCommitteesRequest {
    ///Create a new request with the given parameters.
    #[builder]
    pub fn new(state_id: String, epoch: Option<String>) -> anyhow::Result<Self> {
        let request = Self {
            path: GetEpochSyncCommitteesRequestPath {
                state_id,
            },
            query: GetEpochSyncCommitteesRequestQuery {
                epoch,
            },
        };
        request.validate()?;
        Ok(request)
    }
}
impl GetEpochSyncCommitteesRequest {
    ///Parse the HTTP response into the response enum.
    pub async fn parse_response(
        req: reqwest::Response,
    ) -> anyhow::Result<GetEpochSyncCommitteesResponse> {
        let status = req.status();
        if status.is_success() {
            let data = oas3_gen_support::Diagnostics::<
                GetEpochSyncCommitteesResponseResponse,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetEpochSyncCommitteesResponse::Ok(data));
        }
        if status.as_u16() == 400u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetEpochSyncCommitteesResponse::BadRequest(data));
        }
        if status.as_u16() == 404u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetEpochSyncCommitteesResponse::NotFound(data));
        }
        if status.as_u16() == 500u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetEpochSyncCommitteesResponse::InternalServerError(data));
        }
        let _ = req.bytes().await?;
        return Ok(GetEpochSyncCommitteesResponse::Unknown);
    }
}
#[derive(Debug, Clone, PartialEq, validator::Validate, oas3_gen_support::Default)]
pub struct GetEpochSyncCommitteesRequestPath {
    ///State identifier.
    ///Can be one of: "head" (canonical head in node's view), "genesis", "finalized", "justified", \<slot\>, \<hex encoded stateRoot with 0x prefix\>.
    ///- Example: `"head".to_string()`
    #[validate(length(min = 1u64))]
    pub state_id: String,
}
#[serde_with::skip_serializing_none]
#[derive(Debug, Clone, PartialEq, Serialize, oas3_gen_support::Default)]
pub struct GetEpochSyncCommitteesRequestQuery {
    ///Fetch sync committees for the given epoch.  If not present then the sync committees for the epoch of the state will be obtained.
    pub epoch: Option<String>,
}
///Response types for getEpochSyncCommittees
#[derive(Debug, Clone)]
pub enum GetEpochSyncCommitteesResponse {
    ///200: Success
    Ok(GetEpochSyncCommitteesResponseResponse),
    ///400: Invalid state ID or epoch
    BadRequest(BlindedBlock400Response),
    ///404: State not found
    NotFound(BlindedBlock400Response),
    ///500: Beacon node internal error.
    InternalServerError(BlindedBlock400Response),
    ///default: Unknown response
    Unknown,
}
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct GetEpochSyncCommitteesResponseResponse {
    pub data: GetEpochSyncCommitteesResponseResponseData,
    ///True if the response references an unverified execution payload. Optimistic information may be invalidated at a later time. If the field is not present, assume the False value.
    pub execution_optimistic: bool,
    ///True if the response references the finalized history of the chain, as determined by fork choice. If the field is not present, additional calls are necessary to compare the epoch of the requested information with the finalized checkpoint.
    pub finalized: bool,
}
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct GetEpochSyncCommitteesResponseResponseData {
    pub validator_aggregates: Vec<Vec<String>>,
    ///all of the validator indices in the current sync committee
    pub validators: Vec<String>,
}
///Debugging context of fork choice
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct GetForkChoiceResponseResponse {
    ///Optional extra data that clients may provide, which could differ from client to client.
    pub extra_data: Option<serde_json::Value>,
    ///The [`Checkpoint`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/phase0/beacon-chain.md#checkpoint) object from the CL spec.
    pub finalized_checkpoint: AltairBeaconStateCurrentJustifiedCheckpoint,
    ///Fork choice nodes
    pub fork_choice_nodes: Vec<GetForkChoiceResponseResponseForkChoiceNode>,
    ///The [`Checkpoint`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/phase0/beacon-chain.md#checkpoint) object from the CL spec.
    pub justified_checkpoint: AltairBeaconStateCurrentJustifiedCheckpoint,
}
///fork choice node attributes
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct GetForkChoiceResponseResponseForkChoiceNode {
    ///The signing merkle root of the `BeaconBlock`.
    pub block_root: String,
    ///The `block_hash` from the `execution_payload` of the `BeaconBlock`
    pub execution_block_hash: String,
    ///Optional extra data that clients may provide, which could differ from client to client.
    pub extra_data: Option<serde_json::Value>,
    pub finalized_epoch: String,
    pub justified_epoch: String,
    ///The signing merkle root of the parent `BeaconBlock`.
    pub parent_root: String,
    ///The slot to which this block corresponds.
    pub slot: String,
    pub validity: NodeValidity,
    pub weight: String,
}
///Retrieve all forks, past present and future, of which this node is aware.
#[derive(Debug, Clone, validator::Validate, oas3_gen_support::Default)]
pub struct GetForkScheduleRequest {}
impl GetForkScheduleRequest {
    ///Parse the HTTP response into the response enum.
    pub async fn parse_response(
        req: reqwest::Response,
    ) -> anyhow::Result<GetForkScheduleResponse> {
        let status = req.status();
        if status.is_success() {
            let data = oas3_gen_support::Diagnostics::<
                GetForkScheduleResponseResponse,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetForkScheduleResponse::Ok(data));
        }
        if status.as_u16() == 500u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetForkScheduleResponse::InternalServerError(data));
        }
        let _ = req.bytes().await?;
        return Ok(GetForkScheduleResponse::Unknown);
    }
}
///Response types for getForkSchedule
#[derive(Debug, Clone)]
pub enum GetForkScheduleResponse {
    ///200: Success
    Ok(GetForkScheduleResponseResponse),
    ///500: Beacon node internal error.
    InternalServerError(BlindedBlock400Response),
    ///default: Unknown response
    Unknown,
}
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct GetForkScheduleResponseResponse {
    pub data: Vec<BeaconStateFork>,
}
///Retrieve details of the chain's genesis which can be used to identify chain.
#[derive(Debug, Clone, validator::Validate, oas3_gen_support::Default)]
pub struct GetGenesisRequest {}
impl GetGenesisRequest {
    ///Parse the HTTP response into the response enum.
    pub async fn parse_response(
        req: reqwest::Response,
    ) -> anyhow::Result<GetGenesisResponse> {
        let status = req.status();
        if status.is_success() {
            let data = oas3_gen_support::Diagnostics::<
                GetGenesisResponseResponse,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetGenesisResponse::Ok(data));
        }
        if status.as_u16() == 404u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetGenesisResponse::NotFound(data));
        }
        if status.as_u16() == 500u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetGenesisResponse::InternalServerError(data));
        }
        let _ = req.bytes().await?;
        return Ok(GetGenesisResponse::Unknown);
    }
}
///Response types for getGenesis
#[derive(Debug, Clone)]
pub enum GetGenesisResponse {
    ///200: Request successful
    Ok(GetGenesisResponseResponse),
    ///404: Chain genesis info is not yet known
    NotFound(BlindedBlock400Response),
    ///500: Beacon node internal error.
    InternalServerError(BlindedBlock400Response),
    ///default: Unknown response
    Unknown,
}
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct GetGenesisResponseResponse {
    pub data: GetGenesisResponseResponseData,
}
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct GetGenesisResponseResponseData {
    ///a fork version number
    pub genesis_fork_version: String,
    ///The genesis_time configured for the beacon node, which is the unix time in seconds at which the Eth2.0 chain began.
    pub genesis_time: String,
    pub genesis_validators_root: String,
}
///Returns node health status in http status codes. Useful for load balancers.
#[derive(Debug, Clone, validator::Validate, oas3_gen_support::Default)]
pub struct GetHealthRequest {
    #[validate(nested)]
    pub query: GetHealthRequestQuery,
}
#[bon::bon]
impl GetHealthRequest {
    ///Create a new request with the given parameters.
    #[builder]
    pub fn new(syncing_status: Option<i64>) -> anyhow::Result<Self> {
        let request = Self {
            query: GetHealthRequestQuery {
                syncing_status,
            },
        };
        request.validate()?;
        Ok(request)
    }
}
impl GetHealthRequest {
    ///Parse the HTTP response into the response enum.
    pub async fn parse_response(
        req: reqwest::Response,
    ) -> anyhow::Result<GetHealthResponse> {
        let status = req.status();
        if status.is_success() {
            let _ = req.bytes().await?;
            return Ok(GetHealthResponse::Ok);
        }
        if status.is_success() {
            let _ = req.bytes().await?;
            return Ok(GetHealthResponse::PartialContent);
        }
        if status.as_u16() == 400u16 {
            let _ = req.bytes().await?;
            return Ok(GetHealthResponse::BadRequest);
        }
        if status.as_u16() == 503u16 {
            let _ = req.bytes().await?;
            return Ok(GetHealthResponse::ServiceUnavailable);
        }
        let _ = req.bytes().await?;
        return Ok(GetHealthResponse::Unknown);
    }
}
#[serde_with::skip_serializing_none]
#[derive(
    Debug,
    Clone,
    PartialEq,
    Serialize,
    validator::Validate,
    oas3_gen_support::Default
)]
pub struct GetHealthRequestQuery {
    ///Customize syncing status instead of default status code (206)
    #[validate(range(min = 100i64, max = 599i64))]
    pub syncing_status: Option<i64>,
}
///Response types for getHealth
#[derive(Debug, Clone)]
pub enum GetHealthResponse {
    ///200: Node is ready
    Ok,
    ///206: Either the beacon node is syncing, or its execution node is optimistic or offline, so data served may be incorrect
    PartialContent,
    ///400: Invalid syncing status code
    BadRequest,
    ///503: Node not initialized or having issues
    ServiceUnavailable,
    ///default: Unknown response
    Unknown,
}
///Requests the [`LightClientBootstrap`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/altair/light-client/sync-protocol.md#lightclientbootstrap) structure corresponding to a given post-Altair beacon block root.
///Depending on the `Accept` header it can be returned either as JSON or SSZ-serialized bytes.
///
///Servers SHOULD provide results as defined in [`create_light_client_bootstrap`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/altair/light-client/full-node.md#create_light_client_bootstrap). To fulfill a request, the requested block's post state needs to be known.
#[derive(Debug, Clone, validator::Validate, oas3_gen_support::Default)]
pub struct GetLightClientBootstrapRequest {
    #[validate(nested)]
    pub path: GetLightClientBootstrapRequestPath,
}
#[bon::bon]
impl GetLightClientBootstrapRequest {
    ///Create a new request with the given parameters.
    #[builder]
    pub fn new(block_root: String) -> anyhow::Result<Self> {
        let request = Self {
            path: GetLightClientBootstrapRequestPath {
                block_root,
            },
        };
        request.validate()?;
        Ok(request)
    }
}
impl GetLightClientBootstrapRequest {
    ///Parse the HTTP response into the response enum.
    pub async fn parse_response(
        req: reqwest::Response,
    ) -> anyhow::Result<GetLightClientBootstrapResponse> {
        let status = req.status();
        if status.is_success() {
            let content_type_str = req
                .headers()
                .get(reqwest::header::CONTENT_TYPE)
                .and_then(|v| v.to_str().ok())
                .unwrap_or("application/json");
            if content_type_str.contains("json") {
                let data = oas3_gen_support::Diagnostics::<
                    GetLightClientBootstrapResponseResponse,
                >::json_with_diagnostics(req)
                    .await?;
                return Ok(GetLightClientBootstrapResponse::Ok(data));
            }
            if content_type_str.starts_with("application/octet-stream")
                || content_type_str.starts_with("image/")
                || content_type_str.starts_with("audio/")
                || content_type_str.starts_with("video/")
            {
                let data = req.bytes().await?.to_vec();
                return Ok(GetLightClientBootstrapResponse::OkBinary(data));
            }
        }
        if status.as_u16() == 400u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetLightClientBootstrapResponse::BadRequest(data));
        }
        if status.as_u16() == 404u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetLightClientBootstrapResponse::NotFound(data));
        }
        if status.as_u16() == 406u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock406Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetLightClientBootstrapResponse::NotAcceptable(data));
        }
        if status.as_u16() == 500u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetLightClientBootstrapResponse::InternalServerError(data));
        }
        let _ = req.bytes().await?;
        return Ok(GetLightClientBootstrapResponse::Unknown);
    }
}
#[derive(Debug, Clone, PartialEq, validator::Validate, oas3_gen_support::Default)]
pub struct GetLightClientBootstrapRequestPath {
    ///Block root.
    ///\<hex encoded blockRoot with 0x prefix\>.
    #[validate(
        length(min = 1u64),
        regex(path = "REGEX_ALTAIR_BEACON_STATE_CURRENT_JUSTIFIED_CHECKPOINT_ROOT")
    )]
    pub block_root: String,
}
///Response types for getLightClientBootstrap
#[derive(Debug, Clone)]
pub enum GetLightClientBootstrapResponse {
    ///200: Success
    Ok(GetLightClientBootstrapResponseResponse),
    ///200: Success
    OkBinary(Vec<u8>),
    ///400: Malformed request parameter
    BadRequest(BlindedBlock400Response),
    ///404: `LightClientBootstrap` instance cannot be produced for the given block root
    NotFound(BlindedBlock400Response),
    ///406: Accepted media type is not supported.
    NotAcceptable(BlindedBlock406Response),
    ///500: Beacon node internal error.
    InternalServerError(BlindedBlock400Response),
    ///default: Unknown response
    Unknown,
}
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct GetLightClientBootstrapResponseResponse {
    pub data: GetLightClientBootstrapResponseResponseData,
    pub version: ConsensusVersion,
}
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
#[serde(untagged)]
pub enum GetLightClientBootstrapResponseResponseData {
    #[default]
    Object(GetLightClientBootstrapResponseResponseDataObject),
    Object2(GetLightClientBootstrapResponseResponseDataObject2),
    Object3(GetLightClientBootstrapResponseResponseDataObject3),
    Object4(GetLightClientBootstrapResponseResponseDataObject4),
}
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct GetLightClientBootstrapResponseResponseDataObject {
    pub current_sync_committee: AltairBeaconStateCurrentSyncCommittee,
    pub current_sync_committee_branch: Vec<String>,
    pub header: Header2,
}
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct GetLightClientBootstrapResponseResponseDataObject2 {
    pub current_sync_committee: AltairBeaconStateCurrentSyncCommittee,
    pub current_sync_committee_branch: Vec<String>,
    pub header: Header2,
}
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct GetLightClientBootstrapResponseResponseDataObject3 {
    pub current_sync_committee: AltairBeaconStateCurrentSyncCommittee,
    pub current_sync_committee_branch: Vec<String>,
    pub header: Header,
}
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct GetLightClientBootstrapResponseResponseDataObject4 {
    pub current_sync_committee: AltairBeaconStateCurrentSyncCommittee,
    pub current_sync_committee_branch: Vec<String>,
    pub header: Header3,
}
///Requests the latest [`LightClientFinalityUpdate`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/altair/light-client/sync-protocol.md#lightclientfinalityupdate) known by the server.
///Depending on the `Accept` header it can be returned either as JSON or SSZ-serialized bytes.
///
///Servers SHOULD provide results as defined in [`create_light_client_finality_update`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/altair/light-client/full-node.md#create_light_client_finality_update).
#[derive(Debug, Clone, validator::Validate, oas3_gen_support::Default)]
pub struct GetLightClientFinalityUpdateRequest {}
impl GetLightClientFinalityUpdateRequest {
    ///Parse the HTTP response into the response enum.
    pub async fn parse_response(
        req: reqwest::Response,
    ) -> anyhow::Result<GetLightClientFinalityUpdateResponse> {
        let status = req.status();
        if status.is_success() {
            let content_type_str = req
                .headers()
                .get(reqwest::header::CONTENT_TYPE)
                .and_then(|v| v.to_str().ok())
                .unwrap_or("application/json");
            if content_type_str.contains("json") {
                let data = oas3_gen_support::Diagnostics::<
                    GetLightClientFinalityUpdateResponseResponse,
                >::json_with_diagnostics(req)
                    .await?;
                return Ok(GetLightClientFinalityUpdateResponse::Ok(data));
            }
            if content_type_str.starts_with("application/octet-stream")
                || content_type_str.starts_with("image/")
                || content_type_str.starts_with("audio/")
                || content_type_str.starts_with("video/")
            {
                let data = req.bytes().await?.to_vec();
                return Ok(GetLightClientFinalityUpdateResponse::OkBinary(data));
            }
        }
        if status.as_u16() == 404u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetLightClientFinalityUpdateResponse::NotFound(data));
        }
        if status.as_u16() == 406u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock406Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetLightClientFinalityUpdateResponse::NotAcceptable(data));
        }
        if status.as_u16() == 500u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetLightClientFinalityUpdateResponse::InternalServerError(data));
        }
        let _ = req.bytes().await?;
        return Ok(GetLightClientFinalityUpdateResponse::Unknown);
    }
}
///Response types for getLightClientFinalityUpdate
#[derive(Debug, Clone)]
pub enum GetLightClientFinalityUpdateResponse {
    ///200: Success
    Ok(GetLightClientFinalityUpdateResponseResponse),
    ///200: Success
    OkBinary(Vec<u8>),
    ///404: No `LightClientFinalityUpdate` is available
    NotFound(BlindedBlock400Response),
    ///406: Accepted media type is not supported.
    NotAcceptable(BlindedBlock406Response),
    ///500: Beacon node internal error.
    InternalServerError(BlindedBlock400Response),
    ///default: Unknown response
    Unknown,
}
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct GetLightClientFinalityUpdateResponseResponse {
    pub data: GetLightClientFinalityUpdateResponseResponseData,
    pub version: ConsensusVersion,
}
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
#[serde(untagged)]
pub enum GetLightClientFinalityUpdateResponseResponseData {
    #[default]
    Object(GetLightClientFinalityUpdateResponseResponseDataObject),
    Object2(GetLightClientFinalityUpdateResponseResponseDataObject2),
    Object3(GetLightClientFinalityUpdateResponseResponseDataObject3),
    Object4(GetLightClientFinalityUpdateResponseResponseDataObject4),
}
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct GetLightClientFinalityUpdateResponseResponseDataObject {
    pub attested_header: Header2,
    pub finality_branch: Vec<String>,
    pub finalized_header: Header2,
    pub signature_slot: String,
    ///The [`SyncAggregate`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/altair/beacon-chain.md#syncaggregate) object from the CL Altair spec.
    pub sync_aggregate: SyncAggregate,
}
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct GetLightClientFinalityUpdateResponseResponseDataObject2 {
    pub attested_header: Header2,
    pub finality_branch: Vec<String>,
    pub finalized_header: Header2,
    pub signature_slot: String,
    ///The [`SyncAggregate`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/altair/beacon-chain.md#syncaggregate) object from the CL Altair spec.
    pub sync_aggregate: SyncAggregate,
}
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct GetLightClientFinalityUpdateResponseResponseDataObject3 {
    pub attested_header: Header,
    pub finality_branch: Vec<String>,
    pub finalized_header: Header,
    pub signature_slot: String,
    ///The [`SyncAggregate`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/altair/beacon-chain.md#syncaggregate) object from the CL Altair spec.
    pub sync_aggregate: SyncAggregate,
}
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct GetLightClientFinalityUpdateResponseResponseDataObject4 {
    pub attested_header: Header3,
    pub finality_branch: Vec<String>,
    pub finalized_header: Header3,
    pub signature_slot: String,
    ///The [`SyncAggregate`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/altair/beacon-chain.md#syncaggregate) object from the CL Altair spec.
    pub sync_aggregate: SyncAggregate,
}
///Requests the latest [`LightClientOptimisticUpdate`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/altair/light-client/sync-protocol.md#lightclientoptimisticupdate) known by the server.
///Depending on the `Accept` header it can be returned either as JSON or SSZ-serialized bytes.
///
///Servers SHOULD provide results as defined in [`create_light_client_optimistic_update`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/altair/light-client/full-node.md#create_light_client_optimistic_update).
#[derive(Debug, Clone, validator::Validate, oas3_gen_support::Default)]
pub struct GetLightClientOptimisticUpdateRequest {}
impl GetLightClientOptimisticUpdateRequest {
    ///Parse the HTTP response into the response enum.
    pub async fn parse_response(
        req: reqwest::Response,
    ) -> anyhow::Result<GetLightClientOptimisticUpdateResponse> {
        let status = req.status();
        if status.is_success() {
            let content_type_str = req
                .headers()
                .get(reqwest::header::CONTENT_TYPE)
                .and_then(|v| v.to_str().ok())
                .unwrap_or("application/json");
            if content_type_str.contains("json") {
                let data = oas3_gen_support::Diagnostics::<
                    GetLightClientOptimisticUpdateResponseResponse,
                >::json_with_diagnostics(req)
                    .await?;
                return Ok(GetLightClientOptimisticUpdateResponse::Ok(data));
            }
            if content_type_str.starts_with("application/octet-stream")
                || content_type_str.starts_with("image/")
                || content_type_str.starts_with("audio/")
                || content_type_str.starts_with("video/")
            {
                let data = req.bytes().await?.to_vec();
                return Ok(GetLightClientOptimisticUpdateResponse::OkBinary(data));
            }
        }
        if status.as_u16() == 404u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetLightClientOptimisticUpdateResponse::NotFound(data));
        }
        if status.as_u16() == 406u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock406Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetLightClientOptimisticUpdateResponse::NotAcceptable(data));
        }
        if status.as_u16() == 500u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetLightClientOptimisticUpdateResponse::InternalServerError(data));
        }
        let _ = req.bytes().await?;
        return Ok(GetLightClientOptimisticUpdateResponse::Unknown);
    }
}
///Response types for getLightClientOptimisticUpdate
#[derive(Debug, Clone)]
pub enum GetLightClientOptimisticUpdateResponse {
    ///200: Success
    Ok(GetLightClientOptimisticUpdateResponseResponse),
    ///200: Success
    OkBinary(Vec<u8>),
    ///404: No `LightClientOptimisticUpdate` is available
    NotFound(BlindedBlock400Response),
    ///406: Accepted media type is not supported.
    NotAcceptable(BlindedBlock406Response),
    ///500: Beacon node internal error.
    InternalServerError(BlindedBlock400Response),
    ///default: Unknown response
    Unknown,
}
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct GetLightClientOptimisticUpdateResponseResponse {
    pub data: GetLightClientOptimisticUpdateResponseResponseData,
    pub version: ConsensusVersion,
}
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
#[serde(untagged)]
pub enum GetLightClientOptimisticUpdateResponseResponseData {
    #[default]
    Object(GetLightClientOptimisticUpdateResponseResponseDataObject),
    Object2(GetLightClientOptimisticUpdateResponseResponseDataObject),
    Object3(GetLightClientOptimisticUpdateResponseResponseDataObject3),
    Object4(GetLightClientOptimisticUpdateResponseResponseDataObject4),
}
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct GetLightClientOptimisticUpdateResponseResponseDataObject {
    pub attested_header: Header2,
    pub signature_slot: String,
    ///The [`SyncAggregate`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/altair/beacon-chain.md#syncaggregate) object from the CL Altair spec.
    pub sync_aggregate: SyncAggregate,
}
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct GetLightClientOptimisticUpdateResponseResponseDataObject3 {
    pub attested_header: Header,
    pub signature_slot: String,
    ///The [`SyncAggregate`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/altair/beacon-chain.md#syncaggregate) object from the CL Altair spec.
    pub sync_aggregate: SyncAggregate,
}
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct GetLightClientOptimisticUpdateResponseResponseDataObject4 {
    pub attested_header: Header3,
    pub signature_slot: String,
    ///The [`SyncAggregate`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/altair/beacon-chain.md#syncaggregate) object from the CL Altair spec.
    pub sync_aggregate: SyncAggregate,
}
///Requests the [`LightClientUpdate`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/altair/light-client/sync-protocol.md#lightclientupdate) instances in the sync committee period range `[start_period, start_period + count)`, leading up to the current head sync committee period as selected by fork choice.
///Depending on the `Accept` header they can be returned either as JSON or SSZ-serialized bytes.
///
///Servers SHOULD provide results as defined in [`create_light_client_update`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/altair/light-client/full-node.md#create_light_client_update). They MUST respond with at least the earliest known result within the requested range, and MUST send results in consecutive order (by period). The response MUST NOT contain more than [`min(MAX_REQUEST_LIGHT_CLIENT_UPDATES, count)`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/altair/light-client/p2p-interface.md#configuration) results.
#[derive(Debug, Clone, validator::Validate, oas3_gen_support::Default)]
pub struct GetLightClientUpdatesByRangeRequest {
    #[validate(nested)]
    pub query: GetLightClientUpdatesByRangeRequestQuery,
}
#[bon::bon]
impl GetLightClientUpdatesByRangeRequest {
    ///Create a new request with the given parameters.
    #[builder]
    pub fn new(start_period: String, count: String) -> anyhow::Result<Self> {
        let request = Self {
            query: GetLightClientUpdatesByRangeRequestQuery {
                start_period,
                count,
            },
        };
        request.validate()?;
        Ok(request)
    }
}
impl GetLightClientUpdatesByRangeRequest {
    ///Parse the HTTP response into the response enum.
    pub async fn parse_response(
        req: reqwest::Response,
    ) -> anyhow::Result<GetLightClientUpdatesByRangeResponse> {
        let status = req.status();
        if status.is_success() {
            let content_type_str = req
                .headers()
                .get(reqwest::header::CONTENT_TYPE)
                .and_then(|v| v.to_str().ok())
                .unwrap_or("application/json");
            if content_type_str.contains("json") {
                let data = oas3_gen_support::Diagnostics::<
                    Vec<serde_json::Value>,
                >::json_with_diagnostics(req)
                    .await?;
                return Ok(GetLightClientUpdatesByRangeResponse::Ok(data));
            }
            if content_type_str.starts_with("application/octet-stream")
                || content_type_str.starts_with("image/")
                || content_type_str.starts_with("audio/")
                || content_type_str.starts_with("video/")
            {
                let data = req.bytes().await?.to_vec();
                return Ok(GetLightClientUpdatesByRangeResponse::OkBinary(data));
            }
        }
        if status.as_u16() == 400u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetLightClientUpdatesByRangeResponse::BadRequest(data));
        }
        if status.as_u16() == 406u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock406Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetLightClientUpdatesByRangeResponse::NotAcceptable(data));
        }
        if status.as_u16() == 500u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetLightClientUpdatesByRangeResponse::InternalServerError(data));
        }
        let _ = req.bytes().await?;
        return Ok(GetLightClientUpdatesByRangeResponse::Unknown);
    }
}
#[derive(
    Debug,
    Clone,
    PartialEq,
    Serialize,
    validator::Validate,
    oas3_gen_support::Default
)]
pub struct GetLightClientUpdatesByRangeRequestQuery {
    #[validate(length(min = 1u64))]
    pub start_period: String,
    #[validate(length(min = 1u64))]
    pub count: String,
}
///Response types for getLightClientUpdatesByRange
#[derive(Debug, Clone)]
pub enum GetLightClientUpdatesByRangeResponse {
    ///200: Success
    Ok(Vec<serde_json::Value>),
    ///200: Success
    OkBinary(Vec<u8>),
    ///400: Malformed or missing request parameter
    BadRequest(BlindedBlock400Response),
    ///406: Accepted media type is not supported.
    NotAcceptable(BlindedBlock406Response),
    ///500: Beacon node internal error.
    InternalServerError(BlindedBlock400Response),
    ///default: Unknown response
    Unknown,
}
///Requests the beacon node to indicate if a validator has been observed to be live in a given epoch. The beacon node might detect liveness by observing messages from the validator on the network, in the beacon chain, from its API or from any other source. A beacon node SHOULD support the current and previous epoch, however it MAY support earlier epoch. It is important to note that the values returned by the beacon node are not canonical; they are best-effort and based upon a subjective view of the network. A beacon node that was recently started or suffered a network partition may indicate that a validator is not live when it actually is.
#[derive(Debug, Clone, validator::Validate, oas3_gen_support::Default)]
pub struct GetLivenessRequest {
    #[validate(nested)]
    pub path: GetLivenessRequestPath,
    ///An array of the validator indices for which to detect liveness.
    pub body: PostLivenessRequestBodyRequestBody,
}
#[bon::bon]
impl GetLivenessRequest {
    ///Create a new request with the given parameters.
    #[builder]
    pub fn new(
        epoch: String,
        body: PostLivenessRequestBodyRequestBody,
    ) -> anyhow::Result<Self> {
        let request = Self {
            path: GetLivenessRequestPath { epoch },
            body,
        };
        request.validate()?;
        Ok(request)
    }
}
impl GetLivenessRequest {
    ///Parse the HTTP response into the response enum.
    pub async fn parse_response(
        req: reqwest::Response,
    ) -> anyhow::Result<GetLivenessResponse> {
        let status = req.status();
        if status.is_success() {
            let data = oas3_gen_support::Diagnostics::<
                PostLivenessResponseBodyResponse,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetLivenessResponse::Ok(data));
        }
        if status.as_u16() == 400u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetLivenessResponse::BadRequest(data));
        }
        if status.as_u16() == 500u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetLivenessResponse::InternalServerError(data));
        }
        if status.as_u16() == 503u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetLivenessResponse::ServiceUnavailable(data));
        }
        let _ = req.bytes().await?;
        return Ok(GetLivenessResponse::Unknown);
    }
}
#[derive(Debug, Clone, PartialEq, validator::Validate, oas3_gen_support::Default)]
pub struct GetLivenessRequestPath {
    ///The epoch for which liveness is being queried
    #[validate(length(min = 1u64))]
    pub epoch: String,
}
///Response types for getLiveness
#[derive(Debug, Clone)]
pub enum GetLivenessResponse {
    ///200: Success response
    Ok(PostLivenessResponseBodyResponse),
    ///400: Invalid epoch or index
    BadRequest(BlindedBlock400Response),
    ///500: Beacon node internal error.
    InternalServerError(BlindedBlock400Response),
    ///503: Beacon node is currently syncing, try again later.
    ServiceUnavailable(BlindedBlock400Response),
    ///default: Unknown response
    Unknown,
}
///Retrieves data about the node's network presence
#[derive(Debug, Clone, validator::Validate, oas3_gen_support::Default)]
pub struct GetNetworkIdentityRequest {}
impl GetNetworkIdentityRequest {
    ///Parse the HTTP response into the response enum.
    pub async fn parse_response(
        req: reqwest::Response,
    ) -> anyhow::Result<GetNetworkIdentityResponse> {
        let status = req.status();
        if status.is_success() {
            let data = oas3_gen_support::Diagnostics::<
                GetNetworkIdentityResponseResponse,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetNetworkIdentityResponse::Ok(data));
        }
        if status.as_u16() == 500u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetNetworkIdentityResponse::InternalServerError(data));
        }
        let _ = req.bytes().await?;
        return Ok(GetNetworkIdentityResponse::Unknown);
    }
}
///Response types for getNetworkIdentity
#[derive(Debug, Clone)]
pub enum GetNetworkIdentityResponse {
    ///200: Request successful
    Ok(GetNetworkIdentityResponseResponse),
    ///500: Beacon node internal error.
    InternalServerError(BlindedBlock400Response),
    ///default: Unknown response
    Unknown,
}
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct GetNetworkIdentityResponseResponse {
    pub data: GetNetworkIdentityResponseResponseData,
}
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct GetNetworkIdentityResponseResponseData {
    pub discovery_addresses: Vec<String>,
    ///Ethereum node record. [Read more](https://eips.ethereum.org/EIPS/eip-778)
    pub enr: String,
    ///Based on eth2 [Metadata object](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/phase0/p2p-interface.md#metadata)
    pub metadata: NetworkIdentityMetadata,
    pub p2p_addresses: Vec<String>,
    ///Cryptographic hash of a peer’s public key. [Read more](https://docs.libp2p.io/concepts/peer-id/)
    pub peer_id: String,
}
///Requests that the beacon node identify information about its implementation in a format similar to a  [HTTP User-Agent](https://tools.ietf.org/html/rfc7231#section-5.5.3) field.
#[derive(Debug, Clone, validator::Validate, oas3_gen_support::Default)]
pub struct GetNodeVersionRequest {}
impl GetNodeVersionRequest {
    ///Parse the HTTP response into the response enum.
    pub async fn parse_response(
        req: reqwest::Response,
    ) -> anyhow::Result<GetNodeVersionResponse> {
        let status = req.status();
        if status.is_success() {
            let data = oas3_gen_support::Diagnostics::<
                GetVersionResponseResponse,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetNodeVersionResponse::Ok(data));
        }
        if status.as_u16() == 500u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetNodeVersionResponse::InternalServerError(data));
        }
        let _ = req.bytes().await?;
        return Ok(GetNodeVersionResponse::Unknown);
    }
}
///Response types for getNodeVersion
#[derive(Debug, Clone)]
pub enum GetNodeVersionResponse {
    ///200: Request successful
    Ok(GetVersionResponseResponse),
    ///500: Beacon node internal error.
    InternalServerError(BlindedBlock400Response),
    ///default: Unknown response
    Unknown,
}
///Retrieves number of known peers.
#[derive(Debug, Clone, validator::Validate, oas3_gen_support::Default)]
pub struct GetPeerCountRequest {}
impl GetPeerCountRequest {
    ///Parse the HTTP response into the response enum.
    pub async fn parse_response(
        req: reqwest::Response,
    ) -> anyhow::Result<GetPeerCountResponse> {
        let status = req.status();
        if status.is_success() {
            let data = oas3_gen_support::Diagnostics::<
                GetPeerCountResponseResponse,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetPeerCountResponse::Ok(data));
        }
        if status.as_u16() == 500u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetPeerCountResponse::InternalServerError(data));
        }
        let _ = req.bytes().await?;
        return Ok(GetPeerCountResponse::Unknown);
    }
}
///Response types for getPeerCount
#[derive(Debug, Clone)]
pub enum GetPeerCountResponse {
    ///200: Request successful
    Ok(GetPeerCountResponseResponse),
    ///500: Beacon node internal error.
    InternalServerError(BlindedBlock400Response),
    ///default: Unknown response
    Unknown,
}
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct GetPeerCountResponseResponse {
    pub data: GetPeerCountResponseResponseData,
}
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct GetPeerCountResponseResponseData {
    pub connected: String,
    pub connecting: String,
    pub disconnected: String,
    pub disconnecting: String,
}
///Retrieves data about the given peer
#[derive(Debug, Clone, validator::Validate, oas3_gen_support::Default)]
pub struct GetPeerRequest {
    #[validate(nested)]
    pub path: GetPeerRequestPath,
}
#[bon::bon]
impl GetPeerRequest {
    ///Create a new request with the given parameters.
    #[builder]
    pub fn new(peer_id: String) -> anyhow::Result<Self> {
        let request = Self {
            path: GetPeerRequestPath { peer_id },
        };
        request.validate()?;
        Ok(request)
    }
}
impl GetPeerRequest {
    ///Parse the HTTP response into the response enum.
    pub async fn parse_response(
        req: reqwest::Response,
    ) -> anyhow::Result<GetPeerResponse> {
        let status = req.status();
        if status.is_success() {
            let data = oas3_gen_support::Diagnostics::<
                GetPeerResponseResponse,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetPeerResponse::Ok(data));
        }
        if status.as_u16() == 400u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetPeerResponse::BadRequest(data));
        }
        if status.as_u16() == 404u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetPeerResponse::NotFound(data));
        }
        if status.as_u16() == 500u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetPeerResponse::InternalServerError(data));
        }
        let _ = req.bytes().await?;
        return Ok(GetPeerResponse::Unknown);
    }
}
#[derive(Debug, Clone, PartialEq, validator::Validate, oas3_gen_support::Default)]
pub struct GetPeerRequestPath {
    #[validate(length(min = 1u64))]
    pub peer_id: String,
}
///Response types for getPeer
#[derive(Debug, Clone)]
pub enum GetPeerResponse {
    ///200: Request successful
    Ok(GetPeerResponseResponse),
    ///400: The peer ID supplied could not be parsed
    BadRequest(BlindedBlock400Response),
    ///404: Peer not found
    NotFound(BlindedBlock400Response),
    ///500: Beacon node internal error.
    InternalServerError(BlindedBlock400Response),
    ///default: Unknown response
    Unknown,
}
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct GetPeerResponseResponse {
    pub data: GetPeersResponseResponseDatum,
}
///Retrieves data about the node's network peers. By default this returns all peers. Multiple query params are combined using AND conditions
#[derive(Debug, Clone, validator::Validate, oas3_gen_support::Default)]
pub struct GetPeersRequest {
    pub query: GetPeersRequestQuery,
}
#[bon::bon]
impl GetPeersRequest {
    ///Create a new request with the given parameters.
    #[builder]
    pub fn new(
        state: Option<Vec<PeerConnectionState>>,
        direction: Option<Vec<PeerConnectionDirection>>,
    ) -> anyhow::Result<Self> {
        let request = Self {
            query: GetPeersRequestQuery {
                state,
                direction,
            },
        };
        request.validate()?;
        Ok(request)
    }
}
impl GetPeersRequest {
    ///Parse the HTTP response into the response enum.
    pub async fn parse_response(
        req: reqwest::Response,
    ) -> anyhow::Result<GetPeersResponse> {
        let status = req.status();
        if status.is_success() {
            let data = oas3_gen_support::Diagnostics::<
                GetPeersResponseResponse,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetPeersResponse::Ok(data));
        }
        if status.as_u16() == 500u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetPeersResponse::InternalServerError(data));
        }
        let _ = req.bytes().await?;
        return Ok(GetPeersResponse::Unknown);
    }
}
#[serde_with::skip_serializing_none]
#[derive(Debug, Clone, PartialEq, Serialize, oas3_gen_support::Default)]
pub struct GetPeersRequestQuery {
    pub state: Option<Vec<PeerConnectionState>>,
    pub direction: Option<Vec<PeerConnectionDirection>>,
}
///Response types for getPeers
#[derive(Debug, Clone)]
pub enum GetPeersResponse {
    ///200: Request successful
    Ok(GetPeersResponseResponse),
    ///500: Beacon node internal error.
    InternalServerError(BlindedBlock400Response),
    ///default: Unknown response
    Unknown,
}
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct GetPeersResponseResponse {
    pub data: Vec<GetPeersResponseResponseDatum>,
    pub meta: GetPeersResponseResponseMeta,
}
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct GetPeersResponseResponseDatum {
    pub direction: PeerConnectionDirection,
    pub enr: Option<String>,
    ///Multiaddrs used in last peer connection.
    pub last_seen_p2p_address: String,
    ///Cryptographic hash of a peer’s public key. [Read more](https://docs.libp2p.io/concepts/peer-id/)
    pub peer_id: String,
    pub state: PeerConnectionState,
}
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct GetPeersResponseResponseMeta {
    ///Total number of items
    pub count: f64,
}
///Returns pending consolidations for state with given 'stateId'. Should return 400 if the state retrieved is prior to Electra.
#[derive(Debug, Clone, validator::Validate, oas3_gen_support::Default)]
pub struct GetPendingConsolidationsRequest {
    #[validate(nested)]
    pub path: GetPendingConsolidationsRequestPath,
}
#[bon::bon]
impl GetPendingConsolidationsRequest {
    ///Create a new request with the given parameters.
    #[builder]
    pub fn new(state_id: String) -> anyhow::Result<Self> {
        let request = Self {
            path: GetPendingConsolidationsRequestPath {
                state_id,
            },
        };
        request.validate()?;
        Ok(request)
    }
}
impl GetPendingConsolidationsRequest {
    ///Parse the HTTP response into the response enum.
    pub async fn parse_response(
        req: reqwest::Response,
    ) -> anyhow::Result<GetPendingConsolidationsResponse> {
        let status = req.status();
        if status.is_success() {
            let content_type_str = req
                .headers()
                .get(reqwest::header::CONTENT_TYPE)
                .and_then(|v| v.to_str().ok())
                .unwrap_or("application/json");
            if content_type_str.contains("json") {
                let data = oas3_gen_support::Diagnostics::<
                    GetPendingDepositsResponseResponse,
                >::json_with_diagnostics(req)
                    .await?;
                return Ok(GetPendingConsolidationsResponse::Ok(data));
            }
            if content_type_str.starts_with("application/octet-stream")
                || content_type_str.starts_with("image/")
                || content_type_str.starts_with("audio/")
                || content_type_str.starts_with("video/")
            {
                let data = req.bytes().await?.to_vec();
                return Ok(GetPendingConsolidationsResponse::OkBinary(data));
            }
        }
        if status.as_u16() == 400u16 {
            let data = oas3_gen_support::Diagnostics::<
                PendingConsolidation400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetPendingConsolidationsResponse::BadRequest(data));
        }
        if status.as_u16() == 404u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetPendingConsolidationsResponse::NotFound(data));
        }
        if status.as_u16() == 406u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock406Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetPendingConsolidationsResponse::NotAcceptable(data));
        }
        if status.as_u16() == 500u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetPendingConsolidationsResponse::InternalServerError(data));
        }
        let _ = req.bytes().await?;
        return Ok(GetPendingConsolidationsResponse::Unknown);
    }
}
#[derive(Debug, Clone, PartialEq, validator::Validate, oas3_gen_support::Default)]
pub struct GetPendingConsolidationsRequestPath {
    ///State identifier.
    ///Can be one of: "head" (canonical head in node's view), "genesis", "finalized", "justified", \<slot\>, \<hex encoded stateRoot with 0x prefix\>.
    ///- Example: `"head".to_string()`
    #[validate(length(min = 1u64))]
    pub state_id: String,
}
///Response types for getPendingConsolidations
#[derive(Debug, Clone)]
pub enum GetPendingConsolidationsResponse {
    ///200: Success
    Ok(GetPendingDepositsResponseResponse),
    ///200: Success
    OkBinary(Vec<u8>),
    ///400: Invalid request syntax.
    BadRequest(PendingConsolidation400Response),
    ///404: Not found
    NotFound(BlindedBlock400Response),
    ///406: Accepted media type is not supported.
    NotAcceptable(BlindedBlock406Response),
    ///500: Beacon node internal error.
    InternalServerError(BlindedBlock400Response),
    ///default: Unknown response
    Unknown,
}
///Returns pending deposits for state with given 'stateId'. Should return 400 if the state retrieved is prior to Electra.
#[derive(Debug, Clone, validator::Validate, oas3_gen_support::Default)]
pub struct GetPendingDepositsRequest {
    #[validate(nested)]
    pub path: GetPendingDepositsRequestPath,
}
#[bon::bon]
impl GetPendingDepositsRequest {
    ///Create a new request with the given parameters.
    #[builder]
    pub fn new(state_id: String) -> anyhow::Result<Self> {
        let request = Self {
            path: GetPendingDepositsRequestPath {
                state_id,
            },
        };
        request.validate()?;
        Ok(request)
    }
}
impl GetPendingDepositsRequest {
    ///Parse the HTTP response into the response enum.
    pub async fn parse_response(
        req: reqwest::Response,
    ) -> anyhow::Result<GetPendingDepositsResponse> {
        let status = req.status();
        if status.is_success() {
            let content_type_str = req
                .headers()
                .get(reqwest::header::CONTENT_TYPE)
                .and_then(|v| v.to_str().ok())
                .unwrap_or("application/json");
            if content_type_str.contains("json") {
                let data = oas3_gen_support::Diagnostics::<
                    GetPendingDepositsResponseResponse2,
                >::json_with_diagnostics(req)
                    .await?;
                return Ok(GetPendingDepositsResponse::Ok(data));
            }
            if content_type_str.starts_with("application/octet-stream")
                || content_type_str.starts_with("image/")
                || content_type_str.starts_with("audio/")
                || content_type_str.starts_with("video/")
            {
                let data = req.bytes().await?.to_vec();
                return Ok(GetPendingDepositsResponse::OkBinary(data));
            }
        }
        if status.as_u16() == 400u16 {
            let data = oas3_gen_support::Diagnostics::<
                PendingConsolidation400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetPendingDepositsResponse::BadRequest(data));
        }
        if status.as_u16() == 404u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetPendingDepositsResponse::NotFound(data));
        }
        if status.as_u16() == 406u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock406Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetPendingDepositsResponse::NotAcceptable(data));
        }
        if status.as_u16() == 500u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetPendingDepositsResponse::InternalServerError(data));
        }
        let _ = req.bytes().await?;
        return Ok(GetPendingDepositsResponse::Unknown);
    }
}
#[derive(Debug, Clone, PartialEq, validator::Validate, oas3_gen_support::Default)]
pub struct GetPendingDepositsRequestPath {
    ///State identifier.
    ///Can be one of: "head" (canonical head in node's view), "genesis", "finalized", "justified", \<slot\>, \<hex encoded stateRoot with 0x prefix\>.
    ///- Example: `"head".to_string()`
    #[validate(length(min = 1u64))]
    pub state_id: String,
}
///Response types for getPendingDeposits
#[derive(Debug, Clone)]
pub enum GetPendingDepositsResponse {
    ///200: Success
    Ok(GetPendingDepositsResponseResponse2),
    ///200: Success
    OkBinary(Vec<u8>),
    ///400: Invalid request syntax.
    BadRequest(PendingConsolidation400Response),
    ///404: Not found
    NotFound(BlindedBlock400Response),
    ///406: Accepted media type is not supported.
    NotAcceptable(BlindedBlock406Response),
    ///500: Beacon node internal error.
    InternalServerError(BlindedBlock400Response),
    ///default: Unknown response
    Unknown,
}
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct GetPendingDepositsResponseResponse {
    pub data: Vec<GetPendingDepositsResponseResponseDatum>,
    ///True if the response references an unverified execution payload. Optimistic information may be invalidated at a later time. If the field is not present, assume the False value.
    pub execution_optimistic: bool,
    ///True if the response references the finalized history of the chain, as determined by fork choice. If the field is not present, additional calls are necessary to compare the epoch of the requested information with the finalized checkpoint.
    pub finalized: bool,
    pub version: ConsensusVersion,
}
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct GetPendingDepositsResponseResponse2 {
    pub data: Vec<GetPendingDepositsResponseResponse2Datum>,
    ///True if the response references an unverified execution payload. Optimistic information may be invalidated at a later time. If the field is not present, assume the False value.
    pub execution_optimistic: bool,
    ///True if the response references the finalized history of the chain, as determined by fork choice. If the field is not present, additional calls are necessary to compare the epoch of the requested information with the finalized checkpoint.
    pub finalized: bool,
    pub version: ConsensusVersion,
}
///The [`PendingDeposit`](https://github.com/ethereum/consensus-specs/blob/v1.5.0/specs/electra/beacon-chain.md#pendingdeposit) object from the CL Electra spec.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct GetPendingDepositsResponseResponse2Datum {
    ///The value to be deposited (gwei).
    pub amount: String,
    ///The validator's BLS public key, uniquely identifying them. _48-bytes, hex encoded with 0x prefix, case insensitive._
    pub pubkey: String,
    pub signature: String,
    ///The slot at which the deposit request was processed.
    pub slot: String,
    ///The withdrawal credentials.
    pub withdrawal_credentials: String,
}
///The [`PendingConsolidation`](https://github.com/ethereum/consensus-specs/blob/v1.5.0/specs/electra/beacon-chain.md#pendingconsolidation) object from the CL Electra spec.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct GetPendingDepositsResponseResponseDatum {
    ///Index of validator to consolidate from.
    pub source_index: String,
    ///Index of validator to consolidate to.
    pub target_index: String,
}
///Returns pending partial withdrawals for state with given 'stateId'. Should return 400 if the state retrieved is prior to Electra.
#[derive(Debug, Clone, validator::Validate, oas3_gen_support::Default)]
pub struct GetPendingPartialWithdrawalsRequest {
    #[validate(nested)]
    pub path: GetPendingPartialWithdrawalsRequestPath,
}
#[bon::bon]
impl GetPendingPartialWithdrawalsRequest {
    ///Create a new request with the given parameters.
    #[builder]
    pub fn new(state_id: String) -> anyhow::Result<Self> {
        let request = Self {
            path: GetPendingPartialWithdrawalsRequestPath {
                state_id,
            },
        };
        request.validate()?;
        Ok(request)
    }
}
impl GetPendingPartialWithdrawalsRequest {
    ///Parse the HTTP response into the response enum.
    pub async fn parse_response(
        req: reqwest::Response,
    ) -> anyhow::Result<GetPendingPartialWithdrawalsResponse> {
        let status = req.status();
        if status.is_success() {
            let content_type_str = req
                .headers()
                .get(reqwest::header::CONTENT_TYPE)
                .and_then(|v| v.to_str().ok())
                .unwrap_or("application/json");
            if content_type_str.contains("json") {
                let data = oas3_gen_support::Diagnostics::<
                    GetPendingPartialWithdrawalsResponseResponse,
                >::json_with_diagnostics(req)
                    .await?;
                return Ok(GetPendingPartialWithdrawalsResponse::Ok(data));
            }
            if content_type_str.starts_with("application/octet-stream")
                || content_type_str.starts_with("image/")
                || content_type_str.starts_with("audio/")
                || content_type_str.starts_with("video/")
            {
                let data = req.bytes().await?.to_vec();
                return Ok(GetPendingPartialWithdrawalsResponse::OkBinary(data));
            }
        }
        if status.as_u16() == 400u16 {
            let data = oas3_gen_support::Diagnostics::<
                PendingConsolidation400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetPendingPartialWithdrawalsResponse::BadRequest(data));
        }
        if status.as_u16() == 404u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetPendingPartialWithdrawalsResponse::NotFound(data));
        }
        if status.as_u16() == 406u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock406Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetPendingPartialWithdrawalsResponse::NotAcceptable(data));
        }
        if status.as_u16() == 500u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetPendingPartialWithdrawalsResponse::InternalServerError(data));
        }
        let _ = req.bytes().await?;
        return Ok(GetPendingPartialWithdrawalsResponse::Unknown);
    }
}
#[derive(Debug, Clone, PartialEq, validator::Validate, oas3_gen_support::Default)]
pub struct GetPendingPartialWithdrawalsRequestPath {
    ///State identifier.
    ///Can be one of: "head" (canonical head in node's view), "genesis", "finalized", "justified", \<slot\>, \<hex encoded stateRoot with 0x prefix\>.
    ///- Example: `"head".to_string()`
    #[validate(length(min = 1u64))]
    pub state_id: String,
}
///Response types for getPendingPartialWithdrawals
#[derive(Debug, Clone)]
pub enum GetPendingPartialWithdrawalsResponse {
    ///200: Success
    Ok(GetPendingPartialWithdrawalsResponseResponse),
    ///200: Success
    OkBinary(Vec<u8>),
    ///400: Invalid request syntax.
    BadRequest(PendingConsolidation400Response),
    ///404: Not found
    NotFound(BlindedBlock400Response),
    ///406: Accepted media type is not supported.
    NotAcceptable(BlindedBlock406Response),
    ///500: Beacon node internal error.
    InternalServerError(BlindedBlock400Response),
    ///default: Unknown response
    Unknown,
}
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct GetPendingPartialWithdrawalsResponseResponse {
    pub data: Vec<GetPendingPartialWithdrawalsResponseResponseDatum>,
    ///True if the response references an unverified execution payload. Optimistic information may be invalidated at a later time. If the field is not present, assume the False value.
    pub execution_optimistic: bool,
    ///True if the response references the finalized history of the chain, as determined by fork choice. If the field is not present, additional calls are necessary to compare the epoch of the requested information with the finalized checkpoint.
    pub finalized: bool,
    pub version: ConsensusVersion,
}
///The [`PendingPartialWithdrawal`](https://github.com/ethereum/consensus-specs/blob/v1.5.0/specs/electra/beacon-chain.md#pendingpartialwithdrawal) object from the CL Electra spec.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct GetPendingPartialWithdrawalsResponseResponseDatum {
    ///The value to be withdrawn (gwei).
    pub amount: String,
    ///Index of validator in validator registry.
    pub validator_index: String,
    ///The epoch when the amount is withdrawable.
    pub withdrawable_epoch: String,
}
///Retrieves attestations known by the node but not necessarily incorporated into any block
#[derive(Debug, Clone, validator::Validate, oas3_gen_support::Default)]
pub struct GetPoolAttestationsV2Request {
    pub query: GetPoolAttestationsV2RequestQuery,
}
#[bon::bon]
impl GetPoolAttestationsV2Request {
    ///Create a new request with the given parameters.
    #[builder]
    pub fn new(
        slot: Option<String>,
        committee_index: Option<String>,
    ) -> anyhow::Result<Self> {
        let request = Self {
            query: GetPoolAttestationsV2RequestQuery {
                slot,
                committee_index,
            },
        };
        request.validate()?;
        Ok(request)
    }
}
impl GetPoolAttestationsV2Request {
    ///Parse the HTTP response into the response enum.
    pub async fn parse_response(
        req: reqwest::Response,
    ) -> anyhow::Result<GetPoolAttestationsV2Response> {
        let status = req.status();
        if status.is_success() {
            let data = oas3_gen_support::Diagnostics::<
                GetPoolAttestationsV2ResponseResponse,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetPoolAttestationsV2Response::Ok(data));
        }
        if status.as_u16() == 400u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetPoolAttestationsV2Response::BadRequest(data));
        }
        if status.as_u16() == 500u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetPoolAttestationsV2Response::InternalServerError(data));
        }
        let _ = req.bytes().await?;
        return Ok(GetPoolAttestationsV2Response::Unknown);
    }
}
#[serde_with::skip_serializing_none]
#[derive(Debug, Clone, PartialEq, Serialize, oas3_gen_support::Default)]
pub struct GetPoolAttestationsV2RequestQuery {
    pub slot: Option<String>,
    pub committee_index: Option<String>,
}
///Response types for getPoolAttestationsV2
#[derive(Debug, Clone)]
pub enum GetPoolAttestationsV2Response {
    ///200: Successful response
    Ok(GetPoolAttestationsV2ResponseResponse),
    ///400: The slot or committee index could not be parsed
    BadRequest(BlindedBlock400Response),
    ///500: Beacon node internal error.
    InternalServerError(BlindedBlock400Response),
    ///default: Unknown response
    Unknown,
}
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct GetPoolAttestationsV2ResponseResponse {
    pub data: GetBlockAttestationsV2ResponseResponseData,
    pub version: ConsensusVersion,
}
///Retrieves attester slashings known by the node but not necessarily incorporated into any block
#[derive(Debug, Clone, validator::Validate, oas3_gen_support::Default)]
pub struct GetPoolAttesterSlashingsV2Request {}
impl GetPoolAttesterSlashingsV2Request {
    ///Parse the HTTP response into the response enum.
    pub async fn parse_response(
        req: reqwest::Response,
    ) -> anyhow::Result<GetPoolAttesterSlashingsV2Response> {
        let status = req.status();
        if status.is_success() {
            let data = oas3_gen_support::Diagnostics::<
                GetPoolAttesterSlashingsV2ResponseResponse,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetPoolAttesterSlashingsV2Response::Ok(data));
        }
        if status.as_u16() == 500u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetPoolAttesterSlashingsV2Response::InternalServerError(data));
        }
        let _ = req.bytes().await?;
        return Ok(GetPoolAttesterSlashingsV2Response::Unknown);
    }
}
///Response types for getPoolAttesterSlashingsV2
#[derive(Debug, Clone)]
pub enum GetPoolAttesterSlashingsV2Response {
    ///200: Successful response
    Ok(GetPoolAttesterSlashingsV2ResponseResponse),
    ///500: Beacon node internal error.
    InternalServerError(BlindedBlock400Response),
    ///default: Unknown response
    Unknown,
}
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct GetPoolAttesterSlashingsV2ResponseResponse {
    pub data: GetPoolAttesterSlashingsV2ResponseResponseData,
    pub version: ConsensusVersion,
}
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
#[serde(untagged)]
pub enum GetPoolAttesterSlashingsV2ResponseResponseData {
    #[default]
    Array(Vec<GetPoolAttesterSlashingsV2ResponseResponseDataArray>),
    Array2(Vec<GetPoolAttesterSlashingsV2ResponseResponseDataArray2>),
}
///The [`AttesterSlashing`](https://github.com/ethereum/consensus-specs/blob/v1.5.0/specs/electra/beacon-chain.md#attesterslashing) object from the CL spec.
#[derive(
    Debug,
    Clone,
    PartialEq,
    Serialize,
    Deserialize,
    validator::Validate,
    oas3_gen_support::Default
)]
pub struct GetPoolAttesterSlashingsV2ResponseResponseDataArray {
    ///The [`IndexedAttestation`](https://github.com/ethereum/consensus-specs/blob/v1.5.0/specs/electra/beacon-chain.md#indexedattestation) object from the CL spec.
    #[validate(nested)]
    pub attestation_1: ElectraAttesterSlashingAttestation1,
    ///The [`IndexedAttestation`](https://github.com/ethereum/consensus-specs/blob/v1.5.0/specs/electra/beacon-chain.md#indexedattestation) object from the CL spec.
    #[validate(nested)]
    pub attestation_2: ElectraAttesterSlashingAttestation1,
}
///The [`AttesterSlashing`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/phase0/beacon-chain.md#attesterslashing) object from the CL spec.
#[derive(
    Debug,
    Clone,
    PartialEq,
    Serialize,
    Deserialize,
    validator::Validate,
    oas3_gen_support::Default
)]
pub struct GetPoolAttesterSlashingsV2ResponseResponseDataArray2 {
    ///The [`IndexedAttestation`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/phase0/beacon-chain.md#indexedattestation) object from the CL spec.
    #[validate(nested)]
    pub attestation_1: Phase0AttesterSlashingAttestation1,
    ///The [`IndexedAttestation`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/phase0/beacon-chain.md#indexedattestation) object from the CL spec.
    #[validate(nested)]
    pub attestation_2: Phase0AttesterSlashingAttestation1,
}
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct GetPoolBLSToExecutionChangesResponseResponse {
    pub data: Vec<GetPoolBLSToExecutionChangesResponseResponseDatum>,
}
///The [`SignedBLSToExecutionChange`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/capella/beacon-chain.md#signedblstoexecutionchange) object from the CL spec.
#[derive(
    Debug,
    Clone,
    PartialEq,
    Serialize,
    Deserialize,
    validator::Validate,
    oas3_gen_support::Default
)]
pub struct GetPoolBLSToExecutionChangesResponseResponseDatum {
    ///The [`BLSToExecutionChange`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/capella/beacon-chain.md#blstoexecutionchange) object from the CL spec.
    #[validate(nested)]
    pub message: CapellaSignedBlsToExecutionChangeMessage,
    #[validate(
        length(min = 1u64),
        regex(path = "REGEX_AGGREGATE_AND_PROOF_REQUEST_BODY_ARRAY_SIGNATURE")
    )]
    pub signature: String,
}
///Retrieves BLS to execution changes known by the node but not necessarily incorporated into any block
#[derive(Debug, Clone, validator::Validate, oas3_gen_support::Default)]
pub struct GetPoolBlstoExecutionChangesRequest {}
impl GetPoolBlstoExecutionChangesRequest {
    ///Parse the HTTP response into the response enum.
    pub async fn parse_response(
        req: reqwest::Response,
    ) -> anyhow::Result<GetPoolBlstoExecutionChangesResponse> {
        let status = req.status();
        if status.is_success() {
            let data = oas3_gen_support::Diagnostics::<
                GetPoolBLSToExecutionChangesResponseResponse,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetPoolBlstoExecutionChangesResponse::Ok(data));
        }
        if status.as_u16() == 500u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetPoolBlstoExecutionChangesResponse::InternalServerError(data));
        }
        let _ = req.bytes().await?;
        return Ok(GetPoolBlstoExecutionChangesResponse::Unknown);
    }
}
///Response types for getPoolBLSToExecutionChanges
#[derive(Debug, Clone)]
pub enum GetPoolBlstoExecutionChangesResponse {
    ///200: Successful response
    Ok(GetPoolBLSToExecutionChangesResponseResponse),
    ///500: Beacon node internal error.
    InternalServerError(BlindedBlock400Response),
    ///default: Unknown response
    Unknown,
}
///Retrieves proposer slashings known by the node but not necessarily incorporated into any block
#[derive(Debug, Clone, validator::Validate, oas3_gen_support::Default)]
pub struct GetPoolProposerSlashingsRequest {}
impl GetPoolProposerSlashingsRequest {
    ///Parse the HTTP response into the response enum.
    pub async fn parse_response(
        req: reqwest::Response,
    ) -> anyhow::Result<GetPoolProposerSlashingsResponse> {
        let status = req.status();
        if status.is_success() {
            let data = oas3_gen_support::Diagnostics::<
                GetPoolProposerSlashingsResponseResponse,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetPoolProposerSlashingsResponse::Ok(data));
        }
        if status.as_u16() == 500u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetPoolProposerSlashingsResponse::InternalServerError(data));
        }
        let _ = req.bytes().await?;
        return Ok(GetPoolProposerSlashingsResponse::Unknown);
    }
}
///Response types for getPoolProposerSlashings
#[derive(Debug, Clone)]
pub enum GetPoolProposerSlashingsResponse {
    ///200: Successful response
    Ok(GetPoolProposerSlashingsResponseResponse),
    ///500: Beacon node internal error.
    InternalServerError(BlindedBlock400Response),
    ///default: Unknown response
    Unknown,
}
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct GetPoolProposerSlashingsResponseResponse {
    pub data: Vec<GetPoolProposerSlashingsResponseResponseDatum>,
}
///The [`ProposerSlashing`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/phase0/beacon-chain.md#proposerslashing) object from the CL spec.
#[derive(
    Debug,
    Clone,
    PartialEq,
    Serialize,
    Deserialize,
    validator::Validate,
    oas3_gen_support::Default
)]
pub struct GetPoolProposerSlashingsResponseResponseDatum {
    ///The [`SignedBeaconBlockHeader`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/phase0/beacon-chain.md#signedbeaconblockheader) object envelope from the CL spec.
    #[validate(nested)]
    pub signed_header_1: Phase0ProposerSlashingSignedHeader1,
    ///The [`SignedBeaconBlockHeader`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/phase0/beacon-chain.md#signedbeaconblockheader) object envelope from the CL spec.
    #[validate(nested)]
    pub signed_header_2: Phase0ProposerSlashingSignedHeader1,
}
///Retrieves voluntary exits known by the node but not necessarily incorporated into any block
#[derive(Debug, Clone, validator::Validate, oas3_gen_support::Default)]
pub struct GetPoolVoluntaryExitsRequest {}
impl GetPoolVoluntaryExitsRequest {
    ///Parse the HTTP response into the response enum.
    pub async fn parse_response(
        req: reqwest::Response,
    ) -> anyhow::Result<GetPoolVoluntaryExitsResponse> {
        let status = req.status();
        if status.is_success() {
            let data = oas3_gen_support::Diagnostics::<
                GetPoolVoluntaryExitsResponseResponse,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetPoolVoluntaryExitsResponse::Ok(data));
        }
        if status.as_u16() == 500u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetPoolVoluntaryExitsResponse::InternalServerError(data));
        }
        let _ = req.bytes().await?;
        return Ok(GetPoolVoluntaryExitsResponse::Unknown);
    }
}
///Response types for getPoolVoluntaryExits
#[derive(Debug, Clone)]
pub enum GetPoolVoluntaryExitsResponse {
    ///200: Successful response
    Ok(GetPoolVoluntaryExitsResponseResponse),
    ///500: Beacon node internal error.
    InternalServerError(BlindedBlock400Response),
    ///default: Unknown response
    Unknown,
}
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct GetPoolVoluntaryExitsResponseResponse {
    pub data: Vec<GetPoolVoluntaryExitsResponseResponseDatum>,
}
///The [`SignedVoluntaryExit`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/phase0/beacon-chain.md#signedvoluntaryexit) object from the CL spec.
#[derive(
    Debug,
    Clone,
    PartialEq,
    Serialize,
    Deserialize,
    validator::Validate,
    oas3_gen_support::Default
)]
pub struct GetPoolVoluntaryExitsResponseResponseDatum {
    ///The [`VoluntaryExit`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/phase0/beacon-chain.md#voluntaryexit) object from the CL spec.
    #[validate(nested)]
    pub message: Phase0SignedVoluntaryExitMessage,
    #[validate(
        length(min = 1u64),
        regex(path = "REGEX_AGGREGATE_AND_PROOF_REQUEST_BODY_ARRAY_SIGNATURE")
    )]
    pub signature: String,
}
///Request beacon node to provide all validators that are scheduled to propose a block in the given epoch.
///Duties should only need to be checked once per epoch, however a chain reorganization could occur that results in a change of duties. For full safety, you should monitor head events and confirm the dependent root in this response matches:
///- event.current_duty_dependent_root when `compute_epoch_at_slot(event.slot) == epoch`
///- event.block otherwise
///
///The dependent_root value is `get_block_root_at_slot(state, compute_start_slot_at_epoch(epoch) - 1)` or the genesis block root in the case of underflow.
#[derive(Debug, Clone, validator::Validate, oas3_gen_support::Default)]
pub struct GetProposerDutiesRequest {
    #[validate(nested)]
    pub path: GetProposerDutiesRequestPath,
}
#[bon::bon]
impl GetProposerDutiesRequest {
    ///Create a new request with the given parameters.
    #[builder]
    pub fn new(epoch: String) -> anyhow::Result<Self> {
        let request = Self {
            path: GetProposerDutiesRequestPath {
                epoch,
            },
        };
        request.validate()?;
        Ok(request)
    }
}
impl GetProposerDutiesRequest {
    ///Parse the HTTP response into the response enum.
    pub async fn parse_response(
        req: reqwest::Response,
    ) -> anyhow::Result<GetProposerDutiesResponse> {
        let status = req.status();
        if status.is_success() {
            let data = oas3_gen_support::Diagnostics::<
                GetProposerDutiesResponseResponse,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetProposerDutiesResponse::Ok(data));
        }
        if status.as_u16() == 400u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetProposerDutiesResponse::BadRequest(data));
        }
        if status.as_u16() == 500u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetProposerDutiesResponse::InternalServerError(data));
        }
        if status.as_u16() == 503u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetProposerDutiesResponse::ServiceUnavailable(data));
        }
        let _ = req.bytes().await?;
        return Ok(GetProposerDutiesResponse::Unknown);
    }
}
#[derive(Debug, Clone, PartialEq, validator::Validate, oas3_gen_support::Default)]
pub struct GetProposerDutiesRequestPath {
    #[validate(length(min = 1u64))]
    pub epoch: String,
}
///Response types for getProposerDuties
#[derive(Debug, Clone)]
pub enum GetProposerDutiesResponse {
    ///200: Success response
    Ok(GetProposerDutiesResponseResponse),
    ///400: Invalid epoch
    BadRequest(BlindedBlock400Response),
    ///500: Beacon node internal error.
    InternalServerError(BlindedBlock400Response),
    ///503: Beacon node is currently syncing, try again later.
    ServiceUnavailable(BlindedBlock400Response),
    ///default: Unknown response
    Unknown,
}
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct GetProposerDutiesResponseResponse {
    pub data: Vec<GetProposerDutiesResponseResponseDatum>,
    ///The block root that this response is dependent on.
    pub dependent_root: String,
    ///True if the response references an unverified execution payload. Optimistic information may be invalidated at a later time. If the field is not present, assume the False value.
    pub execution_optimistic: bool,
}
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct GetProposerDutiesResponseResponseDatum {
    ///The validator's BLS public key, uniquely identifying them. _48-bytes, hex encoded with 0x prefix, case insensitive._
    pub pubkey: String,
    ///The slot at which the validator must propose block.
    pub slot: String,
    ///Index of validator in validator registry.
    pub validator_index: String,
}
///Returns proposer lookahead for state with given 'stateId'. Should return 400 if the state retrieved is prior to Fulu.
#[derive(Debug, Clone, validator::Validate, oas3_gen_support::Default)]
pub struct GetProposerLookaheadRequest {
    #[validate(nested)]
    pub path: GetProposerLookaheadRequestPath,
}
#[bon::bon]
impl GetProposerLookaheadRequest {
    ///Create a new request with the given parameters.
    #[builder]
    pub fn new(state_id: String) -> anyhow::Result<Self> {
        let request = Self {
            path: GetProposerLookaheadRequestPath {
                state_id,
            },
        };
        request.validate()?;
        Ok(request)
    }
}
impl GetProposerLookaheadRequest {
    ///Parse the HTTP response into the response enum.
    pub async fn parse_response(
        req: reqwest::Response,
    ) -> anyhow::Result<GetProposerLookaheadResponse> {
        let status = req.status();
        if status.is_success() {
            let content_type_str = req
                .headers()
                .get(reqwest::header::CONTENT_TYPE)
                .and_then(|v| v.to_str().ok())
                .unwrap_or("application/json");
            if content_type_str.contains("json") {
                let data = oas3_gen_support::Diagnostics::<
                    GetProposerLookaheadResponseResponse,
                >::json_with_diagnostics(req)
                    .await?;
                return Ok(GetProposerLookaheadResponse::Ok(data));
            }
            if content_type_str.starts_with("application/octet-stream")
                || content_type_str.starts_with("image/")
                || content_type_str.starts_with("audio/")
                || content_type_str.starts_with("video/")
            {
                let data = req.bytes().await?.to_vec();
                return Ok(GetProposerLookaheadResponse::OkBinary(data));
            }
        }
        if status.as_u16() == 400u16 {
            let data = oas3_gen_support::Diagnostics::<
                PendingConsolidation400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetProposerLookaheadResponse::BadRequest(data));
        }
        if status.as_u16() == 404u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetProposerLookaheadResponse::NotFound(data));
        }
        if status.as_u16() == 406u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock406Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetProposerLookaheadResponse::NotAcceptable(data));
        }
        if status.as_u16() == 500u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetProposerLookaheadResponse::InternalServerError(data));
        }
        let _ = req.bytes().await?;
        return Ok(GetProposerLookaheadResponse::Unknown);
    }
}
#[derive(Debug, Clone, PartialEq, validator::Validate, oas3_gen_support::Default)]
pub struct GetProposerLookaheadRequestPath {
    ///State identifier.
    ///Can be one of: "head" (canonical head in node's view), "genesis", "finalized", "justified", \<slot\>, \<hex encoded stateRoot with 0x prefix\>.
    ///- Example: `"head".to_string()`
    #[validate(length(min = 1u64))]
    pub state_id: String,
}
///Response types for getProposerLookahead
#[derive(Debug, Clone)]
pub enum GetProposerLookaheadResponse {
    ///200: Success
    Ok(GetProposerLookaheadResponseResponse),
    ///200: Success
    OkBinary(Vec<u8>),
    ///400: Invalid request syntax.
    BadRequest(PendingConsolidation400Response),
    ///404: Not found
    NotFound(BlindedBlock400Response),
    ///406: Accepted media type is not supported.
    NotAcceptable(BlindedBlock406Response),
    ///500: Beacon node internal error.
    InternalServerError(BlindedBlock400Response),
    ///default: Unknown response
    Unknown,
}
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct GetProposerLookaheadResponseResponse {
    pub data: Vec<String>,
    ///True if the response references an unverified execution payload. Optimistic information may be invalidated at a later time. If the field is not present, assume the False value.
    pub execution_optimistic: bool,
    ///True if the response references the finalized history of the chain, as determined by fork choice. If the field is not present, additional calls are necessary to compare the epoch of the requested information with the finalized checkpoint.
    pub finalized: bool,
    pub version: ConsensusVersion,
}
///Retrieve specification configuration used on this node.  The configuration should include:
///  - Constants for all hard forks known by the beacon node, for example the [phase 0](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/phase0/beacon-chain.md#constants) and [altair](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/altair/beacon-chain.md#constants) values
///  - Presets for all hard forks supplied to the beacon node, for example the [phase 0](https://github.com/ethereum/consensus-specs/blob/v1.3.0/presets/mainnet/phase0.yaml) and [altair](https://github.com/ethereum/consensus-specs/blob/v1.3.0/presets/mainnet/altair.yaml) values
///  - Configuration for the beacon node, for example the [mainnet](https://github.com/ethereum/consensus-specs/blob/v1.3.0/configs/mainnet.yaml) values
///
///Values are returned with following format:
///  - any value starting with 0x in the spec is returned as a hex string
///  - numeric values are returned as a quoted integer
///  - array values are returned as a JSON array
#[derive(Debug, Clone, validator::Validate, oas3_gen_support::Default)]
pub struct GetSpecRequest {}
impl GetSpecRequest {
    ///Parse the HTTP response into the response enum.
    pub async fn parse_response(
        req: reqwest::Response,
    ) -> anyhow::Result<GetSpecResponse> {
        let status = req.status();
        if status.is_success() {
            let data = oas3_gen_support::Diagnostics::<
                GetSpecResponseResponse,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetSpecResponse::Ok(data));
        }
        if status.as_u16() == 500u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetSpecResponse::InternalServerError(data));
        }
        let _ = req.bytes().await?;
        return Ok(GetSpecResponse::Unknown);
    }
}
///Response types for getSpec
#[derive(Debug, Clone)]
pub enum GetSpecResponse {
    ///200: Success
    Ok(GetSpecResponseResponse),
    ///500: Beacon node internal error.
    InternalServerError(BlindedBlock400Response),
    ///default: Unknown response
    Unknown,
}
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct GetSpecResponseResponse {
    ///Key value mapping of all constants, presets and configuration values for all known hard forks
    ///Values are returned with following format:
    ///  - any value starting with 0x in the spec is returned as a hex string
    ///  - numeric values are returned as a quoted integer
    pub data: serde_json::Value,
}
///Returns finality checkpoints for state with given 'stateId'.
///In case finality is not yet achieved, checkpoint should return epoch 0 and ZERO_HASH as root.
#[derive(Debug, Clone, validator::Validate, oas3_gen_support::Default)]
pub struct GetStateFinalityCheckpointsRequest {
    #[validate(nested)]
    pub path: GetStateFinalityCheckpointsRequestPath,
}
#[bon::bon]
impl GetStateFinalityCheckpointsRequest {
    ///Create a new request with the given parameters.
    #[builder]
    pub fn new(state_id: String) -> anyhow::Result<Self> {
        let request = Self {
            path: GetStateFinalityCheckpointsRequestPath {
                state_id,
            },
        };
        request.validate()?;
        Ok(request)
    }
}
impl GetStateFinalityCheckpointsRequest {
    ///Parse the HTTP response into the response enum.
    pub async fn parse_response(
        req: reqwest::Response,
    ) -> anyhow::Result<GetStateFinalityCheckpointsResponse> {
        let status = req.status();
        if status.is_success() {
            let data = oas3_gen_support::Diagnostics::<
                GetStateFinalityCheckpointsResponseResponse,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetStateFinalityCheckpointsResponse::Ok(data));
        }
        if status.as_u16() == 400u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetStateFinalityCheckpointsResponse::BadRequest(data));
        }
        if status.as_u16() == 404u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetStateFinalityCheckpointsResponse::NotFound(data));
        }
        if status.as_u16() == 500u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetStateFinalityCheckpointsResponse::InternalServerError(data));
        }
        let _ = req.bytes().await?;
        return Ok(GetStateFinalityCheckpointsResponse::Unknown);
    }
}
#[derive(Debug, Clone, PartialEq, validator::Validate, oas3_gen_support::Default)]
pub struct GetStateFinalityCheckpointsRequestPath {
    ///State identifier.
    ///Can be one of: "head" (canonical head in node's view), "genesis", "finalized", "justified", \<slot\>, \<hex encoded stateRoot with 0x prefix\>.
    ///- Example: `"head".to_string()`
    #[validate(length(min = 1u64))]
    pub state_id: String,
}
///Response types for getStateFinalityCheckpoints
#[derive(Debug, Clone)]
pub enum GetStateFinalityCheckpointsResponse {
    ///200: Success
    Ok(GetStateFinalityCheckpointsResponseResponse),
    ///400: Invalid state ID
    BadRequest(BlindedBlock400Response),
    ///404: State not found
    NotFound(BlindedBlock400Response),
    ///500: Beacon node internal error.
    InternalServerError(BlindedBlock400Response),
    ///default: Unknown response
    Unknown,
}
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct GetStateFinalityCheckpointsResponseResponse {
    pub data: GetStateFinalityCheckpointsResponseResponseData,
    ///True if the response references an unverified execution payload. Optimistic information may be invalidated at a later time. If the field is not present, assume the False value.
    pub execution_optimistic: bool,
    ///True if the response references the finalized history of the chain, as determined by fork choice. If the field is not present, additional calls are necessary to compare the epoch of the requested information with the finalized checkpoint.
    pub finalized: bool,
}
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct GetStateFinalityCheckpointsResponseResponseData {
    ///The [`Checkpoint`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/phase0/beacon-chain.md#checkpoint) object from the CL spec.
    pub current_justified: AltairBeaconStateCurrentJustifiedCheckpoint,
    ///The [`Checkpoint`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/phase0/beacon-chain.md#checkpoint) object from the CL spec.
    pub finalized: AltairBeaconStateCurrentJustifiedCheckpoint,
    ///The [`Checkpoint`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/phase0/beacon-chain.md#checkpoint) object from the CL spec.
    pub previous_justified: AltairBeaconStateCurrentJustifiedCheckpoint,
}
///Returns [Fork](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/phase0/beacon-chain.md#fork) object for state with given 'stateId'.
#[derive(Debug, Clone, validator::Validate, oas3_gen_support::Default)]
pub struct GetStateForkRequest {
    #[validate(nested)]
    pub path: GetStateForkRequestPath,
}
#[bon::bon]
impl GetStateForkRequest {
    ///Create a new request with the given parameters.
    #[builder]
    pub fn new(state_id: String) -> anyhow::Result<Self> {
        let request = Self {
            path: GetStateForkRequestPath {
                state_id,
            },
        };
        request.validate()?;
        Ok(request)
    }
}
impl GetStateForkRequest {
    ///Parse the HTTP response into the response enum.
    pub async fn parse_response(
        req: reqwest::Response,
    ) -> anyhow::Result<GetStateForkResponse> {
        let status = req.status();
        if status.is_success() {
            let data = oas3_gen_support::Diagnostics::<
                GetStateForkResponseResponse,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetStateForkResponse::Ok(data));
        }
        if status.as_u16() == 400u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetStateForkResponse::BadRequest(data));
        }
        if status.as_u16() == 404u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetStateForkResponse::NotFound(data));
        }
        if status.as_u16() == 500u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetStateForkResponse::InternalServerError(data));
        }
        let _ = req.bytes().await?;
        return Ok(GetStateForkResponse::Unknown);
    }
}
#[derive(Debug, Clone, PartialEq, validator::Validate, oas3_gen_support::Default)]
pub struct GetStateForkRequestPath {
    ///State identifier.
    ///Can be one of: "head" (canonical head in node's view), "genesis", "finalized", "justified", \<slot\>, \<hex encoded stateRoot with 0x prefix\>.
    ///- Example: `"head".to_string()`
    #[validate(length(min = 1u64))]
    pub state_id: String,
}
///Response types for getStateFork
#[derive(Debug, Clone)]
pub enum GetStateForkResponse {
    ///200: Success
    Ok(GetStateForkResponseResponse),
    ///400: Invalid state ID
    BadRequest(BlindedBlock400Response),
    ///404: State not found
    NotFound(BlindedBlock400Response),
    ///500: Beacon node internal error.
    InternalServerError(BlindedBlock400Response),
    ///default: Unknown response
    Unknown,
}
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct GetStateForkResponseResponse {
    ///The [`Fork`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/phase0/beacon-chain.md#fork) object from the CL spec.
    pub data: BeaconStateFork,
    ///True if the response references an unverified execution payload. Optimistic information may be invalidated at a later time. If the field is not present, assume the False value.
    pub execution_optimistic: bool,
    ///True if the response references the finalized history of the chain, as determined by fork choice. If the field is not present, additional calls are necessary to compare the epoch of the requested information with the finalized checkpoint.
    pub finalized: bool,
}
///Fetch the RANDAO mix for the requested epoch from the state identified by `state_id`.
///
///If an epoch is not specified then the RANDAO mix for the state's current epoch will be returned.
///
///By adjusting the `state_id` parameter you can query for any historic value of the RANDAO mix.
///Ordinarily states from the same epoch will mutate the RANDAO mix for that epoch as blocks are
///applied.
#[derive(Debug, Clone, validator::Validate, oas3_gen_support::Default)]
pub struct GetStateRandaoRequest {
    #[validate(nested)]
    pub path: GetStateRandaoRequestPath,
    pub query: GetStateRandaoRequestQuery,
}
#[bon::bon]
impl GetStateRandaoRequest {
    ///Create a new request with the given parameters.
    #[builder]
    pub fn new(state_id: String, epoch: Option<String>) -> anyhow::Result<Self> {
        let request = Self {
            path: GetStateRandaoRequestPath {
                state_id,
            },
            query: GetStateRandaoRequestQuery {
                epoch,
            },
        };
        request.validate()?;
        Ok(request)
    }
}
impl GetStateRandaoRequest {
    ///Parse the HTTP response into the response enum.
    pub async fn parse_response(
        req: reqwest::Response,
    ) -> anyhow::Result<GetStateRandaoResponse> {
        let status = req.status();
        if status.is_success() {
            let data = oas3_gen_support::Diagnostics::<
                GetStateRandaoResponseResponse,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetStateRandaoResponse::Ok(data));
        }
        if status.as_u16() == 400u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetStateRandaoResponse::BadRequest(data));
        }
        if status.as_u16() == 404u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetStateRandaoResponse::NotFound(data));
        }
        if status.as_u16() == 500u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetStateRandaoResponse::InternalServerError(data));
        }
        let _ = req.bytes().await?;
        return Ok(GetStateRandaoResponse::Unknown);
    }
}
#[derive(Debug, Clone, PartialEq, validator::Validate, oas3_gen_support::Default)]
pub struct GetStateRandaoRequestPath {
    ///State identifier.
    ///Can be one of: "head" (canonical head in node's view), "genesis", "finalized", "justified", \<slot\>, \<hex encoded stateRoot with 0x prefix\>.
    ///- Example: `"head".to_string()`
    #[validate(length(min = 1u64))]
    pub state_id: String,
}
#[serde_with::skip_serializing_none]
#[derive(Debug, Clone, PartialEq, Serialize, oas3_gen_support::Default)]
pub struct GetStateRandaoRequestQuery {
    ///Epoch to fetch the RANDAO mix for. Default: state's current epoch.
    pub epoch: Option<String>,
}
///Response types for getStateRandao
#[derive(Debug, Clone)]
pub enum GetStateRandaoResponse {
    ///200: Success
    Ok(GetStateRandaoResponseResponse),
    ///400: Invalid state ID or epoch
    BadRequest(BlindedBlock400Response),
    ///404: State not found
    NotFound(BlindedBlock400Response),
    ///500: Beacon node internal error.
    InternalServerError(BlindedBlock400Response),
    ///default: Unknown response
    Unknown,
}
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct GetStateRandaoResponseResponse {
    pub data: GetStateRandaoResponseResponseData,
    ///True if the response references an unverified execution payload. Optimistic information may be invalidated at a later time. If the field is not present, assume the False value.
    pub execution_optimistic: bool,
    ///True if the response references the finalized history of the chain, as determined by fork choice. If the field is not present, additional calls are necessary to compare the epoch of the requested information with the finalized checkpoint.
    pub finalized: bool,
}
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct GetStateRandaoResponseResponseData {
    ///RANDAO mix for requested epoch in state.
    pub randao: String,
}
///Calculates HashTreeRoot for state with given 'stateId'. If stateId is root, same value will be returned.
#[derive(Debug, Clone, validator::Validate, oas3_gen_support::Default)]
pub struct GetStateRootRequest {
    #[validate(nested)]
    pub path: GetStateRootRequestPath,
}
#[bon::bon]
impl GetStateRootRequest {
    ///Create a new request with the given parameters.
    #[builder]
    pub fn new(state_id: String) -> anyhow::Result<Self> {
        let request = Self {
            path: GetStateRootRequestPath {
                state_id,
            },
        };
        request.validate()?;
        Ok(request)
    }
}
impl GetStateRootRequest {
    ///Parse the HTTP response into the response enum.
    pub async fn parse_response(
        req: reqwest::Response,
    ) -> anyhow::Result<GetStateRootResponse> {
        let status = req.status();
        if status.is_success() {
            let data = oas3_gen_support::Diagnostics::<
                GetStateRootResponseResponse,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetStateRootResponse::Ok(data));
        }
        if status.as_u16() == 400u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetStateRootResponse::BadRequest(data));
        }
        if status.as_u16() == 404u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetStateRootResponse::NotFound(data));
        }
        if status.as_u16() == 500u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetStateRootResponse::InternalServerError(data));
        }
        let _ = req.bytes().await?;
        return Ok(GetStateRootResponse::Unknown);
    }
}
#[derive(Debug, Clone, PartialEq, validator::Validate, oas3_gen_support::Default)]
pub struct GetStateRootRequestPath {
    ///State identifier.
    ///Can be one of: "head" (canonical head in node's view), "genesis", "finalized", "justified", \<slot\>, \<hex encoded stateRoot with 0x prefix\>.
    ///- Example: `"head".to_string()`
    #[validate(length(min = 1u64))]
    pub state_id: String,
}
///Response types for getStateRoot
#[derive(Debug, Clone)]
pub enum GetStateRootResponse {
    ///200: Success
    Ok(GetStateRootResponseResponse),
    ///400: Invalid state ID
    BadRequest(BlindedBlock400Response),
    ///404: State not found
    NotFound(BlindedBlock400Response),
    ///500: Beacon node internal error.
    InternalServerError(BlindedBlock400Response),
    ///default: Unknown response
    Unknown,
}
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct GetStateRootResponseResponse {
    pub data: GetStateRootResponseResponseData,
    ///True if the response references an unverified execution payload. Optimistic information may be invalidated at a later time. If the field is not present, assume the False value.
    pub execution_optimistic: bool,
    ///True if the response references the finalized history of the chain, as determined by fork choice. If the field is not present, additional calls are necessary to compare the epoch of the requested information with the finalized checkpoint.
    pub finalized: bool,
}
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct GetStateRootResponseResponseData {
    ///HashTreeRoot of BeaconState object
    pub root: String,
}
///Returns full BeaconState object for given stateId.
///Depending on `Accept` header it can be returned either as json or as bytes serialized by SSZ
#[derive(Debug, Clone, validator::Validate, oas3_gen_support::Default)]
pub struct GetStateV2Request {
    #[validate(nested)]
    pub path: GetStateV2RequestPath,
}
#[bon::bon]
impl GetStateV2Request {
    ///Create a new request with the given parameters.
    #[builder]
    pub fn new(state_id: String) -> anyhow::Result<Self> {
        let request = Self {
            path: GetStateV2RequestPath { state_id },
        };
        request.validate()?;
        Ok(request)
    }
}
impl GetStateV2Request {
    ///Parse the HTTP response into the response enum.
    pub async fn parse_response(
        req: reqwest::Response,
    ) -> anyhow::Result<GetStateV2Response> {
        let status = req.status();
        if status.is_success() {
            let content_type_str = req
                .headers()
                .get(reqwest::header::CONTENT_TYPE)
                .and_then(|v| v.to_str().ok())
                .unwrap_or("application/json");
            if content_type_str.contains("json") {
                let data = oas3_gen_support::Diagnostics::<
                    GetStateV2ResponseResponse,
                >::json_with_diagnostics(req)
                    .await?;
                return Ok(GetStateV2Response::Ok(data));
            }
            if content_type_str.starts_with("application/octet-stream")
                || content_type_str.starts_with("image/")
                || content_type_str.starts_with("audio/")
                || content_type_str.starts_with("video/")
            {
                let data = req.bytes().await?.to_vec();
                return Ok(GetStateV2Response::OkBinary(data));
            }
        }
        if status.as_u16() == 400u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetStateV2Response::BadRequest(data));
        }
        if status.as_u16() == 404u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetStateV2Response::NotFound(data));
        }
        if status.as_u16() == 406u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock406Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetStateV2Response::NotAcceptable(data));
        }
        if status.as_u16() == 500u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetStateV2Response::InternalServerError(data));
        }
        let _ = req.bytes().await?;
        return Ok(GetStateV2Response::Unknown);
    }
}
#[derive(Debug, Clone, PartialEq, validator::Validate, oas3_gen_support::Default)]
pub struct GetStateV2RequestPath {
    ///State identifier.
    ///Can be one of: "head" (canonical head in node's view), "genesis", "finalized", "justified", \<slot\>, \<hex encoded stateRoot with 0x prefix\>.
    ///- Example: `"head".to_string()`
    #[validate(length(min = 1u64))]
    pub state_id: String,
}
///Response types for getStateV2
#[derive(Debug, Clone)]
pub enum GetStateV2Response {
    ///200: Success
    Ok(GetStateV2ResponseResponse),
    ///200: Success
    OkBinary(Vec<u8>),
    ///400: Invalid state ID
    BadRequest(BlindedBlock400Response),
    ///404: State not found
    NotFound(BlindedBlock400Response),
    ///406: Accepted media type is not supported.
    NotAcceptable(BlindedBlock406Response),
    ///500: Beacon node internal error.
    InternalServerError(BlindedBlock400Response),
    ///default: Unknown response
    Unknown,
}
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct GetStateV2ResponseResponse {
    pub data: GetStateV2ResponseResponseData,
    ///True if the response references an unverified execution payload. Optimistic information may be invalidated at a later time. If the field is not present, assume the False value.
    pub execution_optimistic: bool,
    ///True if the response references the finalized history of the chain, as determined by fork choice. If the field is not present, additional calls are necessary to compare the epoch of the requested information with the finalized checkpoint.
    pub finalized: bool,
    pub version: ConsensusVersion,
}
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
#[serde(untagged)]
pub enum GetStateV2ResponseResponseData {
    ///The [`BeaconState`](https://github.com/ethereum/consensus-specs/blob/v1.5.0/specs/electra/beacon-chain.md#beaconstate) object from the CL Electra spec.
    #[default]
    Object(GetStateV2ResponseResponseDataObject),
    ///The [`BeaconState`](https://github.com/ethereum/consensus-specs/blob/v1.5.0/specs/deneb/beacon-chain.md#beaconstate) object from the Eth2.0 Deneb spec.
    Object2(GetStateV2ResponseResponseDataObject2),
    ///The [`BeaconState`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/capella/beacon-chain.md#beaconstate) object from the Eth2.0 Capella spec.
    Object3(GetStateV2ResponseResponseDataObject3),
    ///The [`BeaconState`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/bellatrix/beacon-chain.md#beaconstate) object from the Eth2.0 Bellatrix spec.
    Object4(GetStateV2ResponseResponseDataObject4),
    ///The [`BeaconState`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/altair/beacon-chain.md#beaconstate) object from the CL Altair spec.
    Object5(GetStateV2ResponseResponseDataObject5),
    ///The [`BeaconState`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/phase0/beacon-chain.md#beacon-state) object from the CL spec.
    Object6(GetStateV2ResponseResponseDataObject6),
}
///The [`BeaconState`](https://github.com/ethereum/consensus-specs/blob/v1.5.0/specs/electra/beacon-chain.md#beaconstate) object from the CL Electra spec.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct GetStateV2ResponseResponseDataObject {
    ///Validator balances in gwei. Variable length list, maximum 1099511627776 items
    pub balances: Vec<String>,
    ///Fixed length of 8192 items
    pub block_roots: Vec<String>,
    pub consolidation_balance_to_consume: String,
    pub current_epoch_participation: Vec<String>,
    ///The [`Checkpoint`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/phase0/beacon-chain.md#checkpoint) object from the CL spec.
    pub current_justified_checkpoint: AltairBeaconStateCurrentJustifiedCheckpoint,
    pub current_sync_committee: AltairBeaconStateCurrentSyncCommittee,
    pub deposit_balance_to_consume: String,
    pub deposit_requests_start_index: String,
    pub earliest_consolidation_epoch: String,
    pub earliest_exit_epoch: String,
    ///The [`Eth1Data`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/phase0/beacon-chain.md#eth1data) object from the CL spec.
    pub eth1_data: Eth1Data,
    ///Fixed length of 1024 items
    pub eth1_data_votes: Vec<Eth1Data>,
    pub eth1_deposit_index: String,
    pub exit_balance_to_consume: String,
    ///The [`Checkpoint`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/phase0/beacon-chain.md#checkpoint) object from the CL spec.
    pub finalized_checkpoint: AltairBeaconStateCurrentJustifiedCheckpoint,
    ///The [`Fork`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/phase0/beacon-chain.md#fork) object from the CL spec.
    pub fork: BeaconStateFork,
    pub genesis_time: String,
    pub genesis_validators_root: String,
    ///Variable length list, maximum 16777216 items. Frozen in Capella, replaced by historical_summaries.
    pub historical_roots: Vec<String>,
    ///Variable length list, maximum 16777216 items
    pub historical_summaries: Vec<GetStateV2ResponseResponseDataObjectHistoricalSummary>,
    ///Per-validator inactivity scores. Introduced in Altair. Variable length list, maximum 1099511627776 items
    pub inactivity_scores: Vec<String>,
    ///Bit set for every recent justified epoch
    pub justification_bits: String,
    ///The [`BeaconBlockHeader`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/phase0/beacon-chain.md#beaconblockheader) object from the CL spec.
    pub latest_block_header: serde_json::Value,
    ///The [`ExecutionPayloadHeader`](https://github.com/ethereum/consensus-specs/blob/v1.4.0/specs/deneb/beacon-chain.md#executionpayloadheader) object from the CL Deneb spec.
    pub latest_execution_payload_header: serde_json::Value,
    pub next_sync_committee: AltairBeaconStateCurrentSyncCommittee,
    pub next_withdrawal_index: String,
    pub next_withdrawal_validator_index: String,
    pub pending_consolidations: Vec<GetPendingDepositsResponseResponseDatum>,
    pub pending_deposits: Vec<GetPendingDepositsResponseResponse2Datum>,
    pub pending_partial_withdrawals: Vec<
        GetPendingPartialWithdrawalsResponseResponseDatum,
    >,
    pub previous_epoch_participation: Vec<String>,
    ///The [`Checkpoint`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/phase0/beacon-chain.md#checkpoint) object from the CL spec.
    pub previous_justified_checkpoint: AltairBeaconStateCurrentJustifiedCheckpoint,
    ///Fixed length of 65536 items
    pub randao_mixes: Vec<String>,
    ///Per-epoch sums of slashed effective balances. Fixed length of 8192 items
    pub slashings: Vec<String>,
    pub slot: String,
    ///Fixed length of 8192 items
    pub state_roots: Vec<String>,
    ///Variable length list, maximum 1099511627776 items
    pub validators: Vec<ValidatorResponseValidator>,
}
///The [`BeaconState`](https://github.com/ethereum/consensus-specs/blob/v1.5.0/specs/deneb/beacon-chain.md#beaconstate) object from the Eth2.0 Deneb spec.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct GetStateV2ResponseResponseDataObject2 {
    ///Validator balances in gwei. Variable length list, maximum 1099511627776 items
    pub balances: Vec<String>,
    ///Fixed length of 8192 items
    pub block_roots: Vec<String>,
    pub current_epoch_participation: Vec<String>,
    ///The [`Checkpoint`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/phase0/beacon-chain.md#checkpoint) object from the CL spec.
    pub current_justified_checkpoint: AltairBeaconStateCurrentJustifiedCheckpoint,
    pub current_sync_committee: AltairBeaconStateCurrentSyncCommittee,
    ///The [`Eth1Data`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/phase0/beacon-chain.md#eth1data) object from the CL spec.
    pub eth1_data: Eth1Data,
    ///Fixed length of 1024 items
    pub eth1_data_votes: Vec<Eth1Data>,
    pub eth1_deposit_index: String,
    ///The [`Checkpoint`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/phase0/beacon-chain.md#checkpoint) object from the CL spec.
    pub finalized_checkpoint: AltairBeaconStateCurrentJustifiedCheckpoint,
    ///The [`Fork`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/phase0/beacon-chain.md#fork) object from the CL spec.
    pub fork: BeaconStateFork,
    pub genesis_time: String,
    pub genesis_validators_root: String,
    ///Variable length list, maximum 16777216 items. Frozen in Capella, replaced by historical_summaries.
    pub historical_roots: Vec<String>,
    ///Variable length list, maximum 16777216 items
    pub historical_summaries: Vec<GetStateV2ResponseResponseDataObjectHistoricalSummary>,
    ///Per-validator inactivity scores. Introduced in Altair. Variable length list, maximum 1099511627776 items
    pub inactivity_scores: Vec<String>,
    ///Bit set for every recent justified epoch
    pub justification_bits: String,
    ///The [`BeaconBlockHeader`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/phase0/beacon-chain.md#beaconblockheader) object from the CL spec.
    pub latest_block_header: serde_json::Value,
    ///The [`ExecutionPayloadHeader`](https://github.com/ethereum/consensus-specs/blob/v1.4.0/specs/deneb/beacon-chain.md#executionpayloadheader) object from the CL Deneb spec.
    pub latest_execution_payload_header: serde_json::Value,
    pub next_sync_committee: AltairBeaconStateCurrentSyncCommittee,
    pub next_withdrawal_index: String,
    pub next_withdrawal_validator_index: String,
    pub previous_epoch_participation: Vec<String>,
    ///The [`Checkpoint`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/phase0/beacon-chain.md#checkpoint) object from the CL spec.
    pub previous_justified_checkpoint: AltairBeaconStateCurrentJustifiedCheckpoint,
    ///Fixed length of 65536 items
    pub randao_mixes: Vec<String>,
    ///Per-epoch sums of slashed effective balances. Fixed length of 8192 items
    pub slashings: Vec<String>,
    pub slot: String,
    ///Fixed length of 8192 items
    pub state_roots: Vec<String>,
    ///Variable length list, maximum 1099511627776 items
    pub validators: Vec<ValidatorResponseValidator>,
}
///The [`BeaconState`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/capella/beacon-chain.md#beaconstate) object from the Eth2.0 Capella spec.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct GetStateV2ResponseResponseDataObject3 {
    ///Validator balances in gwei. Variable length list, maximum 1099511627776 items
    pub balances: Vec<String>,
    ///Fixed length of 8192 items
    pub block_roots: Vec<String>,
    pub current_epoch_participation: Vec<String>,
    ///The [`Checkpoint`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/phase0/beacon-chain.md#checkpoint) object from the CL spec.
    pub current_justified_checkpoint: AltairBeaconStateCurrentJustifiedCheckpoint,
    pub current_sync_committee: AltairBeaconStateCurrentSyncCommittee,
    ///The [`Eth1Data`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/phase0/beacon-chain.md#eth1data) object from the CL spec.
    pub eth1_data: Eth1Data,
    ///Fixed length of 1024 items
    pub eth1_data_votes: Vec<Eth1Data>,
    pub eth1_deposit_index: String,
    ///The [`Checkpoint`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/phase0/beacon-chain.md#checkpoint) object from the CL spec.
    pub finalized_checkpoint: AltairBeaconStateCurrentJustifiedCheckpoint,
    ///The [`Fork`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/phase0/beacon-chain.md#fork) object from the CL spec.
    pub fork: BeaconStateFork,
    pub genesis_time: String,
    pub genesis_validators_root: String,
    ///Variable length list, maximum 16777216 items. Frozen in Capella, replaced by historical_summaries.
    pub historical_roots: Vec<String>,
    ///Variable length list, maximum 16777216 items
    pub historical_summaries: Vec<GetStateV2ResponseResponseDataObjectHistoricalSummary>,
    ///Per-validator inactivity scores. Introduced in Altair. Variable length list, maximum 1099511627776 items
    pub inactivity_scores: Vec<String>,
    ///Bit set for every recent justified epoch
    pub justification_bits: String,
    ///The [`BeaconBlockHeader`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/phase0/beacon-chain.md#beaconblockheader) object from the CL spec.
    pub latest_block_header: serde_json::Value,
    ///The [`ExecutionPayloadHeader`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/capella/beacon-chain.md#executionpayloadheader) object from the CL Capella spec.
    pub latest_execution_payload_header: serde_json::Value,
    pub next_sync_committee: AltairBeaconStateCurrentSyncCommittee,
    pub next_withdrawal_index: String,
    pub next_withdrawal_validator_index: String,
    pub previous_epoch_participation: Vec<String>,
    ///The [`Checkpoint`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/phase0/beacon-chain.md#checkpoint) object from the CL spec.
    pub previous_justified_checkpoint: AltairBeaconStateCurrentJustifiedCheckpoint,
    ///Fixed length of 65536 items
    pub randao_mixes: Vec<String>,
    ///Per-epoch sums of slashed effective balances. Fixed length of 8192 items
    pub slashings: Vec<String>,
    pub slot: String,
    ///Fixed length of 8192 items
    pub state_roots: Vec<String>,
    ///Variable length list, maximum 1099511627776 items
    pub validators: Vec<ValidatorResponseValidator>,
}
///The [`BeaconState`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/bellatrix/beacon-chain.md#beaconstate) object from the Eth2.0 Bellatrix spec.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct GetStateV2ResponseResponseDataObject4 {
    ///Validator balances in Gwei. Variable length list, maximum 1099511627776 items
    pub balances: Vec<String>,
    ///Fixed length of 8192 items
    pub block_roots: Vec<String>,
    pub current_epoch_participation: Vec<String>,
    ///The [`Checkpoint`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/phase0/beacon-chain.md#checkpoint) object from the CL spec.
    pub current_justified_checkpoint: AltairBeaconStateCurrentJustifiedCheckpoint,
    pub current_sync_committee: AltairBeaconStateCurrentSyncCommittee,
    ///The [`Eth1Data`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/phase0/beacon-chain.md#eth1data) object from the CL spec.
    pub eth1_data: Eth1Data,
    ///Fixed length of 1024 items
    pub eth1_data_votes: Vec<Eth1Data>,
    pub eth1_deposit_index: String,
    ///The [`Checkpoint`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/phase0/beacon-chain.md#checkpoint) object from the CL spec.
    pub finalized_checkpoint: AltairBeaconStateCurrentJustifiedCheckpoint,
    ///The [`Fork`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/phase0/beacon-chain.md#fork) object from the CL spec.
    pub fork: BeaconStateFork,
    pub genesis_time: String,
    pub genesis_validators_root: String,
    ///Variable length list, maximum 16777216 items
    pub historical_roots: Vec<String>,
    ///Per-validator inactivity scores. Introduced in Altair. Variable length list, maximum 1099511627776 items
    pub inactivity_scores: Vec<String>,
    ///Bit set for every recent justified epoch
    pub justification_bits: String,
    ///The [`BeaconBlockHeader`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/phase0/beacon-chain.md#beaconblockheader) object from the CL spec.
    pub latest_block_header: serde_json::Value,
    ///The [`ExecutionPayloadHeader`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/bellatrix/beacon-chain.md#executionpayloadheader) object from the CL Bellatrix spec.
    pub latest_execution_payload_header: serde_json::Value,
    pub next_sync_committee: AltairBeaconStateCurrentSyncCommittee,
    pub previous_epoch_participation: Vec<String>,
    ///The [`Checkpoint`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/phase0/beacon-chain.md#checkpoint) object from the CL spec.
    pub previous_justified_checkpoint: AltairBeaconStateCurrentJustifiedCheckpoint,
    ///Fixed length of 65536 items
    pub randao_mixes: Vec<String>,
    ///Per-epoch sums of slashed effective balances. Fixed length of 8192 items
    pub slashings: Vec<String>,
    pub slot: String,
    ///Fixed length of 8192 items
    pub state_roots: Vec<String>,
    ///Variable length list, maximum 1099511627776 items
    pub validators: Vec<ValidatorResponseValidator>,
}
///The [`BeaconState`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/altair/beacon-chain.md#beaconstate) object from the CL Altair spec.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct GetStateV2ResponseResponseDataObject5 {
    ///Validator balances in gwei. Variable length list, maximum 1099511627776 items
    pub balances: Vec<String>,
    ///Fixed length of 8192 items
    pub block_roots: Vec<String>,
    pub current_epoch_participation: Vec<String>,
    ///The [`Checkpoint`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/phase0/beacon-chain.md#checkpoint) object from the CL spec.
    pub current_justified_checkpoint: AltairBeaconStateCurrentJustifiedCheckpoint,
    pub current_sync_committee: AltairBeaconStateCurrentSyncCommittee,
    ///The [`Eth1Data`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/phase0/beacon-chain.md#eth1data) object from the CL spec.
    pub eth1_data: Eth1Data,
    ///Fixed length of 1024 items
    pub eth1_data_votes: Vec<Eth1Data>,
    pub eth1_deposit_index: String,
    ///The [`Checkpoint`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/phase0/beacon-chain.md#checkpoint) object from the CL spec.
    pub finalized_checkpoint: AltairBeaconStateCurrentJustifiedCheckpoint,
    ///The [`Fork`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/phase0/beacon-chain.md#fork) object from the CL spec.
    pub fork: BeaconStateFork,
    pub genesis_time: String,
    pub genesis_validators_root: String,
    ///Variable length list, maximum 16777216 items
    pub historical_roots: Vec<String>,
    ///Per-validator inactivity scores. New in Altair. Variable length list, maximum 1099511627776 items
    pub inactivity_scores: Vec<String>,
    ///Bit set for every recent justified epoch
    pub justification_bits: String,
    ///The [`BeaconBlockHeader`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/phase0/beacon-chain.md#beaconblockheader) object from the CL spec.
    pub latest_block_header: serde_json::Value,
    pub next_sync_committee: AltairBeaconStateCurrentSyncCommittee,
    pub previous_epoch_participation: Vec<String>,
    ///The [`Checkpoint`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/phase0/beacon-chain.md#checkpoint) object from the CL spec.
    pub previous_justified_checkpoint: AltairBeaconStateCurrentJustifiedCheckpoint,
    ///Fixed length of 65536 items
    pub randao_mixes: Vec<String>,
    ///Per-epoch sums of slashed effective balances. Fixed length of 8192 items
    pub slashings: Vec<String>,
    pub slot: String,
    ///Fixed length of 8192 items
    pub state_roots: Vec<String>,
    ///Variable length list, maximum 1099511627776 items
    pub validators: Vec<ValidatorResponseValidator>,
}
///The [`BeaconState`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/phase0/beacon-chain.md#beacon-state) object from the CL spec.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct GetStateV2ResponseResponseDataObject6 {
    ///Validator balances in gwei. Variable length list, maximum 1099511627776 items
    pub balances: Vec<String>,
    ///Fixed length of 8192 items
    pub block_roots: Vec<String>,
    pub current_epoch_attestations: Vec<
        GetStateV2ResponseResponseDataObject6CurrentEpochAttestation,
    >,
    ///The [`Checkpoint`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/phase0/beacon-chain.md#checkpoint) object from the CL spec.
    pub current_justified_checkpoint: AltairBeaconStateCurrentJustifiedCheckpoint,
    ///The [`Eth1Data`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/phase0/beacon-chain.md#eth1data) object from the CL spec.
    pub eth1_data: Eth1Data,
    ///Fixed length of 1024 items
    pub eth1_data_votes: Vec<Eth1Data>,
    pub eth1_deposit_index: String,
    ///The [`Checkpoint`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/phase0/beacon-chain.md#checkpoint) object from the CL spec.
    pub finalized_checkpoint: AltairBeaconStateCurrentJustifiedCheckpoint,
    ///The [`Fork`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/phase0/beacon-chain.md#fork) object from the CL spec.
    pub fork: BeaconStateFork,
    pub genesis_time: String,
    pub genesis_validators_root: String,
    ///Variable length list, maximum 16777216 items
    pub historical_roots: Vec<String>,
    ///Bit set for every recent justified epoch
    pub justification_bits: String,
    ///The [`BeaconBlockHeader`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/phase0/beacon-chain.md#beaconblockheader) object from the CL spec.
    pub latest_block_header: serde_json::Value,
    pub previous_epoch_attestations: Vec<
        GetStateV2ResponseResponseDataObject6CurrentEpochAttestation,
    >,
    ///The [`Checkpoint`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/phase0/beacon-chain.md#checkpoint) object from the CL spec.
    pub previous_justified_checkpoint: AltairBeaconStateCurrentJustifiedCheckpoint,
    ///Fixed length of 65536 items
    pub randao_mixes: Vec<String>,
    ///Per-epoch sums of slashed effective balances. Fixed length of 8192 items
    pub slashings: Vec<String>,
    pub slot: String,
    ///Fixed length of 8192 items
    pub state_roots: Vec<String>,
    ///Variable length list, maximum 1099511627776 items
    pub validators: Vec<ValidatorResponseValidator>,
}
///The [`PendingAttestation`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/phase0/beacon-chain.md#pendingattestation) object from the CL spec.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct GetStateV2ResponseResponseDataObject6CurrentEpochAttestation {
    ///Attester aggregation bits.
    pub aggregation_bits: String,
    ///The [`AttestationData`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/phase0/beacon-chain.md#attestationdata) object from the CL spec.
    pub data: Data,
    pub inclusion_delay: String,
    pub proposer_index: String,
}
///The [`HistoricalSummary`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/capella/beacon-chain.md#historicalsummary) object from the CL Capella spec.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct GetStateV2ResponseResponseDataObjectHistoricalSummary {
    pub block_summary_root: String,
    pub state_summary_root: String,
}
///Returns filterable list of validators balances.
///
///Balances will be returned for all indices or public key that match known validators. If an index or public key does not
///match any known validator, no balance will be returned but this will not cause an error. There are no guarantees for the
///returned data in terms of ordering; the index is returned for each balance, and can be used to confirm for which inputs a
///response has been returned.
#[derive(Debug, Clone, validator::Validate, oas3_gen_support::Default)]
pub struct GetStateValidatorBalancesRequest {
    #[validate(nested)]
    pub path: GetStateValidatorBalancesRequestPath,
    #[validate(nested)]
    pub query: GetStateValidatorBalancesRequestQuery,
}
#[bon::bon]
impl GetStateValidatorBalancesRequest {
    ///Create a new request with the given parameters.
    #[builder]
    pub fn new(state_id: String, id: Option<Vec<String>>) -> anyhow::Result<Self> {
        let request = Self {
            path: GetStateValidatorBalancesRequestPath {
                state_id,
            },
            query: GetStateValidatorBalancesRequestQuery {
                id,
            },
        };
        request.validate()?;
        Ok(request)
    }
}
impl GetStateValidatorBalancesRequest {
    ///Parse the HTTP response into the response enum.
    pub async fn parse_response(
        req: reqwest::Response,
    ) -> anyhow::Result<GetStateValidatorBalancesResponse> {
        let status = req.status();
        if status.is_success() {
            let data = oas3_gen_support::Diagnostics::<
                GetStateValidatorBalancesResponseResponse,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetStateValidatorBalancesResponse::Ok(data));
        }
        if status.as_u16() == 400u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetStateValidatorBalancesResponse::BadRequest(data));
        }
        if status.as_u16() == 404u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetStateValidatorBalancesResponse::NotFound(data));
        }
        if status.as_u16() == 414u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetStateValidatorBalancesResponse::UriTooLong(data));
        }
        if status.as_u16() == 500u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetStateValidatorBalancesResponse::InternalServerError(data));
        }
        let _ = req.bytes().await?;
        return Ok(GetStateValidatorBalancesResponse::Unknown);
    }
}
#[derive(Debug, Clone, PartialEq, validator::Validate, oas3_gen_support::Default)]
pub struct GetStateValidatorBalancesRequestPath {
    ///State identifier.
    ///Can be one of: "head" (canonical head in node's view), "genesis", "finalized", "justified", \<slot\>, \<hex encoded stateRoot with 0x prefix\>.
    ///- Example: `"head".to_string()`
    #[validate(length(min = 1u64))]
    pub state_id: String,
}
#[serde_with::skip_serializing_none]
#[derive(
    Debug,
    Clone,
    PartialEq,
    Serialize,
    validator::Validate,
    oas3_gen_support::Default
)]
pub struct GetStateValidatorBalancesRequestQuery {
    ///Either hex encoded public key (any bytes48 with 0x prefix) or validator index
    #[validate(length(max = 64u64))]
    pub id: Option<Vec<String>>,
}
///Response types for getStateValidatorBalances
#[derive(Debug, Clone)]
pub enum GetStateValidatorBalancesResponse {
    ///200: Success
    Ok(GetStateValidatorBalancesResponseResponse),
    ///400: Invalid state or validator ID
    BadRequest(BlindedBlock400Response),
    ///404: State not found
    NotFound(BlindedBlock400Response),
    ///414: Too many validator IDs
    UriTooLong(BlindedBlock400Response),
    ///500: Beacon node internal error.
    InternalServerError(BlindedBlock400Response),
    ///default: Unknown response
    Unknown,
}
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct GetStateValidatorBalancesResponseResponse {
    pub data: Vec<GetStateValidatorBalancesResponseResponseDatum>,
    ///True if the response references an unverified execution payload. Optimistic information may be invalidated at a later time. If the field is not present, assume the False value.
    pub execution_optimistic: bool,
    ///True if the response references the finalized history of the chain, as determined by fork choice. If the field is not present, additional calls are necessary to compare the epoch of the requested information with the finalized checkpoint.
    pub finalized: bool,
}
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct GetStateValidatorBalancesResponseResponseDatum {
    ///Current validator balance in gwei.
    pub balance: String,
    ///Index of validator in validator registry.
    pub index: String,
}
///Returns validator specified by state and id or public key along with status and balance.
#[derive(Debug, Clone, validator::Validate, oas3_gen_support::Default)]
pub struct GetStateValidatorRequest {
    #[validate(nested)]
    pub path: GetStateValidatorRequestPath,
}
#[bon::bon]
impl GetStateValidatorRequest {
    ///Create a new request with the given parameters.
    #[builder]
    pub fn new(state_id: String, validator_id: String) -> anyhow::Result<Self> {
        let request = Self {
            path: GetStateValidatorRequestPath {
                state_id,
                validator_id,
            },
        };
        request.validate()?;
        Ok(request)
    }
}
impl GetStateValidatorRequest {
    ///Parse the HTTP response into the response enum.
    pub async fn parse_response(
        req: reqwest::Response,
    ) -> anyhow::Result<GetStateValidatorResponse> {
        let status = req.status();
        if status.is_success() {
            let data = oas3_gen_support::Diagnostics::<
                GetStateValidatorResponseResponse,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetStateValidatorResponse::Ok(data));
        }
        if status.as_u16() == 400u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetStateValidatorResponse::BadRequest(data));
        }
        if status.as_u16() == 404u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetStateValidatorResponse::NotFound(data));
        }
        if status.as_u16() == 500u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetStateValidatorResponse::InternalServerError(data));
        }
        let _ = req.bytes().await?;
        return Ok(GetStateValidatorResponse::Unknown);
    }
}
#[derive(Debug, Clone, PartialEq, validator::Validate, oas3_gen_support::Default)]
pub struct GetStateValidatorRequestPath {
    ///State identifier.
    ///Can be one of: "head" (canonical head in node's view), "genesis", "finalized", "justified", \<slot\>, \<hex encoded stateRoot with 0x prefix\>.
    ///- Example: `"head".to_string()`
    #[validate(length(min = 1u64))]
    pub state_id: String,
    ///Either hex encoded public key (any bytes48 with 0x prefix) or validator index
    #[validate(length(min = 1u64))]
    pub validator_id: String,
}
///Response types for getStateValidator
#[derive(Debug, Clone)]
pub enum GetStateValidatorResponse {
    ///200: Success
    Ok(GetStateValidatorResponseResponse),
    ///400: Invalid state or validator ID
    BadRequest(BlindedBlock400Response),
    ///404: Not Found
    NotFound(BlindedBlock400Response),
    ///500: Beacon node internal error.
    InternalServerError(BlindedBlock400Response),
    ///default: Unknown response
    Unknown,
}
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct GetStateValidatorResponseResponse {
    pub data: GetStateValidatorsResponseResponseDatum,
    ///True if the response references an unverified execution payload. Optimistic information may be invalidated at a later time. If the field is not present, assume the False value.
    pub execution_optimistic: bool,
    ///True if the response references the finalized history of the chain, as determined by fork choice. If the field is not present, additional calls are necessary to compare the epoch of the requested information with the finalized checkpoint.
    pub finalized: bool,
}
///Returns filterable list of validators with their balance, status and index.
///
///Information will be returned for all indices or public key that match known validators.  If an index or public key does not
///match any known validator, no information will be returned but this will not cause an error.  There are no guarantees for the
///returned data in terms of ordering; both the index and public key are returned for each validator, and can be used to confirm
///for which inputs a response has been returned.
#[derive(Debug, Clone, validator::Validate, oas3_gen_support::Default)]
pub struct GetStateValidatorsRequest {
    #[validate(nested)]
    pub path: GetStateValidatorsRequestPath,
    #[validate(nested)]
    pub query: GetStateValidatorsRequestQuery,
}
#[bon::bon]
impl GetStateValidatorsRequest {
    ///Create a new request with the given parameters.
    #[builder]
    pub fn new(
        state_id: String,
        id: Option<Vec<String>>,
        status: Option<Vec<serde_json::Value>>,
    ) -> anyhow::Result<Self> {
        let request = Self {
            path: GetStateValidatorsRequestPath {
                state_id,
            },
            query: GetStateValidatorsRequestQuery {
                id,
                status,
            },
        };
        request.validate()?;
        Ok(request)
    }
}
impl GetStateValidatorsRequest {
    ///Parse the HTTP response into the response enum.
    pub async fn parse_response(
        req: reqwest::Response,
    ) -> anyhow::Result<GetStateValidatorsResponse> {
        let status = req.status();
        if status.is_success() {
            let data = oas3_gen_support::Diagnostics::<
                GetStateValidatorsResponseResponse,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetStateValidatorsResponse::Ok(data));
        }
        if status.as_u16() == 400u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetStateValidatorsResponse::BadRequest(data));
        }
        if status.as_u16() == 404u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetStateValidatorsResponse::NotFound(data));
        }
        if status.as_u16() == 414u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetStateValidatorsResponse::UriTooLong(data));
        }
        if status.as_u16() == 500u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetStateValidatorsResponse::InternalServerError(data));
        }
        let _ = req.bytes().await?;
        return Ok(GetStateValidatorsResponse::Unknown);
    }
}
#[derive(Debug, Clone, PartialEq, validator::Validate, oas3_gen_support::Default)]
pub struct GetStateValidatorsRequestPath {
    ///State identifier.
    ///Can be one of: "head" (canonical head in node's view), "genesis", "finalized", "justified", \<slot\>, \<hex encoded stateRoot with 0x prefix\>.
    ///- Example: `"head".to_string()`
    #[validate(length(min = 1u64))]
    pub state_id: String,
}
#[serde_with::skip_serializing_none]
#[derive(
    Debug,
    Clone,
    PartialEq,
    Serialize,
    validator::Validate,
    oas3_gen_support::Default
)]
pub struct GetStateValidatorsRequestQuery {
    ///Either hex encoded public key (any bytes48 with 0x prefix) or validator index
    #[validate(length(max = 64u64))]
    pub id: Option<Vec<String>>,
    ///[Validator status specification](https://hackmd.io/ofFJ5gOmQpu1jjHilHbdQQ)
    pub status: Option<Vec<serde_json::Value>>,
}
///Response types for getStateValidators
#[derive(Debug, Clone)]
pub enum GetStateValidatorsResponse {
    ///200: Success
    Ok(GetStateValidatorsResponseResponse),
    ///400: Invalid state or validator ID, or status
    BadRequest(BlindedBlock400Response),
    ///404: State not found
    NotFound(BlindedBlock400Response),
    ///414: Too many validator IDs
    UriTooLong(BlindedBlock400Response),
    ///500: Beacon node internal error.
    InternalServerError(BlindedBlock400Response),
    ///default: Unknown response
    Unknown,
}
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct GetStateValidatorsResponseResponse {
    pub data: Vec<GetStateValidatorsResponseResponseDatum>,
    ///True if the response references an unverified execution payload. Optimistic information may be invalidated at a later time. If the field is not present, assume the False value.
    pub execution_optimistic: bool,
    ///True if the response references the finalized history of the chain, as determined by fork choice. If the field is not present, additional calls are necessary to compare the epoch of the requested information with the finalized checkpoint.
    pub finalized: bool,
}
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct GetStateValidatorsResponseResponseDatum {
    ///Current validator balance in gwei.
    pub balance: String,
    ///Index of validator in validator registry.
    pub index: String,
    ///Possible statuses:
    ///- **pending_initialized** - When the first deposit is processed, but not enough funds are available (or not yet the end of the first epoch) to get validator into the activation queue.
    ///- **pending_queued** - When validator is waiting to get activated, and have enough funds etc. while in the queue, validator activation epoch keeps changing until it gets to the front and make it through (finalization is a requirement here too).
    ///- **active_ongoing** - When validator must be attesting, and have not initiated any exit.
    ///- **active_exiting** - When validator is still active, but filed a voluntary request to exit.
    ///- **active_slashed** - When validator is still active, but have a slashed status and is scheduled to exit.
    ///- **exited_unslashed** - When validator has reached regular exit epoch, not being slashed, and doesn't have to attest any more, but cannot withdraw yet.
    ///- **exited_slashed** - When validator has reached regular exit epoch, but was slashed, have to wait for a longer withdrawal period.
    ///- **withdrawal_possible** - After validator has exited, a while later is permitted to move funds, and is truly out of the system.
    ///- **withdrawal_done** - (not possible in phase0, except slashing full balance) - actually having moved funds away
    ///
    ///[Validator status specification](https://hackmd.io/ofFJ5gOmQpu1jjHilHbdQQ)
    pub status: ValidatorStatus,
    ///The [`Validator`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/phase0/beacon-chain.md#validator) object from the CL spec.
    pub validator: ValidatorResponseValidator,
}
pub type GetSyncCommitteeDutiesBodyRequestBody = Vec<String>;
///Requests the beacon node to provide a set of sync committee duties for a particular epoch.
#[derive(Debug, Clone, validator::Validate, oas3_gen_support::Default)]
pub struct GetSyncCommitteeDutiesRequest {
    #[validate(nested)]
    pub path: GetSyncCommitteeDutiesRequestPath,
    ///An array of the validator indices for which to obtain the duties.
    pub body: GetSyncCommitteeDutiesBodyRequestBody,
}
#[bon::bon]
impl GetSyncCommitteeDutiesRequest {
    ///Create a new request with the given parameters.
    #[builder]
    pub fn new(
        epoch: String,
        body: GetSyncCommitteeDutiesBodyRequestBody,
    ) -> anyhow::Result<Self> {
        let request = Self {
            path: GetSyncCommitteeDutiesRequestPath {
                epoch,
            },
            body,
        };
        request.validate()?;
        Ok(request)
    }
}
impl GetSyncCommitteeDutiesRequest {
    ///Parse the HTTP response into the response enum.
    pub async fn parse_response(
        req: reqwest::Response,
    ) -> anyhow::Result<GetSyncCommitteeDutiesResponse> {
        let status = req.status();
        if status.is_success() {
            let data = oas3_gen_support::Diagnostics::<
                GetSyncCommitteeDutiesResponseResponse,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetSyncCommitteeDutiesResponse::Ok(data));
        }
        if status.as_u16() == 400u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetSyncCommitteeDutiesResponse::BadRequest(data));
        }
        if status.as_u16() == 500u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetSyncCommitteeDutiesResponse::InternalServerError(data));
        }
        if status.as_u16() == 503u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetSyncCommitteeDutiesResponse::ServiceUnavailable(data));
        }
        let _ = req.bytes().await?;
        return Ok(GetSyncCommitteeDutiesResponse::Unknown);
    }
}
#[derive(Debug, Clone, PartialEq, validator::Validate, oas3_gen_support::Default)]
pub struct GetSyncCommitteeDutiesRequestPath {
    ///epoch // EPOCHS_PER_SYNC_COMMITTEE_PERIOD <= current_epoch // EPOCHS_PER_SYNC_COMMITTEE_PERIOD + 1
    #[validate(length(min = 1u64))]
    pub epoch: String,
}
///Response types for getSyncCommitteeDuties
#[derive(Debug, Clone)]
pub enum GetSyncCommitteeDutiesResponse {
    ///200: Success response
    Ok(GetSyncCommitteeDutiesResponseResponse),
    ///400: Invalid epoch or index
    BadRequest(BlindedBlock400Response),
    ///500: Beacon node internal error.
    InternalServerError(BlindedBlock400Response),
    ///503: Beacon node is currently syncing, try again later.
    ServiceUnavailable(BlindedBlock400Response),
    ///default: Unknown response
    Unknown,
}
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct GetSyncCommitteeDutiesResponseResponse {
    pub data: Vec<GetSyncCommitteeDutiesResponseResponseDatum>,
    ///True if the response references an unverified execution payload. Optimistic information may be invalidated at a later time. If the field is not present, assume the False value.
    pub execution_optimistic: bool,
}
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct GetSyncCommitteeDutiesResponseResponseDatum {
    ///The validator's BLS public key, uniquely identifying them. _48-bytes, hex encoded with 0x prefix, case insensitive._
    pub pubkey: String,
    ///Index of validator in validator registry.
    pub validator_index: String,
    ///The indices of the validator in the sync committee.
    pub validator_sync_committee_indices: Vec<String>,
}
///Retrieves rewards info for sync committee members specified by array of public keys or validator index. If no array is provided, return reward info for every committee member.
#[derive(Debug, Clone, validator::Validate, oas3_gen_support::Default)]
pub struct GetSyncCommitteeRewardsRequest {
    #[validate(nested)]
    pub path: GetSyncCommitteeRewardsRequestPath,
    ///An array of either hex encoded public key (any bytes48 with 0x prefix) or validator index
    pub body: Option<AttestationRequestBody>,
}
#[bon::bon]
impl GetSyncCommitteeRewardsRequest {
    ///Create a new request with the given parameters.
    #[builder]
    pub fn new(
        block_id: String,
        body: Option<AttestationRequestBody>,
    ) -> anyhow::Result<Self> {
        let request = Self {
            path: GetSyncCommitteeRewardsRequestPath {
                block_id,
            },
            body,
        };
        request.validate()?;
        Ok(request)
    }
}
impl GetSyncCommitteeRewardsRequest {
    ///Parse the HTTP response into the response enum.
    pub async fn parse_response(
        req: reqwest::Response,
    ) -> anyhow::Result<GetSyncCommitteeRewardsResponse> {
        let status = req.status();
        if status.is_success() {
            let data = oas3_gen_support::Diagnostics::<
                GetSyncCommitteeRewardsResponseResponse,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetSyncCommitteeRewardsResponse::Ok(data));
        }
        if status.as_u16() == 400u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetSyncCommitteeRewardsResponse::BadRequest(data));
        }
        if status.as_u16() == 404u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetSyncCommitteeRewardsResponse::NotFound(data));
        }
        if status.as_u16() == 500u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetSyncCommitteeRewardsResponse::InternalServerError(data));
        }
        let _ = req.bytes().await?;
        return Ok(GetSyncCommitteeRewardsResponse::Unknown);
    }
}
#[derive(Debug, Clone, PartialEq, validator::Validate, oas3_gen_support::Default)]
pub struct GetSyncCommitteeRewardsRequestPath {
    ///Block identifier.
    ///Can be one of: "head" (canonical head in node's view), "genesis", "finalized", \<slot\>, \<hex encoded blockRoot with 0x prefix\>.
    ///- Example: `"head".to_string()`
    #[validate(length(min = 1u64))]
    pub block_id: String,
}
///Response types for getSyncCommitteeRewards
#[derive(Debug, Clone)]
pub enum GetSyncCommitteeRewardsResponse {
    ///200: Success
    Ok(GetSyncCommitteeRewardsResponseResponse),
    ///400: Invalid get sync committee rewards request
    BadRequest(BlindedBlock400Response),
    ///404: Block not found
    NotFound(BlindedBlock400Response),
    ///500: Beacon node internal error.
    InternalServerError(BlindedBlock400Response),
    ///default: Unknown response
    Unknown,
}
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct GetSyncCommitteeRewardsResponseResponse {
    ///Rewards info for sync committee members
    pub data: Vec<GetSyncCommitteeRewardsResponseResponseDatum>,
    ///True if the response references an unverified execution payload. Optimistic information may be invalidated at a later time. If the field is not present, assume the False value.
    pub execution_optimistic: bool,
    ///True if the response references the finalized history of the chain, as determined by fork choice. If the field is not present, additional calls are necessary to compare the epoch of the requested information with the finalized checkpoint.
    pub finalized: bool,
}
///Rewards info for a single sync committee member
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct GetSyncCommitteeRewardsResponseResponseDatum {
    ///sync committee reward in gwei for the validator
    pub reward: String,
    ///one entry for every validator participating in the sync committee
    pub validator_index: String,
}
///Requests the beacon node to describe if it's currently syncing or not, and if it is, what block it is up to.
#[derive(Debug, Clone, validator::Validate, oas3_gen_support::Default)]
pub struct GetSyncingStatusRequest {}
impl GetSyncingStatusRequest {
    ///Parse the HTTP response into the response enum.
    pub async fn parse_response(
        req: reqwest::Response,
    ) -> anyhow::Result<GetSyncingStatusResponse> {
        let status = req.status();
        if status.is_success() {
            let data = oas3_gen_support::Diagnostics::<
                GetSyncingStatusResponseResponse,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetSyncingStatusResponse::Ok(data));
        }
        if status.as_u16() == 500u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(GetSyncingStatusResponse::InternalServerError(data));
        }
        let _ = req.bytes().await?;
        return Ok(GetSyncingStatusResponse::Unknown);
    }
}
///Response types for getSyncingStatus
#[derive(Debug, Clone)]
pub enum GetSyncingStatusResponse {
    ///200: Request successful
    Ok(GetSyncingStatusResponseResponse),
    ///500: Beacon node internal error.
    InternalServerError(BlindedBlock400Response),
    ///default: Unknown response
    Unknown,
}
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct GetSyncingStatusResponseResponse {
    pub data: GetSyncingStatusResponseResponseData,
}
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct GetSyncingStatusResponseResponseData {
    ///Set to true if the execution client is offline.
    pub el_offline: bool,
    ///Head slot node is trying to reach
    pub head_slot: String,
    ///Set to true if the node is optimistically tracking head.
    pub is_optimistic: bool,
    ///Set to true if the node is syncing, false if the node is synced.
    pub is_syncing: bool,
    ///How many slots node needs to process to reach head. 0 if synced.
    pub sync_distance: String,
}
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct GetVersionResponseResponse {
    pub data: GetVersionResponseResponseData,
}
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct GetVersionResponseResponseData {
    ///A string which uniquely identifies the client implementation and its version; similar to [HTTP User-Agent](https://tools.ietf.org/html/rfc7231#section-5.5.3).
    pub version: String,
}
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct Header {
    ///The [`BeaconBlockHeader`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/phase0/beacon-chain.md#beaconblockheader) object from the CL spec.
    pub beacon: serde_json::Value,
    ///The [`ExecutionPayloadHeader`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/capella/beacon-chain.md#executionpayloadheader) object from the CL Capella spec.
    pub execution: serde_json::Value,
    pub execution_branch: Vec<String>,
}
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct Header2 {
    ///The [`BeaconBlockHeader`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/phase0/beacon-chain.md#beaconblockheader) object from the CL spec.
    pub beacon: serde_json::Value,
    ///The [`ExecutionPayloadHeader`](https://github.com/ethereum/consensus-specs/blob/v1.4.0/specs/deneb/beacon-chain.md#executionpayloadheader) object from the CL Deneb spec.
    pub execution: serde_json::Value,
    pub execution_branch: Vec<String>,
}
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct Header3 {
    ///The [`BeaconBlockHeader`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/phase0/beacon-chain.md#beaconblockheader) object from the CL spec.
    pub beacon: serde_json::Value,
}
///Based on eth2 [Metadata object](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/phase0/p2p-interface.md#metadata)
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct NetworkIdentityMetadata {
    ///Bitvector representing the node's persistent attestation subnet subscriptions.
    pub attnets: String,
    ///Uint64 representing the node's custody group count. The metadata is present from the Fulu fork.
    pub custody_group_count: Option<String>,
    ///Uint64 starting at 0 used to version the node's metadata. If any other field in the local MetaData changes, the node MUST increment seq_number by 1.
    pub seq_number: String,
    ///Bitvector representing the node's sync committee subnet subscriptions. This metadata is not present in phase0, but will be present in Altair.
    pub syncnets: Option<String>,
}
#[derive(Debug, Clone, PartialEq, Eq, Hash, Serialize, Deserialize, oas3_gen_support::Default)]
pub enum NodeValidity {
    #[serde(rename = "valid")]
    #[default]
    Valid,
    #[serde(rename = "invalid")]
    Invalid,
    #[serde(rename = "optimistic")]
    Optimistic,
}
impl core::fmt::Display for NodeValidity {
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        match self {
            Self::Valid => write!(f, "valid"),
            Self::Invalid => write!(f, "invalid"),
            Self::Optimistic => write!(f, "optimistic"),
        }
    }
}
#[derive(
    Debug,
    Clone,
    PartialEq,
    Eq,
    Hash,
    Serialize,
    Deserialize,
    oas3_gen_support::Default
)]
pub enum PeerConnectionDirection {
    #[serde(rename = "inbound")]
    #[default]
    Inbound,
    #[serde(rename = "outbound")]
    Outbound,
}
impl core::fmt::Display for PeerConnectionDirection {
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        match self {
            Self::Inbound => write!(f, "inbound"),
            Self::Outbound => write!(f, "outbound"),
        }
    }
}
#[derive(
    Debug,
    Clone,
    PartialEq,
    Eq,
    Hash,
    Serialize,
    Deserialize,
    oas3_gen_support::Default
)]
pub enum PeerConnectionState {
    #[serde(rename = "disconnected")]
    #[default]
    Disconnected,
    #[serde(rename = "connecting")]
    Connecting,
    #[serde(rename = "connected")]
    Connected,
    #[serde(rename = "disconnecting")]
    Disconnecting,
}
impl core::fmt::Display for PeerConnectionState {
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        match self {
            Self::Disconnected => write!(f, "disconnected"),
            Self::Connecting => write!(f, "connecting"),
            Self::Connected => write!(f, "connected"),
            Self::Disconnecting => write!(f, "disconnecting"),
        }
    }
}
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct PendingConsolidation400Response {
    ///Either specific error code in case of invalid request or http status code
    pub code: f64,
    ///Message describing error
    pub message: String,
    ///Optional stacktraces, sent when node is in debug mode
    pub stacktraces: Option<Vec<String>>,
}
///The [`IndexedAttestation`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/phase0/beacon-chain.md#indexedattestation) object from the CL spec.
#[derive(
    Debug,
    Clone,
    PartialEq,
    Serialize,
    Deserialize,
    validator::Validate,
    oas3_gen_support::Default
)]
pub struct Phase0AttesterSlashingAttestation1 {
    ///Attesting validator indices
    #[validate(length(max = 2_048u64))]
    pub attesting_indices: Vec<String>,
    ///The [`AttestationData`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/phase0/beacon-chain.md#attestationdata) object from the CL spec.
    #[validate(nested)]
    pub data: Data,
    ///The BLS signature of the `IndexedAttestation`, created by the validator of the attestation.
    #[validate(
        length(min = 1u64),
        regex(path = "REGEX_AGGREGATE_AND_PROOF_REQUEST_BODY_ARRAY_SIGNATURE")
    )]
    pub signature: String,
}
///The [`BeaconBlockBody`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/phase0/beacon-chain.md#beaconblockbody) object from the CL spec.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct Phase0BeaconBlockBody {
    pub attestations: Vec<GetBlockAttestationsV2ResponseResponseDataArray2>,
    pub attester_slashings: Vec<GetPoolAttesterSlashingsV2ResponseResponseDataArray2>,
    pub deposits: Vec<AltairBeaconBlockBodyDeposit>,
    ///The [`Eth1Data`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/phase0/beacon-chain.md#eth1data) object from the CL spec.
    pub eth1_data: Eth1Data,
    pub graffiti: String,
    pub proposer_slashings: Vec<GetPoolProposerSlashingsResponseResponseDatum>,
    ///The RanDAO reveal value provided by the validator.
    pub randao_reveal: String,
    pub voluntary_exits: Vec<GetPoolVoluntaryExitsResponseResponseDatum>,
}
///The [`SignedBeaconBlockHeader`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/phase0/beacon-chain.md#signedbeaconblockheader) object envelope from the CL spec.
#[derive(
    Debug,
    Clone,
    PartialEq,
    Serialize,
    Deserialize,
    validator::Validate,
    oas3_gen_support::Default
)]
pub struct Phase0ProposerSlashingSignedHeader1 {
    ///The [`BeaconBlockHeader`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/phase0/beacon-chain.md#beaconblockheader) object from the CL spec.
    pub message: serde_json::Value,
    #[validate(
        length(min = 1u64),
        regex(path = "REGEX_AGGREGATE_AND_PROOF_REQUEST_BODY_ARRAY_SIGNATURE")
    )]
    pub signature: String,
}
///The [`VoluntaryExit`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/phase0/beacon-chain.md#voluntaryexit) object from the CL spec.
#[derive(
    Debug,
    Clone,
    PartialEq,
    Serialize,
    Deserialize,
    validator::Validate,
    oas3_gen_support::Default
)]
pub struct Phase0SignedVoluntaryExitMessage {
    ///Minimum epoch for processing exit.
    #[validate(length(min = 1u64))]
    pub epoch: String,
    ///Index of the exiting validator.
    #[validate(length(min = 1u64))]
    pub validator_index: String,
}
pub type PostLivenessRequestBodyRequestBody = Vec<String>;
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct PostLivenessResponseBodyResponse {
    pub data: Vec<PostLivenessResponseBodyResponseDatum>,
}
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct PostLivenessResponseBodyResponseDatum {
    pub index: String,
    pub is_live: bool,
}
///Returns filterable list of validators balances.
///
///Balances will be returned for all indices or public keys that match known validators. If an index or public key does not
///match any known validator, no balance will be returned but this will not cause an error. There are no guarantees for the
///returned data in terms of ordering; the index is returned for each balance, and can be used to confirm for which inputs a
///response has been returned.
#[derive(Debug, Clone, validator::Validate, oas3_gen_support::Default)]
pub struct PostStateValidatorBalancesRequest {
    #[validate(nested)]
    pub path: PostStateValidatorBalancesRequestPath,
    ///An array of values, with each value either a hex encoded public key (any bytes48 with 0x prefix) or a validator index.
    ///
    ///If the supplied list is empty (i.e. the body is `[]`) or no body is supplied then balances will be returned for all validators.
    pub body: Option<AttestationRequestBody>,
}
#[bon::bon]
impl PostStateValidatorBalancesRequest {
    ///Create a new request with the given parameters.
    #[builder]
    pub fn new(
        state_id: String,
        body: Option<AttestationRequestBody>,
    ) -> anyhow::Result<Self> {
        let request = Self {
            path: PostStateValidatorBalancesRequestPath {
                state_id,
            },
            body,
        };
        request.validate()?;
        Ok(request)
    }
}
impl PostStateValidatorBalancesRequest {
    ///Parse the HTTP response into the response enum.
    pub async fn parse_response(
        req: reqwest::Response,
    ) -> anyhow::Result<PostStateValidatorBalancesResponse> {
        let status = req.status();
        if status.is_success() {
            let data = oas3_gen_support::Diagnostics::<
                GetStateValidatorBalancesResponseResponse,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(PostStateValidatorBalancesResponse::Ok(data));
        }
        if status.as_u16() == 400u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(PostStateValidatorBalancesResponse::BadRequest(data));
        }
        if status.as_u16() == 404u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(PostStateValidatorBalancesResponse::NotFound(data));
        }
        if status.as_u16() == 500u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(PostStateValidatorBalancesResponse::InternalServerError(data));
        }
        let _ = req.bytes().await?;
        return Ok(PostStateValidatorBalancesResponse::Unknown);
    }
}
#[derive(Debug, Clone, PartialEq, validator::Validate, oas3_gen_support::Default)]
pub struct PostStateValidatorBalancesRequestPath {
    ///State identifier.
    ///Can be one of: "head" (canonical head in node's view), "genesis", "finalized", "justified", \<slot\>, \<hex encoded stateRoot with 0x prefix\>.
    ///- Example: `"head".to_string()`
    #[validate(length(min = 1u64))]
    pub state_id: String,
}
///Response types for postStateValidatorBalances
#[derive(Debug, Clone)]
pub enum PostStateValidatorBalancesResponse {
    ///200: Success
    Ok(GetStateValidatorBalancesResponseResponse),
    ///400: Invalid state ID or malformed request
    BadRequest(BlindedBlock400Response),
    ///404: State not found
    NotFound(BlindedBlock400Response),
    ///500: Beacon node internal error.
    InternalServerError(BlindedBlock400Response),
    ///default: Unknown response
    Unknown,
}
///Returns filterable list of validators identities.
///
///Identities will be returned for all indices or public keys that match known validators. If an index or public key does not
///match any known validator, no identity will be returned but this will not cause an error. There are no guarantees for the
///returned data in terms of ordering.
///
/// Depending on `Accept` header data can be returned either as JSON or as bytes serialized by SSZ.
#[derive(Debug, Clone, validator::Validate, oas3_gen_support::Default)]
pub struct PostStateValidatorIdentitiesRequest {
    #[validate(nested)]
    pub path: PostStateValidatorIdentitiesRequestPath,
    ///An array of values, with each value either a hex encoded public key (any bytes48 with 0x prefix) or a validator index.
    ///
    ///If the supplied list is empty (i.e. the body is `[]`) or no body is supplied then all validators will be returned.
    pub body: Option<AttestationRequestBody>,
}
#[bon::bon]
impl PostStateValidatorIdentitiesRequest {
    ///Create a new request with the given parameters.
    #[builder]
    pub fn new(
        state_id: String,
        body: Option<AttestationRequestBody>,
    ) -> anyhow::Result<Self> {
        let request = Self {
            path: PostStateValidatorIdentitiesRequestPath {
                state_id,
            },
            body,
        };
        request.validate()?;
        Ok(request)
    }
}
impl PostStateValidatorIdentitiesRequest {
    ///Parse the HTTP response into the response enum.
    pub async fn parse_response(
        req: reqwest::Response,
    ) -> anyhow::Result<PostStateValidatorIdentitiesResponse> {
        let status = req.status();
        if status.is_success() {
            let content_type_str = req
                .headers()
                .get(reqwest::header::CONTENT_TYPE)
                .and_then(|v| v.to_str().ok())
                .unwrap_or("application/json");
            if content_type_str.contains("json") {
                let data = oas3_gen_support::Diagnostics::<
                    PostStateValidatorIdentitiesResponseResponse,
                >::json_with_diagnostics(req)
                    .await?;
                return Ok(PostStateValidatorIdentitiesResponse::Ok(data));
            }
            if content_type_str.starts_with("application/octet-stream")
                || content_type_str.starts_with("image/")
                || content_type_str.starts_with("audio/")
                || content_type_str.starts_with("video/")
            {
                let data = req.bytes().await?.to_vec();
                return Ok(PostStateValidatorIdentitiesResponse::OkBinary(data));
            }
        }
        if status.as_u16() == 400u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(PostStateValidatorIdentitiesResponse::BadRequest(data));
        }
        if status.as_u16() == 404u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(PostStateValidatorIdentitiesResponse::NotFound(data));
        }
        if status.as_u16() == 406u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock406Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(PostStateValidatorIdentitiesResponse::NotAcceptable(data));
        }
        if status.as_u16() == 500u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(PostStateValidatorIdentitiesResponse::InternalServerError(data));
        }
        let _ = req.bytes().await?;
        return Ok(PostStateValidatorIdentitiesResponse::Unknown);
    }
}
#[derive(Debug, Clone, PartialEq, validator::Validate, oas3_gen_support::Default)]
pub struct PostStateValidatorIdentitiesRequestPath {
    ///State identifier.
    ///Can be one of: "head" (canonical head in node's view), "genesis", "finalized", "justified", \<slot\>, \<hex encoded stateRoot with 0x prefix\>.
    ///- Example: `"head".to_string()`
    #[validate(length(min = 1u64))]
    pub state_id: String,
}
///Response types for postStateValidatorIdentities
#[derive(Debug, Clone)]
pub enum PostStateValidatorIdentitiesResponse {
    ///200: Success
    Ok(PostStateValidatorIdentitiesResponseResponse),
    ///200: Success
    OkBinary(Vec<u8>),
    ///400: Invalid state ID or malformed request
    BadRequest(BlindedBlock400Response),
    ///404: State not found
    NotFound(BlindedBlock400Response),
    ///406: Accepted media type is not supported.
    NotAcceptable(BlindedBlock406Response),
    ///500: Beacon node internal error.
    InternalServerError(BlindedBlock400Response),
    ///default: Unknown response
    Unknown,
}
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct PostStateValidatorIdentitiesResponseResponse {
    pub data: Vec<PostStateValidatorIdentitiesResponseResponseDatum>,
    ///True if the response references an unverified execution payload. Optimistic information may be invalidated at a later time. If the field is not present, assume the False value.
    pub execution_optimistic: bool,
    ///True if the response references the finalized history of the chain, as determined by fork choice. If the field is not present, additional calls are necessary to compare the epoch of the requested information with the finalized checkpoint.
    pub finalized: bool,
}
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct PostStateValidatorIdentitiesResponseResponseDatum {
    ///Epoch when validator activated. 'FAR_FUTURE_EPOCH' if not activated
    pub activation_epoch: String,
    ///Index of validator in validator registry.
    pub index: String,
    ///The validator's BLS public key, uniquely identifying them. _48-bytes, hex encoded with 0x prefix, case insensitive._
    pub pubkey: String,
}
///Returns filterable list of validators with their balance, status and index.
///
///Information will be returned for all indices or public key that match known validators.  If an index or public key does not
///match any known validator, no information will be returned but this will not cause an error.  There are no guarantees for the
///returned data in terms of ordering; both the index and public key are returned for each validator, and can be used to confirm
///for which inputs a response has been returned.
///
///The POST variant of this endpoint has the same semantics as the GET endpoint but passes
///the lists of IDs and statuses via a POST body in order to enable larger requests.
#[derive(Debug, Clone, validator::Validate, oas3_gen_support::Default)]
pub struct PostStateValidatorsRequest {
    #[validate(nested)]
    pub path: PostStateValidatorsRequestPath,
    ///The lists of validator IDs and statuses to filter on. Either or both may be `null` to signal that no filtering on that attribute is desired.
    pub body: ValidatorRequestBody,
}
#[bon::bon]
impl PostStateValidatorsRequest {
    ///Create a new request with the given parameters.
    #[builder]
    pub fn new(state_id: String, body: ValidatorRequestBody) -> anyhow::Result<Self> {
        let request = Self {
            path: PostStateValidatorsRequestPath {
                state_id,
            },
            body,
        };
        request.validate()?;
        Ok(request)
    }
}
impl PostStateValidatorsRequest {
    ///Parse the HTTP response into the response enum.
    pub async fn parse_response(
        req: reqwest::Response,
    ) -> anyhow::Result<PostStateValidatorsResponse> {
        let status = req.status();
        if status.is_success() {
            let data = oas3_gen_support::Diagnostics::<
                GetStateValidatorsResponseResponse,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(PostStateValidatorsResponse::Ok(data));
        }
        if status.as_u16() == 400u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(PostStateValidatorsResponse::BadRequest(data));
        }
        if status.as_u16() == 404u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(PostStateValidatorsResponse::NotFound(data));
        }
        if status.as_u16() == 500u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(PostStateValidatorsResponse::InternalServerError(data));
        }
        let _ = req.bytes().await?;
        return Ok(PostStateValidatorsResponse::Unknown);
    }
}
#[derive(Debug, Clone, PartialEq, validator::Validate, oas3_gen_support::Default)]
pub struct PostStateValidatorsRequestPath {
    ///State identifier.
    ///Can be one of: "head" (canonical head in node's view), "genesis", "finalized", "justified", \<slot\>, \<hex encoded stateRoot with 0x prefix\>.
    ///- Example: `"head".to_string()`
    #[validate(length(min = 1u64))]
    pub state_id: String,
}
///Response types for postStateValidators
#[derive(Debug, Clone)]
pub enum PostStateValidatorsResponse {
    ///200: Success
    Ok(GetStateValidatorsResponseResponse),
    ///400: Invalid state or validator ID, or status
    BadRequest(BlindedBlock400Response),
    ///404: State not found
    NotFound(BlindedBlock400Response),
    ///500: Beacon node internal error.
    InternalServerError(BlindedBlock400Response),
    ///default: Unknown response
    Unknown,
}
///After beacon node receives this request,
///search using discv5 for peers related to this subnet
///and replace current peers with those ones if necessary
///If validator `is_aggregator`, beacon node must:
///- announce subnet topic subscription on gossipsub
///- aggregate attestations received on that subnet
#[derive(Debug, Clone, validator::Validate, oas3_gen_support::Default)]
pub struct PrepareBeaconCommitteeSubnetRequest {
    pub body: SubscribeToBeaconCommitteeSubnetRequestBodyRequestBody,
}
#[bon::bon]
impl PrepareBeaconCommitteeSubnetRequest {
    ///Create a new request with the given parameters.
    #[builder]
    pub fn new(
        body: SubscribeToBeaconCommitteeSubnetRequestBodyRequestBody,
    ) -> anyhow::Result<Self> {
        let request = Self { body };
        request.validate()?;
        Ok(request)
    }
}
impl PrepareBeaconCommitteeSubnetRequest {
    ///Parse the HTTP response into the response enum.
    pub async fn parse_response(
        req: reqwest::Response,
    ) -> anyhow::Result<PrepareBeaconCommitteeSubnetResponse> {
        let status = req.status();
        if status.is_success() {
            let _ = req.bytes().await?;
            return Ok(PrepareBeaconCommitteeSubnetResponse::Ok);
        }
        if status.as_u16() == 400u16 {
            let data = oas3_gen_support::Diagnostics::<
                PendingConsolidation400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(PrepareBeaconCommitteeSubnetResponse::BadRequest(data));
        }
        if status.as_u16() == 500u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(PrepareBeaconCommitteeSubnetResponse::InternalServerError(data));
        }
        if status.as_u16() == 503u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(PrepareBeaconCommitteeSubnetResponse::ServiceUnavailable(data));
        }
        let _ = req.bytes().await?;
        return Ok(PrepareBeaconCommitteeSubnetResponse::Unknown);
    }
}
///Response types for prepareBeaconCommitteeSubnet
#[derive(Debug, Clone)]
pub enum PrepareBeaconCommitteeSubnetResponse {
    /**200: Slot signature is valid and beacon node has prepared the attestation subnet.

Note that we cannot be certain the Beacon node will find peers for that subnet for various reasons.
*/
    Ok,
    ///400: Invalid request syntax.
    BadRequest(PendingConsolidation400Response),
    ///500: Beacon node internal error.
    InternalServerError(BlindedBlock400Response),
    ///503: Beacon node is currently syncing, try again later.
    ServiceUnavailable(BlindedBlock400Response),
    ///default: Unknown response
    Unknown,
}
///Prepares the beacon node for potential proposers by supplying information
///required when proposing blocks for the given validators.  The information
///supplied for each validator index will persist through the epoch in which
///the call is submitted and for a further two epochs after that, or until the
///beacon node restarts.  It is expected that validator clients will send this
///information periodically, for example each epoch, to ensure beacon nodes have
///correct and timely fee recipient information.
///
///Note that there is no guarantee that the beacon node will use the supplied fee
///recipient when creating a block proposal, so on receipt of a proposed block the
///validator should confirm that it finds the fee recipient within the block
///acceptable before signing it.
///
///Also note that requests containing currently inactive or unknown validator
///indices will be accepted, as they may become active at a later epoch.
#[derive(Debug, Clone, validator::Validate, oas3_gen_support::Default)]
pub struct PrepareBeaconProposerRequest {
    pub body: PrepareBeaconProposerRequestBody,
}
#[bon::bon]
impl PrepareBeaconProposerRequest {
    ///Create a new request with the given parameters.
    #[builder]
    pub fn new(body: PrepareBeaconProposerRequestBody) -> anyhow::Result<Self> {
        let request = Self { body };
        request.validate()?;
        Ok(request)
    }
}
impl PrepareBeaconProposerRequest {
    ///Parse the HTTP response into the response enum.
    pub async fn parse_response(
        req: reqwest::Response,
    ) -> anyhow::Result<PrepareBeaconProposerResponse> {
        let status = req.status();
        if status.is_success() {
            let _ = req.bytes().await?;
            return Ok(PrepareBeaconProposerResponse::Ok);
        }
        if status.as_u16() == 400u16 {
            let data = oas3_gen_support::Diagnostics::<
                PendingConsolidation400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(PrepareBeaconProposerResponse::BadRequest(data));
        }
        if status.as_u16() == 500u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(PrepareBeaconProposerResponse::InternalServerError(data));
        }
        let _ = req.bytes().await?;
        return Ok(PrepareBeaconProposerResponse::Unknown);
    }
}
pub type PrepareBeaconProposerRequestBody = Vec<PrepareBeaconProposerRequestBodyItem>;
#[derive(
    Debug,
    Clone,
    PartialEq,
    Serialize,
    validator::Validate,
    oas3_gen_support::Default
)]
pub struct PrepareBeaconProposerRequestBodyItem {
    ///An address on the execution (Ethereum 1) network.
    #[validate(
        length(min = 1u64),
        regex(
            path = "REGEX_CAPELLA_SIGNED_BLS_TO_EXECUTION_CHANGE_MESSAGE_TO_EXECUTION_ADDRESS"
        )
    )]
    pub fee_recipient: String,
    #[validate(length(min = 1u64))]
    pub validator_index: String,
}
///Response types for prepareBeaconProposer
#[derive(Debug, Clone)]
pub enum PrepareBeaconProposerResponse {
    /**200: Preparation information has been received.
*/
    Ok,
    ///400: Invalid request syntax.
    BadRequest(PendingConsolidation400Response),
    ///500: Beacon node internal error.
    InternalServerError(BlindedBlock400Response),
    ///default: Unknown response
    Unknown,
}
///Subscribe to a number of sync committee subnets
///
///Sync committees are not present in phase0, but are required for Altair networks.
///
///Subscribing to sync committee subnets is an action performed by VC to enable network participation in Altair networks, and only required if the VC has an active validator in an active sync committee.
#[derive(Debug, Clone, validator::Validate, oas3_gen_support::Default)]
pub struct PrepareSyncCommitteeSubnetsRequest {
    pub body: SyncCommitteeSubscriptionRequestBody,
}
#[bon::bon]
impl PrepareSyncCommitteeSubnetsRequest {
    ///Create a new request with the given parameters.
    #[builder]
    pub fn new(body: SyncCommitteeSubscriptionRequestBody) -> anyhow::Result<Self> {
        let request = Self { body };
        request.validate()?;
        Ok(request)
    }
}
impl PrepareSyncCommitteeSubnetsRequest {
    ///Parse the HTTP response into the response enum.
    pub async fn parse_response(
        req: reqwest::Response,
    ) -> anyhow::Result<PrepareBeaconProposerResponse> {
        let status = req.status();
        if status.is_success() {
            let _ = req.bytes().await?;
            return Ok(PrepareBeaconProposerResponse::Ok);
        }
        if status.as_u16() == 400u16 {
            let data = oas3_gen_support::Diagnostics::<
                PendingConsolidation400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(PrepareBeaconProposerResponse::BadRequest(data));
        }
        if status.as_u16() == 500u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(PrepareBeaconProposerResponse::InternalServerError(data));
        }
        let _ = req.bytes().await?;
        return Ok(PrepareBeaconProposerResponse::Unknown);
    }
}
///Requests that the beacon node produce an AttestationData. For `slot`s in
///Electra and later, this AttestationData must have a `committee_index` of 0.
///
///A 503 error must be returned if the block identified by the response
///`beacon_block_root` is optimistic (i.e. the attestation attests to a block
///that has not been fully verified by an execution engine).
#[derive(Debug, Clone, validator::Validate, oas3_gen_support::Default)]
pub struct ProduceAttestationDataRequest {
    #[validate(nested)]
    pub query: ProduceAttestationDataRequestQuery,
}
#[bon::bon]
impl ProduceAttestationDataRequest {
    ///Create a new request with the given parameters.
    #[builder]
    pub fn new(slot: String, committee_index: String) -> anyhow::Result<Self> {
        let request = Self {
            query: ProduceAttestationDataRequestQuery {
                slot,
                committee_index,
            },
        };
        request.validate()?;
        Ok(request)
    }
}
impl ProduceAttestationDataRequest {
    ///Parse the HTTP response into the response enum.
    pub async fn parse_response(
        req: reqwest::Response,
    ) -> anyhow::Result<ProduceAttestationDataResponse> {
        let status = req.status();
        if status.is_success() {
            let content_type_str = req
                .headers()
                .get(reqwest::header::CONTENT_TYPE)
                .and_then(|v| v.to_str().ok())
                .unwrap_or("application/json");
            if content_type_str.contains("json") {
                let data = oas3_gen_support::Diagnostics::<
                    ProduceAttestationDataResponseResponse,
                >::json_with_diagnostics(req)
                    .await?;
                return Ok(ProduceAttestationDataResponse::Ok(data));
            }
            if content_type_str.starts_with("application/octet-stream")
                || content_type_str.starts_with("image/")
                || content_type_str.starts_with("audio/")
                || content_type_str.starts_with("video/")
            {
                let data = req.bytes().await?.to_vec();
                return Ok(ProduceAttestationDataResponse::OkBinary(data));
            }
        }
        if status.as_u16() == 400u16 {
            let data = oas3_gen_support::Diagnostics::<
                PendingConsolidation400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(ProduceAttestationDataResponse::BadRequest(data));
        }
        if status.as_u16() == 406u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock406Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(ProduceAttestationDataResponse::NotAcceptable(data));
        }
        if status.as_u16() == 500u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(ProduceAttestationDataResponse::InternalServerError(data));
        }
        if status.as_u16() == 503u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(ProduceAttestationDataResponse::ServiceUnavailable(data));
        }
        let _ = req.bytes().await?;
        return Ok(ProduceAttestationDataResponse::Unknown);
    }
}
#[derive(
    Debug,
    Clone,
    PartialEq,
    Serialize,
    validator::Validate,
    oas3_gen_support::Default
)]
pub struct ProduceAttestationDataRequestQuery {
    ///The slot for which an attestation data should be created.
    #[validate(length(min = 1u64))]
    pub slot: String,
    ///The committee index for which an attestation data should be created. For `slot`s in
    ///Electra and later, this parameter MAY always be set to 0.
    #[validate(length(min = 1u64))]
    pub committee_index: String,
}
///Response types for produceAttestationData
#[derive(Debug, Clone)]
pub enum ProduceAttestationDataResponse {
    ///200: Success response
    Ok(ProduceAttestationDataResponseResponse),
    ///200: Success response
    OkBinary(Vec<u8>),
    ///400: Invalid request syntax.
    BadRequest(PendingConsolidation400Response),
    ///406: Accepted media type is not supported.
    NotAcceptable(BlindedBlock406Response),
    ///500: Beacon node internal error.
    InternalServerError(BlindedBlock400Response),
    ///503: Beacon node is currently syncing, try again later.
    ServiceUnavailable(BlindedBlock400Response),
    ///default: Unknown response
    Unknown,
}
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct ProduceAttestationDataResponseResponse {
    ///The [`AttestationData`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/phase0/beacon-chain.md#attestationdata) object from the CL spec.
    pub data: Data,
}
///Requests a beacon node to produce a valid block, which can then be signed by a validator. The
///returned block may be blinded or unblinded, depending on the current state of the network as
///decided by the execution and beacon nodes.
///
///The beacon node must return an unblinded block if it obtains the execution payload from its
///paired execution node. It must only return a blinded block if it obtains the execution payload
///header from an MEV relay.
///
///Metadata in the response indicates the type of block produced, and the supported types of block
///will be added to as forks progress.
#[derive(Debug, Clone, validator::Validate, oas3_gen_support::Default)]
pub struct ProduceBlockV3Request {
    #[validate(nested)]
    pub path: ProduceBlockV3RequestPath,
    #[validate(nested)]
    pub query: ProduceBlockV3RequestQuery,
}
#[bon::bon]
impl ProduceBlockV3Request {
    ///Create a new request with the given parameters.
    #[builder]
    pub fn new(
        slot: String,
        randao_reveal: String,
        graffiti: Option<String>,
        skip_randao_verification: Option<String>,
        builder_boost_factor: Option<String>,
    ) -> anyhow::Result<Self> {
        let request = Self {
            path: ProduceBlockV3RequestPath { slot },
            query: ProduceBlockV3RequestQuery {
                randao_reveal,
                graffiti,
                skip_randao_verification,
                builder_boost_factor,
            },
        };
        request.validate()?;
        Ok(request)
    }
}
impl ProduceBlockV3Request {
    ///Parse the HTTP response into the response enum.
    pub async fn parse_response(
        req: reqwest::Response,
    ) -> anyhow::Result<ProduceBlockV3Response> {
        let status = req.status();
        if status.is_success() {
            let content_type_str = req
                .headers()
                .get(reqwest::header::CONTENT_TYPE)
                .and_then(|v| v.to_str().ok())
                .unwrap_or("application/json");
            if content_type_str.contains("json") {
                let data = oas3_gen_support::Diagnostics::<
                    ProduceBlockV3ResponseResponse,
                >::json_with_diagnostics(req)
                    .await?;
                return Ok(ProduceBlockV3Response::Ok(data));
            }
            if content_type_str.starts_with("application/octet-stream")
                || content_type_str.starts_with("image/")
                || content_type_str.starts_with("audio/")
                || content_type_str.starts_with("video/")
            {
                let data = req.bytes().await?.to_vec();
                return Ok(ProduceBlockV3Response::OkBinary(data));
            }
        }
        if status.as_u16() == 400u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(ProduceBlockV3Response::BadRequest(data));
        }
        if status.as_u16() == 406u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock406Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(ProduceBlockV3Response::NotAcceptable(data));
        }
        if status.as_u16() == 500u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(ProduceBlockV3Response::InternalServerError(data));
        }
        if status.as_u16() == 503u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(ProduceBlockV3Response::ServiceUnavailable(data));
        }
        let _ = req.bytes().await?;
        return Ok(ProduceBlockV3Response::Unknown);
    }
}
#[derive(Debug, Clone, PartialEq, validator::Validate, oas3_gen_support::Default)]
pub struct ProduceBlockV3RequestPath {
    ///The slot for which the block should be proposed.
    #[validate(length(min = 1u64))]
    pub slot: String,
}
#[serde_with::skip_serializing_none]
#[derive(
    Debug,
    Clone,
    PartialEq,
    Serialize,
    validator::Validate,
    oas3_gen_support::Default
)]
pub struct ProduceBlockV3RequestQuery {
    ///The validator's randao reveal value.
    #[validate(
        length(min = 1u64),
        regex(path = "REGEX_AGGREGATE_AND_PROOF_REQUEST_BODY_ARRAY_SIGNATURE")
    )]
    pub randao_reveal: String,
    ///Arbitrary data validator wants to include in block.
    #[validate(
        regex(path = "REGEX_ALTAIR_BEACON_STATE_CURRENT_JUSTIFIED_CHECKPOINT_ROOT")
    )]
    pub graffiti: Option<String>,
    ///Skip verification of the `randao_reveal` value. If this flag is set then the
    ///`randao_reveal` must be set to the point at infinity (`0xc0..00`).
    #[validate(length(min = 0u64, max = 0u64))]
    pub skip_randao_verification: Option<String>,
    ///Percentage multiplier to apply to the builder's payload value when choosing between a
    ///builder payload header and payload from the paired execution node. This parameter is only
    ///relevant if the beacon node is connected to a builder, deems it safe to produce a builder
    ///payload, and receives valid responses from both the builder endpoint _and_ the paired
    ///execution node. When these preconditions are met, the server MUST act as follows:
    ///
    ///* if `exec_node_payload_value >= builder_boost_factor * (builder_payload_value // 100)`,
    ///  then return a full (unblinded) block containing the execution node payload.
    ///* otherwise, return a blinded block containing the builder payload header.
    ///
    ///Servers must support the following values of the boost factor which encode common
    ///preferences:
    ///
    ///* `builder_boost_factor=0`: prefer the local execution node payload unless an error makes it
    ///  unviable.
    ///* `builder_boost_factor=100`: profit maximization mode; choose whichever
    ///  payload pays more.
    ///* `builder_boost_factor=2**64 - 1`: prefer the external builder payload unless an error or
    ///  beacon node health check makes it unviable.
    ///
    ///Servers should use saturating arithmetic or another technique to ensure that large values of
    ///the `builder_boost_factor` do not trigger overflows or errors. If this parameter is
    ///provided and the beacon node is not configured with a builder then the beacon node MUST
    ///respond with a full block, which the caller can choose to reject if it wishes.
    ///If the value is provided but out of range for a 64-bit unsigned integer, then an error
    ///response with status code 400 MUST be returned.
    pub builder_boost_factor: Option<String>,
}
///Response types for produceBlockV3
#[derive(Debug, Clone)]
pub enum ProduceBlockV3Response {
    ///200: Success response
    Ok(ProduceBlockV3ResponseResponse),
    ///200: Success response
    OkBinary(Vec<u8>),
    ///400: Invalid block production request
    BadRequest(BlindedBlock400Response),
    ///406: Accepted media type is not supported.
    NotAcceptable(BlindedBlock406Response),
    ///500: Beacon node internal error.
    InternalServerError(BlindedBlock400Response),
    ///503: Beacon node is currently syncing, try again later.
    ServiceUnavailable(BlindedBlock400Response),
    ///default: Unknown response
    Unknown,
}
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct ProduceBlockV3ResponseResponse {
    pub consensus_block_value: String,
    pub data: ProduceBlockV3ResponseResponseData,
    pub execution_payload_blinded: bool,
    pub execution_payload_value: String,
    pub version: ConsensusVersion,
}
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
#[serde(untagged)]
pub enum ProduceBlockV3ResponseResponseData {
    ///The required object for block production according to the Fulu CL spec.
    #[default]
    Object(ProduceBlockV3ResponseResponseDataObject),
    ///The required object for block production according to the Electra CL spec.
    Object2(ProduceBlockV3ResponseResponseDataObject2),
    ///A variant of the [`BeaconBlock`](https://github.com/ethereum/consensus-specs/blob/v1.5.0/specs/phase0/beacon-chain.md#beaconblock) object from the CL Electra spec, which contains a `BlindedBeaconBlockBody` rather than a `BeaconBlockBody`.
    Variant2(ProduceBlockV3ResponseResponseDataVariant2),
    ///The required object for block production according to the Deneb CL spec.
    Object3(ProduceBlockV3ResponseResponseDataObject3),
    ///A variant of the [`BeaconBlock`](https://github.com/ethereum/consensus-specs/blob/v1.4.0/specs/phase0/beacon-chain.md#beaconblock) object from the CL Deneb spec, which contains a `BlindedBeaconBlockBody` rather than a `BeaconBlockBody`.
    Variant4(ProduceBlockV3ResponseResponseDataVariant4),
    ///The [`BeaconBlock`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/phase0/beacon-chain.md#beaconblock) object from the CL Capella spec.
    Variant5(ProduceBlockV3ResponseResponseDataVariant5),
    ///A variant of the [`BeaconBlock`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/phase0/beacon-chain.md#beaconblock) object from the CL Capella spec, which contains a `BlindedBeaconBlockBody` rather than a `BeaconBlockBody`.
    Variant6(ProduceBlockV3ResponseResponseDataVariant6),
    ///The [`BeaconBlock`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/phase0/beacon-chain.md#beaconblock) object from the CL Bellatrix spec.
    Variant7(ProduceBlockV3ResponseResponseDataVariant7),
    ///A variant of the [`BeaconBlock`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/phase0/beacon-chain.md#beaconblock) object from the CL Bellatrix spec, which contains a `BlindedBeaconBlockBody` rather than a `BeaconBlockBody`.
    Variant8(ProduceBlockV3ResponseResponseDataVariant8),
    ///The [`BeaconBlock`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/phase0/beacon-chain.md#beaconblock) object from the CL Altair spec.
    Variant9(ProduceBlockV3ResponseResponseDataVariant9),
    ///The [`BeaconBlock`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/phase0/beacon-chain.md#beaconblock) object from the CL spec.
    Variant10(ProduceBlockV3ResponseResponseDataVariant10),
}
///The required object for block production according to the Fulu CL spec.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct ProduceBlockV3ResponseResponseDataObject {
    pub blobs: Vec<String>,
    ///The [`BeaconBlock`](https://github.com/ethereum/consensus-specs/blob/v1.5.0/specs/phase0/beacon-chain.md#beaconblock) object from the CL Electra spec.
    pub block: serde_json::Value,
    ///Cell proofs of the blobs as defined in EIP-7594
    pub kzg_proofs: Vec<String>,
}
///The required object for block production according to the Electra CL spec.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct ProduceBlockV3ResponseResponseDataObject2 {
    pub blobs: Vec<String>,
    ///The [`BeaconBlock`](https://github.com/ethereum/consensus-specs/blob/v1.5.0/specs/phase0/beacon-chain.md#beaconblock) object from the CL Electra spec.
    pub block: serde_json::Value,
    pub kzg_proofs: Vec<String>,
}
///The required object for block production according to the Deneb CL spec.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct ProduceBlockV3ResponseResponseDataObject3 {
    pub blobs: Vec<String>,
    ///The [`BeaconBlock`](https://github.com/ethereum/consensus-specs/blob/v1.4.0/specs/phase0/beacon-chain.md#beaconblock) object from the CL Deneb spec.
    pub block: serde_json::Value,
    pub kzg_proofs: Vec<String>,
}
///The [`BeaconBlock`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/phase0/beacon-chain.md#beaconblock) object from the CL spec.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct ProduceBlockV3ResponseResponseDataVariant10 {
    ///The [`BeaconBlockBody`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/phase0/beacon-chain.md#beaconblockbody) object from the CL spec.
    pub body: Phase0BeaconBlockBody,
    ///The signing merkle root of the parent `BeaconBlock`.
    pub parent_root: String,
    ///Index of validator in validator registry.
    pub proposer_index: String,
    ///The slot to which this block corresponds.
    pub slot: String,
    ///The tree hash merkle root of the `BeaconState` for the `BeaconBlock`.
    pub state_root: String,
}
///A variant of the [`BeaconBlock`](https://github.com/ethereum/consensus-specs/blob/v1.5.0/specs/phase0/beacon-chain.md#beaconblock) object from the CL Electra spec, which contains a `BlindedBeaconBlockBody` rather than a `BeaconBlockBody`.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct ProduceBlockV3ResponseResponseDataVariant2 {
    ///A variant of the [`BeaconBlockBody`](https://github.com/ethereum/consensus-specs/blob/v1.5.0/specs/electra/beacon-chain.md#beaconblockbody) object from the CL Electra spec, which contains a transactions root rather than a full transactions list.
    pub body: serde_json::Value,
    ///The signing Merkle root of the parent `BeaconBlock`.
    pub parent_root: String,
    ///Index of validator in validator registry.
    pub proposer_index: String,
    ///The slot to which this block corresponds.
    pub slot: String,
    ///The tree hash Merkle root of the `BeaconState` for the `BeaconBlock`.
    pub state_root: String,
}
///A variant of the [`BeaconBlock`](https://github.com/ethereum/consensus-specs/blob/v1.4.0/specs/phase0/beacon-chain.md#beaconblock) object from the CL Deneb spec, which contains a `BlindedBeaconBlockBody` rather than a `BeaconBlockBody`.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct ProduceBlockV3ResponseResponseDataVariant4 {
    ///A variant of the [`BeaconBlockBody`](https://github.com/ethereum/consensus-specs/blob/v1.4.0/specs/deneb/beacon-chain.md#beaconblockbody) object from the CL Deneb spec, which contains a transactions root rather than a full transactions list.
    pub body: serde_json::Value,
    ///The signing Merkle root of the parent `BeaconBlock`.
    pub parent_root: String,
    ///Index of validator in validator registry.
    pub proposer_index: String,
    ///The slot to which this block corresponds.
    pub slot: String,
    ///The tree hash Merkle root of the `BeaconState` for the `BeaconBlock`.
    pub state_root: String,
}
///The [`BeaconBlock`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/phase0/beacon-chain.md#beaconblock) object from the CL Capella spec.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct ProduceBlockV3ResponseResponseDataVariant5 {
    pub body: serde_json::Value,
    ///The signing Merkle root of the parent `BeaconBlock`.
    pub parent_root: String,
    ///Index of validator in validator registry.
    pub proposer_index: String,
    ///The slot to which this block corresponds.
    pub slot: String,
    ///The tree hash Merkle root of the `BeaconState` for the `BeaconBlock`.
    pub state_root: String,
}
///A variant of the [`BeaconBlock`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/phase0/beacon-chain.md#beaconblock) object from the CL Capella spec, which contains a `BlindedBeaconBlockBody` rather than a `BeaconBlockBody`.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct ProduceBlockV3ResponseResponseDataVariant6 {
    ///A variant of the [`BeaconBlockBody`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/capella/beacon-chain.md#beaconblockbody) object from the CL Capella spec, which contains a transactions root rather than a full transactions list.
    pub body: serde_json::Value,
    ///The signing Merkle root of the parent `BeaconBlock`.
    pub parent_root: String,
    ///Index of validator in validator registry.
    pub proposer_index: String,
    ///The slot to which this block corresponds.
    pub slot: String,
    ///The tree hash Merkle root of the `BeaconState` for the `BeaconBlock`.
    pub state_root: String,
}
///The [`BeaconBlock`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/phase0/beacon-chain.md#beaconblock) object from the CL Bellatrix spec.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct ProduceBlockV3ResponseResponseDataVariant7 {
    pub body: serde_json::Value,
    ///The signing Merkle root of the parent `BeaconBlock`.
    pub parent_root: String,
    ///Index of validator in validator registry.
    pub proposer_index: String,
    ///The slot to which this block corresponds.
    pub slot: String,
    ///The tree hash Merkle root of the `BeaconState` for the `BeaconBlock`.
    pub state_root: String,
}
///A variant of the [`BeaconBlock`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/phase0/beacon-chain.md#beaconblock) object from the CL Bellatrix spec, which contains a `BlindedBeaconBlockBody` rather than a `BeaconBlockBody`.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct ProduceBlockV3ResponseResponseDataVariant8 {
    ///A variant of the [`BeaconBlockBody`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/bellatrix/beacon-chain.md#beaconblockbody) object from the CL Bellatrix spec, which contains a transactions root rather than a full transactions list.
    pub body: serde_json::Value,
    ///The signing Merkle root of the parent `BeaconBlock`.
    pub parent_root: String,
    ///Index of validator in validator registry.
    pub proposer_index: String,
    ///The slot to which this block corresponds.
    pub slot: String,
    ///The tree hash Merkle root of the `BeaconState` for the `BeaconBlock`.
    pub state_root: String,
}
///The [`BeaconBlock`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/phase0/beacon-chain.md#beaconblock) object from the CL Altair spec.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct ProduceBlockV3ResponseResponseDataVariant9 {
    ///The [`BeaconBlockBody`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/altair/beacon-chain.md#beaconblockbody) object from the CL Altair spec.
    pub body: AltairBeaconBlockBody,
    ///The signing Merkle root of the parent `BeaconBlock`.
    pub parent_root: String,
    ///Index of validator in validator registry.
    pub proposer_index: String,
    ///The slot to which this block corresponds.
    pub slot: String,
    ///The tree hash Merkle root of the `BeaconState` for the `BeaconBlock`.
    pub state_root: String,
}
///Requests that the beacon node produce a sync committee contribution.
///
///A 503 error must be returned if the block identified by the response
///`beacon_block_root` is optimistic (i.e. the sync committee contribution
///refers to a block that has not been fully verified by an execution engine).
///
///A 404 error must be returned if no sync committee contribution is available
///for the requested `beacon_block_root`.
#[derive(Debug, Clone, validator::Validate, oas3_gen_support::Default)]
pub struct ProduceSyncCommitteeContributionRequest {
    #[validate(nested)]
    pub query: ProduceSyncCommitteeContributionRequestQuery,
}
#[bon::bon]
impl ProduceSyncCommitteeContributionRequest {
    ///Create a new request with the given parameters.
    #[builder]
    pub fn new(
        slot: String,
        subcommittee_index: String,
        beacon_block_root: String,
    ) -> anyhow::Result<Self> {
        let request = Self {
            query: ProduceSyncCommitteeContributionRequestQuery {
                slot,
                subcommittee_index,
                beacon_block_root,
            },
        };
        request.validate()?;
        Ok(request)
    }
}
impl ProduceSyncCommitteeContributionRequest {
    ///Parse the HTTP response into the response enum.
    pub async fn parse_response(
        req: reqwest::Response,
    ) -> anyhow::Result<ProduceSyncCommitteeContributionResponse> {
        let status = req.status();
        if status.is_success() {
            let data = oas3_gen_support::Diagnostics::<
                ProduceSyncCommitteeContributionResponseResponse,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(ProduceSyncCommitteeContributionResponse::Ok(data));
        }
        if status.as_u16() == 400u16 {
            let data = oas3_gen_support::Diagnostics::<
                PendingConsolidation400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(ProduceSyncCommitteeContributionResponse::BadRequest(data));
        }
        if status.as_u16() == 404u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(ProduceSyncCommitteeContributionResponse::NotFound(data));
        }
        if status.as_u16() == 500u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(
                ProduceSyncCommitteeContributionResponse::InternalServerError(data),
            );
        }
        if status.as_u16() == 503u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(
                ProduceSyncCommitteeContributionResponse::ServiceUnavailable(data),
            );
        }
        let _ = req.bytes().await?;
        return Ok(ProduceSyncCommitteeContributionResponse::Unknown);
    }
}
#[derive(
    Debug,
    Clone,
    PartialEq,
    Serialize,
    validator::Validate,
    oas3_gen_support::Default
)]
pub struct ProduceSyncCommitteeContributionRequestQuery {
    ///The slot for which a sync committee contribution should be created.
    #[validate(length(min = 1u64))]
    pub slot: String,
    ///the subcommittee index for which to produce the contribution.
    #[validate(length(min = 1u64))]
    pub subcommittee_index: String,
    ///the block root for which to produce the contribution.
    #[validate(
        length(min = 1u64),
        regex(path = "REGEX_ALTAIR_BEACON_STATE_CURRENT_JUSTIFIED_CHECKPOINT_ROOT")
    )]
    pub beacon_block_root: String,
}
///Response types for produceSyncCommitteeContribution
#[derive(Debug, Clone)]
pub enum ProduceSyncCommitteeContributionResponse {
    ///200: Success response
    Ok(ProduceSyncCommitteeContributionResponseResponse),
    ///400: Invalid request syntax.
    BadRequest(PendingConsolidation400Response),
    ///404: Not found
    NotFound(BlindedBlock400Response),
    ///500: Beacon node internal error.
    InternalServerError(BlindedBlock400Response),
    ///503: Beacon node is currently syncing, try again later.
    ServiceUnavailable(BlindedBlock400Response),
    ///default: Unknown response
    Unknown,
}
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct ProduceSyncCommitteeContributionResponseResponse {
    pub data: Contribution,
}
///Verifies given aggregate and proofs and publishes them on appropriate gossipsub topic.
#[derive(Debug, Clone, validator::Validate, oas3_gen_support::Default)]
pub struct PublishAggregateAndProofsV2Request {
    pub header: PublishAggregateAndProofsV2RequestHeader,
    pub body: AggregateAndProofRequestBody,
}
#[bon::bon]
impl PublishAggregateAndProofsV2Request {
    ///Create a new request with the given parameters.
    #[builder]
    pub fn new(
        eth_consensus_version: ConsensusVersion,
        body: AggregateAndProofRequestBody,
    ) -> anyhow::Result<Self> {
        let request = Self {
            header: PublishAggregateAndProofsV2RequestHeader {
                eth_consensus_version,
            },
            body,
        };
        request.validate()?;
        Ok(request)
    }
}
impl PublishAggregateAndProofsV2Request {
    ///Parse the HTTP response into the response enum.
    pub async fn parse_response(
        req: reqwest::Response,
    ) -> anyhow::Result<SubmitPoolAttestationsV2Response> {
        let status = req.status();
        if status.is_success() {
            let _ = req.bytes().await?;
            return Ok(SubmitPoolAttestationsV2Response::Ok);
        }
        if status.as_u16() == 400u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlsToExecutionChange400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(SubmitPoolAttestationsV2Response::BadRequest(data));
        }
        if status.as_u16() == 415u16 {
            let data = oas3_gen_support::Diagnostics::<
                RegisterValidator415Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(SubmitPoolAttestationsV2Response::UnsupportedMediaType(data));
        }
        if status.as_u16() == 500u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(SubmitPoolAttestationsV2Response::InternalServerError(data));
        }
        let _ = req.bytes().await?;
        return Ok(SubmitPoolAttestationsV2Response::Unknown);
    }
}
#[derive(Debug, Clone, PartialEq, oas3_gen_support::Default)]
pub struct PublishAggregateAndProofsV2RequestHeader {
    ///The active consensus version to which the aggregate and proofs being submitted belong.
    pub eth_consensus_version: ConsensusVersion,
}
impl core::convert::TryFrom<&PublishAggregateAndProofsV2RequestHeader>
for http::HeaderMap {
    type Error = http::header::InvalidHeaderValue;
    fn try_from(
        headers: &PublishAggregateAndProofsV2RequestHeader,
    ) -> core::result::Result<Self, Self::Error> {
        let mut map = http::HeaderMap::with_capacity(1usize);
        let header_value = http::HeaderValue::try_from(
            &headers.eth_consensus_version.to_string(),
        )?;
        map.insert(ETH_CONSENSUS_VERSION, header_value);
        Ok(map)
    }
}
impl core::convert::TryFrom<PublishAggregateAndProofsV2RequestHeader>
for http::HeaderMap {
    type Error = http::header::InvalidHeaderValue;
    fn try_from(
        headers: PublishAggregateAndProofsV2RequestHeader,
    ) -> core::result::Result<Self, Self::Error> {
        http::HeaderMap::try_from(&headers)
    }
}
///Instructs the beacon node to use the components of the `SignedBlindedBeaconBlock` to construct and publish a
///`SignedBeaconBlock` by swapping out the `transactions_root` for the corresponding full list of `transactions`.
///The beacon node should broadcast a newly constructed `SignedBeaconBlock` to the beacon network,
///to be included in the beacon chain. The beacon node is not required to validate the signed
///`BeaconBlock`, and a successful response (20X) only indicates that the broadcast has been
///successful. The beacon node is expected to integrate the new block into its state, and
///therefore validate the block internally, however blocks which fail the validation are still
///broadcast but a different status code is returned (202). Before Bellatrix, this endpoint will accept
///a `SignedBeaconBlock`. The broadcast behaviour may be adjusted via the `broadcast_validation`
///query parameter.
#[derive(Debug, Clone, validator::Validate, oas3_gen_support::Default)]
pub struct PublishBlindedBlockV2Request {
    pub query: PublishBlindedBlockV2RequestQuery,
    pub header: PublishBlindedBlockV2RequestHeader,
    ///The `SignedBlindedBeaconBlock` object composed of `BlindedBeaconBlock` object (produced by beacon node) and validator signature.
    pub body: GetBlindedBlockResponseResponseData,
}
#[bon::bon]
impl PublishBlindedBlockV2Request {
    ///Create a new request with the given parameters.
    #[builder]
    pub fn new(
        broadcast_validation: Option<BroadcastValidation>,
        eth_consensus_version: ConsensusVersion,
        body: GetBlindedBlockResponseResponseData,
    ) -> anyhow::Result<Self> {
        let request = Self {
            query: PublishBlindedBlockV2RequestQuery {
                broadcast_validation,
            },
            header: PublishBlindedBlockV2RequestHeader {
                eth_consensus_version,
            },
            body,
        };
        request.validate()?;
        Ok(request)
    }
}
impl PublishBlindedBlockV2Request {
    ///Parse the HTTP response into the response enum.
    pub async fn parse_response(
        req: reqwest::Response,
    ) -> anyhow::Result<PublishBlockV2Response> {
        let status = req.status();
        if status.is_success() {
            let _ = req.bytes().await?;
            return Ok(PublishBlockV2Response::Ok);
        }
        if status.is_success() {
            let _ = req.bytes().await?;
            return Ok(PublishBlockV2Response::Accepted);
        }
        if status.as_u16() == 400u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(PublishBlockV2Response::BadRequest(data));
        }
        if status.as_u16() == 415u16 {
            let data = oas3_gen_support::Diagnostics::<
                RegisterValidator415Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(PublishBlockV2Response::UnsupportedMediaType(data));
        }
        if status.as_u16() == 500u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(PublishBlockV2Response::InternalServerError(data));
        }
        if status.as_u16() == 503u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(PublishBlockV2Response::ServiceUnavailable(data));
        }
        let _ = req.bytes().await?;
        return Ok(PublishBlockV2Response::Unknown);
    }
}
#[derive(Debug, Clone, PartialEq, oas3_gen_support::Default)]
pub struct PublishBlindedBlockV2RequestHeader {
    ///The active consensus version to which the block being submitted belongs.
    pub eth_consensus_version: ConsensusVersion,
}
impl core::convert::TryFrom<&PublishBlindedBlockV2RequestHeader> for http::HeaderMap {
    type Error = http::header::InvalidHeaderValue;
    fn try_from(
        headers: &PublishBlindedBlockV2RequestHeader,
    ) -> core::result::Result<Self, Self::Error> {
        let mut map = http::HeaderMap::with_capacity(1usize);
        let header_value = http::HeaderValue::try_from(
            &headers.eth_consensus_version.to_string(),
        )?;
        map.insert(ETH_CONSENSUS_VERSION, header_value);
        Ok(map)
    }
}
impl core::convert::TryFrom<PublishBlindedBlockV2RequestHeader> for http::HeaderMap {
    type Error = http::header::InvalidHeaderValue;
    fn try_from(
        headers: PublishBlindedBlockV2RequestHeader,
    ) -> core::result::Result<Self, Self::Error> {
        http::HeaderMap::try_from(&headers)
    }
}
#[serde_with::skip_serializing_none]
#[derive(Debug, Clone, PartialEq, Serialize, oas3_gen_support::Default)]
pub struct PublishBlindedBlockV2RequestQuery {
    ///Level of validation that must be applied to a block before it is broadcast.
    ///
    ///Possible values:
    ///- **`gossip`** (default): lightweight gossip checks only
    ///- **`consensus`**: full consensus checks, including validation of all signatures and
    ///  blocks fields _except_ for the execution payload transactions.
    ///- **`consensus_and_equivocation`**: the same as `consensus`, with an extra equivocation
    ///  check immediately before the block is broadcast. If the block is found to be an
    ///  equivocation it fails validation.
    ///
    ///If the block fails the requested level of a validation a 400 status MUST be returned
    ///immediately and the block MUST NOT be broadcast to the network.
    ///
    ///If validation succeeds, the block must still be fully verified before it is
    ///incorporated into the state and a 20x status is returned to the caller.
    pub broadcast_validation: Option<BroadcastValidation>,
}
///Instructs the beacon node to broadcast a newly signed beacon block to the beacon network,
///to be included in the beacon chain. A success response (20x) indicates that the block
///passed gossip validation and was successfully broadcast onto the network.
///The beacon node is also expected to integrate the block into the state, but may broadcast it
///before doing so, so as to aid timely delivery of the block. Should the block fail full
///validation, a separate success response code (202) is used to indicate that the block was
///successfully broadcast but failed integration. After Deneb, this additionally instructs
///the beacon node to broadcast all given blobs. The broadcast behaviour may be adjusted via the
///`broadcast_validation` query parameter.
#[derive(Debug, Clone, validator::Validate, oas3_gen_support::Default)]
pub struct PublishBlockV2Request {
    pub query: PublishBlockV2RequestQuery,
    pub header: PublishBlockV2RequestHeader,
    ///The `SignedBeaconBlock` object composed of `BeaconBlock` object (produced by beacon node) and validator signature.
    pub body: BlockRequestBody,
}
#[bon::bon]
impl PublishBlockV2Request {
    ///Create a new request with the given parameters.
    #[builder]
    pub fn new(
        broadcast_validation: Option<BroadcastValidation>,
        eth_consensus_version: ConsensusVersion,
        body: BlockRequestBody,
    ) -> anyhow::Result<Self> {
        let request = Self {
            query: PublishBlockV2RequestQuery {
                broadcast_validation,
            },
            header: PublishBlockV2RequestHeader {
                eth_consensus_version,
            },
            body,
        };
        request.validate()?;
        Ok(request)
    }
}
impl PublishBlockV2Request {
    ///Parse the HTTP response into the response enum.
    pub async fn parse_response(
        req: reqwest::Response,
    ) -> anyhow::Result<PublishBlockV2Response> {
        let status = req.status();
        if status.is_success() {
            let _ = req.bytes().await?;
            return Ok(PublishBlockV2Response::Ok);
        }
        if status.is_success() {
            let _ = req.bytes().await?;
            return Ok(PublishBlockV2Response::Accepted);
        }
        if status.as_u16() == 400u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(PublishBlockV2Response::BadRequest(data));
        }
        if status.as_u16() == 415u16 {
            let data = oas3_gen_support::Diagnostics::<
                RegisterValidator415Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(PublishBlockV2Response::UnsupportedMediaType(data));
        }
        if status.as_u16() == 500u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(PublishBlockV2Response::InternalServerError(data));
        }
        if status.as_u16() == 503u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(PublishBlockV2Response::ServiceUnavailable(data));
        }
        let _ = req.bytes().await?;
        return Ok(PublishBlockV2Response::Unknown);
    }
}
#[derive(Debug, Clone, PartialEq, oas3_gen_support::Default)]
pub struct PublishBlockV2RequestHeader {
    ///The active consensus version to which the block being submitted belongs.
    pub eth_consensus_version: ConsensusVersion,
}
impl core::convert::TryFrom<&PublishBlockV2RequestHeader> for http::HeaderMap {
    type Error = http::header::InvalidHeaderValue;
    fn try_from(
        headers: &PublishBlockV2RequestHeader,
    ) -> core::result::Result<Self, Self::Error> {
        let mut map = http::HeaderMap::with_capacity(1usize);
        let header_value = http::HeaderValue::try_from(
            &headers.eth_consensus_version.to_string(),
        )?;
        map.insert(ETH_CONSENSUS_VERSION, header_value);
        Ok(map)
    }
}
impl core::convert::TryFrom<PublishBlockV2RequestHeader> for http::HeaderMap {
    type Error = http::header::InvalidHeaderValue;
    fn try_from(
        headers: PublishBlockV2RequestHeader,
    ) -> core::result::Result<Self, Self::Error> {
        http::HeaderMap::try_from(&headers)
    }
}
#[serde_with::skip_serializing_none]
#[derive(Debug, Clone, PartialEq, Serialize, oas3_gen_support::Default)]
pub struct PublishBlockV2RequestQuery {
    ///Level of validation that must be applied to a block before it is broadcast.
    ///
    ///Possible values:
    ///- **`gossip`** (default): lightweight gossip checks only
    ///- **`consensus`**: full consensus checks, including validation of all signatures and
    ///  blocks fields _except_ for the execution payload transactions.
    ///- **`consensus_and_equivocation`**: the same as `consensus`, with an extra equivocation
    ///  check immediately before the block is broadcast. If the block is found to be an
    ///  equivocation it fails validation.
    ///
    ///If the block fails the requested level of a validation a 400 status MUST be returned
    ///immediately and the block MUST NOT be broadcast to the network.
    ///
    ///If validation succeeds, the block must still be fully verified before it is
    ///incorporated into the state and a 20x status is returned to the caller.
    pub broadcast_validation: Option<BroadcastValidation>,
}
///Response types for publishBlockV2
#[derive(Debug, Clone)]
pub enum PublishBlockV2Response {
    ///200: The block was validated successfully and has been broadcast. It has also been integrated into the beacon node's database.
    Ok,
    ///202: The block could not be integrated into the beacon node's database as it failed validation, but was successfully broadcast.
    Accepted,
    ///400: The `SignedBeaconBlock` object is invalid or broadcast validation failed
    BadRequest(BlindedBlock400Response),
    ///415: Supplied content-type is not supported.
    UnsupportedMediaType(RegisterValidator415Response),
    ///500: Beacon node internal error.
    InternalServerError(BlindedBlock400Response),
    ///503: Beacon node is currently syncing, try again later.
    ServiceUnavailable(BlindedBlock400Response),
    ///default: Unknown response
    Unknown,
}
///Publish multiple signed sync committee contribution and proofs
#[derive(Debug, Clone, validator::Validate, oas3_gen_support::Default)]
pub struct PublishContributionAndProofsRequest {
    pub body: ContributionAndProofRequestBody,
}
#[bon::bon]
impl PublishContributionAndProofsRequest {
    ///Create a new request with the given parameters.
    #[builder]
    pub fn new(body: ContributionAndProofRequestBody) -> anyhow::Result<Self> {
        let request = Self { body };
        request.validate()?;
        Ok(request)
    }
}
impl PublishContributionAndProofsRequest {
    ///Parse the HTTP response into the response enum.
    pub async fn parse_response(
        req: reqwest::Response,
    ) -> anyhow::Result<PublishContributionAndProofsResponse> {
        let status = req.status();
        if status.is_success() {
            let _ = req.bytes().await?;
            return Ok(PublishContributionAndProofsResponse::Ok);
        }
        if status.as_u16() == 400u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlsToExecutionChange400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(PublishContributionAndProofsResponse::BadRequest(data));
        }
        if status.as_u16() == 500u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(PublishContributionAndProofsResponse::InternalServerError(data));
        }
        let _ = req.bytes().await?;
        return Ok(PublishContributionAndProofsResponse::Unknown);
    }
}
///Response types for publishContributionAndProofs
#[derive(Debug, Clone)]
pub enum PublishContributionAndProofsResponse {
    ///200: Successful response
    Ok,
    ///400: Errors with one or more contribution and proofs
    BadRequest(BlsToExecutionChange400Response),
    ///500: Beacon node internal error.
    InternalServerError(BlindedBlock400Response),
    ///default: Unknown response
    Unknown,
}
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct RegisterValidator415Response {
    ///The media type in "Content-Type" header is unsupported, and the request has been rejected. This occurs when a HTTP request supplies a payload in a content-type that the server is not able to handle.
    pub code: f64,
    ///Message describing error
    pub message: String,
    ///Optional stacktraces, sent when node is in debug mode
    pub stacktraces: Option<Vec<String>>,
}
///Prepares the beacon node for engaging with external builders. The
///information must be sent by the beacon node to the builder network. It is
///expected that the validator client will send this information periodically
///to ensure the beacon node has correct and timely registration information
///to provide to builders.
///
///Note that only registrations for active or pending validators must be sent to the builder network.
///Registrations for unknown or exited validators must be filtered out and not sent to the builder network.
#[derive(Debug, Clone, validator::Validate, oas3_gen_support::Default)]
pub struct RegisterValidatorRequest {
    pub body: RegisterValidatorRequestBody,
}
#[bon::bon]
impl RegisterValidatorRequest {
    ///Create a new request with the given parameters.
    #[builder]
    pub fn new(body: RegisterValidatorRequestBody) -> anyhow::Result<Self> {
        let request = Self { body };
        request.validate()?;
        Ok(request)
    }
}
impl RegisterValidatorRequest {
    ///Parse the HTTP response into the response enum.
    pub async fn parse_response(
        req: reqwest::Response,
    ) -> anyhow::Result<RegisterValidatorResponse> {
        let status = req.status();
        if status.is_success() {
            let _ = req.bytes().await?;
            return Ok(RegisterValidatorResponse::Ok);
        }
        if status.as_u16() == 400u16 {
            let data = oas3_gen_support::Diagnostics::<
                PendingConsolidation400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(RegisterValidatorResponse::BadRequest(data));
        }
        if status.as_u16() == 415u16 {
            let data = oas3_gen_support::Diagnostics::<
                RegisterValidator415Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(RegisterValidatorResponse::UnsupportedMediaType(data));
        }
        if status.as_u16() == 500u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(RegisterValidatorResponse::InternalServerError(data));
        }
        let _ = req.bytes().await?;
        return Ok(RegisterValidatorResponse::Unknown);
    }
}
pub type RegisterValidatorRequestBody = Vec<RegisterValidatorRequestBodyItem>;
///The `SignedValidatorRegistration` object from the Builder API specification.
#[derive(
    Debug,
    Clone,
    PartialEq,
    Serialize,
    validator::Validate,
    oas3_gen_support::Default
)]
pub struct RegisterValidatorRequestBodyItem {
    ///The `ValidatorRegistration` object from the Builder API specification.
    #[validate(nested)]
    pub message: SignedValidatorRegistrationMessage,
    #[validate(
        length(min = 1u64),
        regex(path = "REGEX_AGGREGATE_AND_PROOF_REQUEST_BODY_ARRAY_SIGNATURE")
    )]
    pub signature: String,
}
///Response types for registerValidator
#[derive(Debug, Clone)]
pub enum RegisterValidatorResponse {
    ///200: Registration information has been received.
    Ok,
    ///400: Invalid request syntax.
    BadRequest(PendingConsolidation400Response),
    ///415: Supplied content-type is not supported.
    UnsupportedMediaType(RegisterValidator415Response),
    ///500: Beacon node internal error.
    InternalServerError(BlindedBlock400Response),
    ///default: Unknown response
    Unknown,
}
///The [`SignedBeaconBlock`](https://github.com/ethereum/consensus-specs/blob/v1.5.0/specs/phase0/beacon-chain.md#signedbeaconblock) object envelope from the CL Electra spec.
#[derive(
    Debug,
    Clone,
    PartialEq,
    Serialize,
    Deserialize,
    validator::Validate,
    oas3_gen_support::Default
)]
pub struct SignedBlockContentsSignedBlock {
    ///The [`BeaconBlock`](https://github.com/ethereum/consensus-specs/blob/v1.5.0/specs/phase0/beacon-chain.md#beaconblock) object from the CL Electra spec.
    pub message: serde_json::Value,
    #[validate(
        length(min = 1u64),
        regex(path = "REGEX_AGGREGATE_AND_PROOF_REQUEST_BODY_ARRAY_SIGNATURE")
    )]
    pub signature: String,
}
///The `ValidatorRegistration` object from the Builder API specification.
#[derive(
    Debug,
    Clone,
    PartialEq,
    Serialize,
    validator::Validate,
    oas3_gen_support::Default
)]
pub struct SignedValidatorRegistrationMessage {
    ///An address on the execution (Ethereum 1) network.
    #[validate(
        length(min = 1u64),
        regex(
            path = "REGEX_CAPELLA_SIGNED_BLS_TO_EXECUTION_CHANGE_MESSAGE_TO_EXECUTION_ADDRESS"
        )
    )]
    pub fee_recipient: String,
    ///Preferred gas limit of validator.
    #[validate(length(min = 1u64))]
    pub gas_limit: String,
    ///The validator's BLS public key, uniquely identifying them. _48-bytes, hex encoded with 0x prefix, case insensitive._
    #[validate(
        length(min = 1u64),
        regex(
            path = "REGEX_CAPELLA_SIGNED_BLS_TO_EXECUTION_CHANGE_MESSAGE_FROM_BLS_PUBKEY"
        )
    )]
    pub pubkey: String,
    ///Unix timestamp of registration.
    #[validate(length(min = 1u64))]
    pub timestamp: String,
}
#[derive(Debug, Clone, PartialEq, Serialize, oas3_gen_support::Default)]
#[serde(untagged)]
pub enum StatusKind {
    ///Possible statuses:
    ///- **pending_initialized** - When the first deposit is processed, but not enough funds are available (or not yet the end of the first epoch) to get validator into the activation queue.
    ///- **pending_queued** - When validator is waiting to get activated, and have enough funds etc. while in the queue, validator activation epoch keeps changing until it gets to the front and make it through (finalization is a requirement here too).
    ///- **active_ongoing** - When validator must be attesting, and have not initiated any exit.
    ///- **active_exiting** - When validator is still active, but filed a voluntary request to exit.
    ///- **active_slashed** - When validator is still active, but have a slashed status and is scheduled to exit.
    ///- **exited_unslashed** - When validator has reached regular exit epoch, not being slashed, and doesn't have to attest any more, but cannot withdraw yet.
    ///- **exited_slashed** - When validator has reached regular exit epoch, but was slashed, have to wait for a longer withdrawal period.
    ///- **withdrawal_possible** - After validator has exited, a while later is permitted to move funds, and is truly out of the system.
    ///- **withdrawal_done** - (not possible in phase0, except slashing full balance) - actually having moved funds away
    ///
    ///[Validator status specification](https://hackmd.io/ofFJ5gOmQpu1jjHilHbdQQ)
    #[default]
    Enum(serde_json::Value),
    Enum2(serde_json::Value),
}
///This endpoint should be used by a validator client running as part of a distributed validator cluster, and is
///implemented by a distributed validator middleware client. This endpoint is used to exchange partial
///selection proofs for combined/aggregated selection proofs to allow a validator client
///to correctly determine if any of its validators has been selected to perform an attestation aggregation duty in a slot.
///Validator clients running in a distributed validator cluster must query this endpoint at the start of an epoch for the current and lookahead (next) epochs for
///all validators that have attester duties in the current and lookahead epochs. Consensus clients need not support this
///endpoint and may return a 501.
#[derive(Debug, Clone, validator::Validate, oas3_gen_support::Default)]
pub struct SubmitBeaconCommitteeSelectionsRequest {
    pub body: BeaconCommitteeSelectionRequestRequestBody,
}
#[bon::bon]
impl SubmitBeaconCommitteeSelectionsRequest {
    ///Create a new request with the given parameters.
    #[builder]
    pub fn new(
        body: BeaconCommitteeSelectionRequestRequestBody,
    ) -> anyhow::Result<Self> {
        let request = Self { body };
        request.validate()?;
        Ok(request)
    }
}
impl SubmitBeaconCommitteeSelectionsRequest {
    ///Parse the HTTP response into the response enum.
    pub async fn parse_response(
        req: reqwest::Response,
    ) -> anyhow::Result<SubmitBeaconCommitteeSelectionsResponse> {
        let status = req.status();
        if status.is_success() {
            let data = oas3_gen_support::Diagnostics::<
                BeaconCommitteeSelectionResponseResponse,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(SubmitBeaconCommitteeSelectionsResponse::Ok(data));
        }
        if status.as_u16() == 400u16 {
            let data = oas3_gen_support::Diagnostics::<
                PendingConsolidation400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(SubmitBeaconCommitteeSelectionsResponse::BadRequest(data));
        }
        if status.as_u16() == 500u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(
                SubmitBeaconCommitteeSelectionsResponse::InternalServerError(data),
            );
        }
        if status.as_u16() == 501u16 {
            let data = oas3_gen_support::Diagnostics::<
                BeaconCommitteeSelection501Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(SubmitBeaconCommitteeSelectionsResponse::NotImplemented(data));
        }
        if status.as_u16() == 503u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(SubmitBeaconCommitteeSelectionsResponse::ServiceUnavailable(data));
        }
        let _ = req.bytes().await?;
        return Ok(SubmitBeaconCommitteeSelectionsResponse::Unknown);
    }
}
///Response types for submitBeaconCommitteeSelections
#[derive(Debug, Clone)]
pub enum SubmitBeaconCommitteeSelectionsResponse {
    /**200: Returns the threshold aggregated beacon committee selection proofs.
*/
    Ok(BeaconCommitteeSelectionResponseResponse),
    ///400: Invalid request syntax.
    BadRequest(PendingConsolidation400Response),
    ///500: Beacon node internal error.
    InternalServerError(BlindedBlock400Response),
    ///501: Endpoint not implemented.
    NotImplemented(BeaconCommitteeSelection501Response),
    ///503: Beacon node is currently syncing, try again later.
    ServiceUnavailable(BlindedBlock400Response),
    ///default: Unknown response
    Unknown,
}
///Submits attestations to the node. Each attestation in the request body is processed individually.
///
///If an attestation is validated successfully, the node MUST publish that attestation on the appropriate subnet.
///
///If one or more attestations fail validation, the node MUST return a 400 error with details of which attestations have failed, and why.
///
///Prior to the Electra hard fork, this endpoint MUST be sent Attestation objects only.  At and after the Electra hard fork, this endpoint MUST be sent SingleAttestation objects only.
#[derive(Debug, Clone, validator::Validate, oas3_gen_support::Default)]
pub struct SubmitPoolAttestationsV2Request {
    pub header: SubmitPoolAttestationsV2RequestHeader,
    pub body: AttestationRequestBody2,
}
#[bon::bon]
impl SubmitPoolAttestationsV2Request {
    ///Create a new request with the given parameters.
    #[builder]
    pub fn new(
        eth_consensus_version: ConsensusVersion,
        body: AttestationRequestBody2,
    ) -> anyhow::Result<Self> {
        let request = Self {
            header: SubmitPoolAttestationsV2RequestHeader {
                eth_consensus_version,
            },
            body,
        };
        request.validate()?;
        Ok(request)
    }
}
impl SubmitPoolAttestationsV2Request {
    ///Parse the HTTP response into the response enum.
    pub async fn parse_response(
        req: reqwest::Response,
    ) -> anyhow::Result<SubmitPoolAttestationsV2Response> {
        let status = req.status();
        if status.is_success() {
            let _ = req.bytes().await?;
            return Ok(SubmitPoolAttestationsV2Response::Ok);
        }
        if status.as_u16() == 400u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlsToExecutionChange400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(SubmitPoolAttestationsV2Response::BadRequest(data));
        }
        if status.as_u16() == 415u16 {
            let data = oas3_gen_support::Diagnostics::<
                RegisterValidator415Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(SubmitPoolAttestationsV2Response::UnsupportedMediaType(data));
        }
        if status.as_u16() == 500u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(SubmitPoolAttestationsV2Response::InternalServerError(data));
        }
        let _ = req.bytes().await?;
        return Ok(SubmitPoolAttestationsV2Response::Unknown);
    }
}
#[derive(Debug, Clone, PartialEq, oas3_gen_support::Default)]
pub struct SubmitPoolAttestationsV2RequestHeader {
    ///The consensus version to which the attestations being submitted belong.
    pub eth_consensus_version: ConsensusVersion,
}
impl core::convert::TryFrom<&SubmitPoolAttestationsV2RequestHeader> for http::HeaderMap {
    type Error = http::header::InvalidHeaderValue;
    fn try_from(
        headers: &SubmitPoolAttestationsV2RequestHeader,
    ) -> core::result::Result<Self, Self::Error> {
        let mut map = http::HeaderMap::with_capacity(1usize);
        let header_value = http::HeaderValue::try_from(
            &headers.eth_consensus_version.to_string(),
        )?;
        map.insert(ETH_CONSENSUS_VERSION, header_value);
        Ok(map)
    }
}
impl core::convert::TryFrom<SubmitPoolAttestationsV2RequestHeader> for http::HeaderMap {
    type Error = http::header::InvalidHeaderValue;
    fn try_from(
        headers: SubmitPoolAttestationsV2RequestHeader,
    ) -> core::result::Result<Self, Self::Error> {
        http::HeaderMap::try_from(&headers)
    }
}
///Response types for submitPoolAttestationsV2
#[derive(Debug, Clone)]
pub enum SubmitPoolAttestationsV2Response {
    ///200: Attestations are stored in pool and broadcast on the appropriate subnet
    Ok,
    ///400: Errors with one or more attestations
    BadRequest(BlsToExecutionChange400Response),
    ///415: Supplied content-type is not supported.
    UnsupportedMediaType(RegisterValidator415Response),
    ///500: Beacon node internal error.
    InternalServerError(BlindedBlock400Response),
    ///default: Unknown response
    Unknown,
}
///Submits AttesterSlashing object to node's pool. Upon successful validation the node MUST broadcast it to network.
#[derive(Debug, Clone, validator::Validate, oas3_gen_support::Default)]
pub struct SubmitPoolAttesterSlashingsV2Request {
    pub header: SubmitPoolAttesterSlashingsV2RequestHeader,
    pub body: AttesterSlashingRequestBody,
}
#[bon::bon]
impl SubmitPoolAttesterSlashingsV2Request {
    ///Create a new request with the given parameters.
    #[builder]
    pub fn new(
        eth_consensus_version: ConsensusVersion,
        body: AttesterSlashingRequestBody,
    ) -> anyhow::Result<Self> {
        let request = Self {
            header: SubmitPoolAttesterSlashingsV2RequestHeader {
                eth_consensus_version,
            },
            body,
        };
        request.validate()?;
        Ok(request)
    }
}
impl SubmitPoolAttesterSlashingsV2Request {
    ///Parse the HTTP response into the response enum.
    pub async fn parse_response(
        req: reqwest::Response,
    ) -> anyhow::Result<SubmitPoolVoluntaryExitResponse> {
        let status = req.status();
        if status.is_success() {
            let _ = req.bytes().await?;
            return Ok(SubmitPoolVoluntaryExitResponse::Ok);
        }
        if status.as_u16() == 400u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(SubmitPoolVoluntaryExitResponse::BadRequest(data));
        }
        if status.as_u16() == 500u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(SubmitPoolVoluntaryExitResponse::InternalServerError(data));
        }
        let _ = req.bytes().await?;
        return Ok(SubmitPoolVoluntaryExitResponse::Unknown);
    }
}
#[derive(Debug, Clone, PartialEq, oas3_gen_support::Default)]
pub struct SubmitPoolAttesterSlashingsV2RequestHeader {
    ///The active consensus version to which the attester slashing being submitted belongs.
    pub eth_consensus_version: ConsensusVersion,
}
impl core::convert::TryFrom<&SubmitPoolAttesterSlashingsV2RequestHeader>
for http::HeaderMap {
    type Error = http::header::InvalidHeaderValue;
    fn try_from(
        headers: &SubmitPoolAttesterSlashingsV2RequestHeader,
    ) -> core::result::Result<Self, Self::Error> {
        let mut map = http::HeaderMap::with_capacity(1usize);
        let header_value = http::HeaderValue::try_from(
            &headers.eth_consensus_version.to_string(),
        )?;
        map.insert(ETH_CONSENSUS_VERSION, header_value);
        Ok(map)
    }
}
impl core::convert::TryFrom<SubmitPoolAttesterSlashingsV2RequestHeader>
for http::HeaderMap {
    type Error = http::header::InvalidHeaderValue;
    fn try_from(
        headers: SubmitPoolAttesterSlashingsV2RequestHeader,
    ) -> core::result::Result<Self, Self::Error> {
        http::HeaderMap::try_from(&headers)
    }
}
///Submits a list of SignedBLSToExecutionChange objects to node's pool. Any that pass validation MUST be broadcast to the network.
#[derive(Debug, Clone, validator::Validate, oas3_gen_support::Default)]
pub struct SubmitPoolBlstoExecutionChangeRequest {
    pub body: BlsToExecutionChangeRequestBody,
}
#[bon::bon]
impl SubmitPoolBlstoExecutionChangeRequest {
    ///Create a new request with the given parameters.
    #[builder]
    pub fn new(body: BlsToExecutionChangeRequestBody) -> anyhow::Result<Self> {
        let request = Self { body };
        request.validate()?;
        Ok(request)
    }
}
impl SubmitPoolBlstoExecutionChangeRequest {
    ///Parse the HTTP response into the response enum.
    pub async fn parse_response(
        req: reqwest::Response,
    ) -> anyhow::Result<PublishContributionAndProofsResponse> {
        let status = req.status();
        if status.is_success() {
            let _ = req.bytes().await?;
            return Ok(PublishContributionAndProofsResponse::Ok);
        }
        if status.as_u16() == 400u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlsToExecutionChange400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(PublishContributionAndProofsResponse::BadRequest(data));
        }
        if status.as_u16() == 500u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(PublishContributionAndProofsResponse::InternalServerError(data));
        }
        let _ = req.bytes().await?;
        return Ok(PublishContributionAndProofsResponse::Unknown);
    }
}
///Submits ProposerSlashing object to node's pool and if passes validation  node MUST broadcast it to network.
#[derive(Debug, Clone, validator::Validate, oas3_gen_support::Default)]
pub struct SubmitPoolProposerSlashingsRequest {
    #[validate(nested)]
    pub body: GetPoolProposerSlashingsResponseResponseDatum,
}
#[bon::bon]
impl SubmitPoolProposerSlashingsRequest {
    ///Create a new request with the given parameters.
    #[builder]
    pub fn new(
        body: GetPoolProposerSlashingsResponseResponseDatum,
    ) -> anyhow::Result<Self> {
        let request = Self { body };
        request.validate()?;
        Ok(request)
    }
}
impl SubmitPoolProposerSlashingsRequest {
    ///Parse the HTTP response into the response enum.
    pub async fn parse_response(
        req: reqwest::Response,
    ) -> anyhow::Result<SubmitPoolVoluntaryExitResponse> {
        let status = req.status();
        if status.is_success() {
            let _ = req.bytes().await?;
            return Ok(SubmitPoolVoluntaryExitResponse::Ok);
        }
        if status.as_u16() == 400u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(SubmitPoolVoluntaryExitResponse::BadRequest(data));
        }
        if status.as_u16() == 500u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(SubmitPoolVoluntaryExitResponse::InternalServerError(data));
        }
        let _ = req.bytes().await?;
        return Ok(SubmitPoolVoluntaryExitResponse::Unknown);
    }
}
///Submits sync committee signature objects to the node.
///
///Sync committee signatures are not present in phase0, but are required for Altair networks.
///
///If a sync committee signature is validated successfully the node MUST publish that sync committee signature on all applicable subnets.
///
///If one or more sync committee signatures fail validation the node MUST return a 400 error with details of which sync committee signatures have failed, and why.
#[derive(Debug, Clone, validator::Validate, oas3_gen_support::Default)]
pub struct SubmitPoolSyncCommitteeSignaturesRequest {
    pub body: SyncCommitteeRequestBody,
}
#[bon::bon]
impl SubmitPoolSyncCommitteeSignaturesRequest {
    ///Create a new request with the given parameters.
    #[builder]
    pub fn new(body: SyncCommitteeRequestBody) -> anyhow::Result<Self> {
        let request = Self { body };
        request.validate()?;
        Ok(request)
    }
}
impl SubmitPoolSyncCommitteeSignaturesRequest {
    ///Parse the HTTP response into the response enum.
    pub async fn parse_response(
        req: reqwest::Response,
    ) -> anyhow::Result<PublishContributionAndProofsResponse> {
        let status = req.status();
        if status.is_success() {
            let _ = req.bytes().await?;
            return Ok(PublishContributionAndProofsResponse::Ok);
        }
        if status.as_u16() == 400u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlsToExecutionChange400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(PublishContributionAndProofsResponse::BadRequest(data));
        }
        if status.as_u16() == 500u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(PublishContributionAndProofsResponse::InternalServerError(data));
        }
        let _ = req.bytes().await?;
        return Ok(PublishContributionAndProofsResponse::Unknown);
    }
}
///Submits SignedVoluntaryExit object to node's pool and if passes validation node MUST broadcast it to network.
#[derive(Debug, Clone, validator::Validate, oas3_gen_support::Default)]
pub struct SubmitPoolVoluntaryExitRequest {
    #[validate(nested)]
    pub body: GetPoolVoluntaryExitsResponseResponseDatum,
}
#[bon::bon]
impl SubmitPoolVoluntaryExitRequest {
    ///Create a new request with the given parameters.
    #[builder]
    pub fn new(
        body: GetPoolVoluntaryExitsResponseResponseDatum,
    ) -> anyhow::Result<Self> {
        let request = Self { body };
        request.validate()?;
        Ok(request)
    }
}
impl SubmitPoolVoluntaryExitRequest {
    ///Parse the HTTP response into the response enum.
    pub async fn parse_response(
        req: reqwest::Response,
    ) -> anyhow::Result<SubmitPoolVoluntaryExitResponse> {
        let status = req.status();
        if status.is_success() {
            let _ = req.bytes().await?;
            return Ok(SubmitPoolVoluntaryExitResponse::Ok);
        }
        if status.as_u16() == 400u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(SubmitPoolVoluntaryExitResponse::BadRequest(data));
        }
        if status.as_u16() == 500u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(SubmitPoolVoluntaryExitResponse::InternalServerError(data));
        }
        let _ = req.bytes().await?;
        return Ok(SubmitPoolVoluntaryExitResponse::Unknown);
    }
}
///Response types for submitPoolVoluntaryExit
#[derive(Debug, Clone)]
pub enum SubmitPoolVoluntaryExitResponse {
    ///200: Voluntary exit is stored in node and broadcasted to network
    Ok,
    ///400: Invalid voluntary exit
    BadRequest(BlindedBlock400Response),
    ///500: Beacon node internal error.
    InternalServerError(BlindedBlock400Response),
    ///default: Unknown response
    Unknown,
}
///Submit sync committee selections to a DVT middleware client. It returns the threshold aggregated sync
///committee selection. This endpoint should be used by a validator client running as part of a distributed
///validator cluster, and is implemented by a distributed validator middleware client. This endpoint is
///used to exchange partial selection proofs (slot signatures) for combined/aggregated selection proofs to
///allow a validator client to correctly determine if any of its validators has been selected to perform a
///sync committee contribution (sync aggregation) duty in a slot. Validator clients running in a distributed validator cluster must query this endpoint
///at the start of each slot for all validators that are included in the current sync committee. Consensus
///clients need not support this endpoint and may return a 501.
#[derive(Debug, Clone, validator::Validate, oas3_gen_support::Default)]
pub struct SubmitSyncCommitteeSelectionsRequest {
    pub body: SyncCommitteeSelectionRequestRequestBody,
}
#[bon::bon]
impl SubmitSyncCommitteeSelectionsRequest {
    ///Create a new request with the given parameters.
    #[builder]
    pub fn new(body: SyncCommitteeSelectionRequestRequestBody) -> anyhow::Result<Self> {
        let request = Self { body };
        request.validate()?;
        Ok(request)
    }
}
impl SubmitSyncCommitteeSelectionsRequest {
    ///Parse the HTTP response into the response enum.
    pub async fn parse_response(
        req: reqwest::Response,
    ) -> anyhow::Result<SubmitSyncCommitteeSelectionsResponse> {
        let status = req.status();
        if status.is_success() {
            let data = oas3_gen_support::Diagnostics::<
                SyncCommitteeSelectionResponseResponse,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(SubmitSyncCommitteeSelectionsResponse::Ok(data));
        }
        if status.as_u16() == 400u16 {
            let data = oas3_gen_support::Diagnostics::<
                PendingConsolidation400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(SubmitSyncCommitteeSelectionsResponse::BadRequest(data));
        }
        if status.as_u16() == 500u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(SubmitSyncCommitteeSelectionsResponse::InternalServerError(data));
        }
        if status.as_u16() == 501u16 {
            let data = oas3_gen_support::Diagnostics::<
                BeaconCommitteeSelection501Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(SubmitSyncCommitteeSelectionsResponse::NotImplemented(data));
        }
        if status.as_u16() == 503u16 {
            let data = oas3_gen_support::Diagnostics::<
                BlindedBlock400Response,
            >::json_with_diagnostics(req)
                .await?;
            return Ok(SubmitSyncCommitteeSelectionsResponse::ServiceUnavailable(data));
        }
        let _ = req.bytes().await?;
        return Ok(SubmitSyncCommitteeSelectionsResponse::Unknown);
    }
}
///Response types for submitSyncCommitteeSelections
#[derive(Debug, Clone)]
pub enum SubmitSyncCommitteeSelectionsResponse {
    /**200: Returns the threshold aggregated sync committee selection proofs.
*/
    Ok(SyncCommitteeSelectionResponseResponse),
    ///400: Invalid request syntax.
    BadRequest(PendingConsolidation400Response),
    ///500: Beacon node internal error.
    InternalServerError(BlindedBlock400Response),
    ///501: Endpoint not implemented.
    NotImplemented(BeaconCommitteeSelection501Response),
    ///503: Beacon node is currently syncing, try again later.
    ServiceUnavailable(BlindedBlock400Response),
    ///default: Unknown response
    Unknown,
}
pub type SubscribeToBeaconCommitteeSubnetRequestBodyRequestBody = Vec<
    SubscribeToBeaconCommitteeSubnetRequestBodyRequestBodyItem,
>;
#[derive(
    Debug,
    Clone,
    PartialEq,
    Serialize,
    validator::Validate,
    oas3_gen_support::Default
)]
pub struct SubscribeToBeaconCommitteeSubnetRequestBodyRequestBodyItem {
    #[validate(length(min = 1u64))]
    pub committee_index: String,
    ///Number of committees at the returned slot
    #[validate(length(min = 1u64))]
    pub committees_at_slot: String,
    ///Signals to BN that a validator on the VC has been chosen for aggregator role.
    pub is_aggregator: bool,
    ///Should be slot at which validator is assigned to attest
    #[validate(length(min = 1u64))]
    pub slot: String,
    #[validate(length(min = 1u64))]
    pub validator_index: String,
}
///The [`SyncAggregate`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/altair/beacon-chain.md#syncaggregate) object from the CL Altair spec.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct SyncAggregate {
    ///Aggregation bits of sync
    pub sync_committee_bits: String,
    pub sync_committee_signature: String,
}
pub type SyncCommitteeRequestBody = Vec<SyncCommitteeRequestBodyItem>;
#[derive(
    Debug,
    Clone,
    PartialEq,
    Serialize,
    validator::Validate,
    oas3_gen_support::Default
)]
pub struct SyncCommitteeRequestBodyItem {
    #[validate(
        length(min = 1u64),
        regex(path = "REGEX_ALTAIR_BEACON_STATE_CURRENT_JUSTIFIED_CHECKPOINT_ROOT")
    )]
    pub beacon_block_root: String,
    #[validate(
        length(min = 1u64),
        regex(path = "REGEX_AGGREGATE_AND_PROOF_REQUEST_BODY_ARRAY_SIGNATURE")
    )]
    pub signature: String,
    #[validate(length(min = 1u64))]
    pub slot: String,
    #[validate(length(min = 1u64))]
    pub validator_index: String,
}
pub type SyncCommitteeSelectionRequestRequestBody = Vec<
    SyncCommitteeSelectionRequestRequestBodyItem,
>;
#[derive(
    Debug,
    Clone,
    PartialEq,
    Serialize,
    Deserialize,
    validator::Validate,
    oas3_gen_support::Default
)]
pub struct SyncCommitteeSelectionRequestRequestBodyItem {
    ///The `slot_signature` calculated by the validator for the upcoming sync committee slot
    #[validate(
        length(min = 1u64),
        regex(path = "REGEX_AGGREGATE_AND_PROOF_REQUEST_BODY_ARRAY_SIGNATURE")
    )]
    pub selection_proof: String,
    ///The slot at which validator is assigned to produce a sync committee contribution
    #[validate(length(min = 1u64))]
    pub slot: String,
    ///SubcommitteeIndex to which the validator is assigned
    #[validate(length(min = 1u64))]
    pub subcommittee_index: String,
    ///Index of the validator
    #[validate(length(min = 1u64))]
    pub validator_index: String,
}
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct SyncCommitteeSelectionResponseResponse {
    pub data: Vec<SyncCommitteeSelectionRequestRequestBodyItem>,
}
pub type SyncCommitteeSubscriptionRequestBody = Vec<
    SyncCommitteeSubscriptionRequestBodyItem,
>;
#[derive(
    Debug,
    Clone,
    PartialEq,
    Serialize,
    validator::Validate,
    oas3_gen_support::Default
)]
pub struct SyncCommitteeSubscriptionRequestBodyItem {
    pub sync_committee_indices: Vec<String>,
    ///The final epoch (exclusive value) that the specified validator requires the subscription for.
    #[validate(length(min = 1u64))]
    pub until_epoch: String,
    #[validate(length(min = 1u64))]
    pub validator_index: String,
}
#[serde_with::skip_serializing_none]
#[derive(Debug, Clone, PartialEq, Serialize, oas3_gen_support::Default)]
pub struct ValidatorRequestBody {
    ///An array of values, with each value either a hex encoded public key (any bytes48 with 0x prefix) or a validator index.
    ///
    ///If the supplied list is empty (i.e. the value is `[]`) or the property is omitted then all validators will be returned.
    pub ids: Option<Vec<String>>,
    ///An array of validator statuses to filter on.
    ///
    ///If the supplied list is empty (i.e. the value is `[]`) or the property is omitted then validators with all statuses will be returned.
    pub statuses: Option<Vec<StatusKind>>,
}
///The [`Validator`](https://github.com/ethereum/consensus-specs/blob/v1.3.0/specs/phase0/beacon-chain.md#validator) object from the CL spec.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize, oas3_gen_support::Default)]
pub struct ValidatorResponseValidator {
    ///When criteria for activation were met.
    pub activation_eligibility_epoch: String,
    ///Epoch when validator activated. 'FAR_FUTURE_EPOCH' if not activated
    pub activation_epoch: String,
    ///Balance at stake in Gwei.
    pub effective_balance: String,
    ///Epoch when validator exited. 'FAR_FUTURE_EPOCH' if not exited.
    pub exit_epoch: String,
    ///The validator's BLS public key, uniquely identifying them. _48-bytes, hex encoded with 0x prefix, case insensitive._
    pub pubkey: String,
    ///Was validator slashed (not longer active).
    pub slashed: bool,
    ///When validator can withdraw or transfer funds. 'FAR_FUTURE_EPOCH' if not defined
    pub withdrawable_epoch: String,
    ///Root of withdrawal credentials
    pub withdrawal_credentials: String,
}
///Possible statuses:
///- **pending_initialized** - When the first deposit is processed, but not enough funds are available (or not yet the end of the first epoch) to get validator into the activation queue.
///- **pending_queued** - When validator is waiting to get activated, and have enough funds etc. while in the queue, validator activation epoch keeps changing until it gets to the front and make it through (finalization is a requirement here too).
///- **active_ongoing** - When validator must be attesting, and have not initiated any exit.
///- **active_exiting** - When validator is still active, but filed a voluntary request to exit.
///- **active_slashed** - When validator is still active, but have a slashed status and is scheduled to exit.
///- **exited_unslashed** - When validator has reached regular exit epoch, not being slashed, and doesn't have to attest any more, but cannot withdraw yet.
///- **exited_slashed** - When validator has reached regular exit epoch, but was slashed, have to wait for a longer withdrawal period.
///- **withdrawal_possible** - After validator has exited, a while later is permitted to move funds, and is truly out of the system.
///- **withdrawal_done** - (not possible in phase0, except slashing full balance) - actually having moved funds away
///
///[Validator status specification](https://hackmd.io/ofFJ5gOmQpu1jjHilHbdQQ)
#[derive(Debug, Clone, PartialEq, Eq, Hash, Serialize, Deserialize, oas3_gen_support::Default)]
pub enum ValidatorStatus {
    #[serde(rename = "pending_initialized")]
    #[default]
    PendingInitialized,
    #[serde(rename = "pending_queued")]
    PendingQueued,
    #[serde(rename = "active_ongoing")]
    ActiveOngoing,
    #[serde(rename = "active_exiting")]
    ActiveExiting,
    #[serde(rename = "active_slashed")]
    ActiveSlashed,
    #[serde(rename = "exited_unslashed")]
    ExitedUnslashed,
    #[serde(rename = "exited_slashed")]
    ExitedSlashed,
    #[serde(rename = "withdrawal_possible")]
    WithdrawalPossible,
    #[serde(rename = "withdrawal_done")]
    WithdrawalDone,
}
impl core::fmt::Display for ValidatorStatus {
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        match self {
            Self::PendingInitialized => write!(f, "pending_initialized"),
            Self::PendingQueued => write!(f, "pending_queued"),
            Self::ActiveOngoing => write!(f, "active_ongoing"),
            Self::ActiveExiting => write!(f, "active_exiting"),
            Self::ActiveSlashed => write!(f, "active_slashed"),
            Self::ExitedUnslashed => write!(f, "exited_unslashed"),
            Self::ExitedSlashed => write!(f, "exited_slashed"),
            Self::WithdrawalPossible => write!(f, "withdrawal_possible"),
            Self::WithdrawalDone => write!(f, "withdrawal_done"),
        }
    }
}

